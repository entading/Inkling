# F1 实施记录 · 阅读字体管理器（2026-09-03）

设置页「阅读排版」卡：正文字体（默认衬线/无衬线/导入字体）+ 字号档 + 行距档 + 样张预览 + 恢复默认。
导入字体走「文件上传 → 服务端 cn-font-split 运行时分片 → @font-face self-host」，桌面与移动端（LAN）完整功能。
设计决策 D1–D7 / E1–E6 全部经用户拍板（见会话记录），本记录只陈述实施事实。

## 复检（2026-09-03，同日对抗性重验）

静态走查 + 对抗矩阵，**发现并修复 1 缺陷**，其余边界全过。

### R-1（缺陷，已修）死字体 id 幽灵偏好

- **复现**：localStorage 写入已删除字体的 id（他页签/他端删除后遗留）→ 刷新。现象：`applyReadingPreferencesAtBoot` 用确定性 family 兜底把 inline `--font-serif` 设为不存在的 `'inkling-font-<死id>', …`；设置页「正文字体」radiogroup **无任何选中项**（列表无此行、预设也不亮）——UI 选中态与实际渲染脱节的幽灵态。渲染本身无害（回落 Charter），但状态不可理解。
- **修复**（两处配套）：① `lib/readingFont.ts` 的 `getReadingFonts()` 每次真实拉取 apply 后校验：偏好非 `''`/`'sans'` 且列表无此 id → `setFontPreference('')`（写键 + 清 inline + 广播；广播对端 resync 后校验通过不再广播，无环）；② `Settings.vue` watch `[readingFonts, fontPref]` 联动：ref 指向列表外 id 时清空，消除跨页签场景的选中态幽灵。
- **回归**：重放死 id → 刷新 → fontKey 归 ''、inline 移除、「默认衬线」radio 正确选中、样张回 Charter 栈。

### 对抗矩阵（全部实测）

| # | 项 | 结果 |
|---|---|---|
| A1 | 重名两条导入 | 共存无害（显示名非唯一键，id 区分） |
| A2 | 串行队列时序 | 双上传第二个明显晚于第一个 pending→ready |
| A3 | `file/:name` 边界 | `.`→400、`..%5C`→400、幽灵 id→404；`fonts.css` 白名单内可拉（content-type 标 font/woff2 不精确，无消费方，无害观察不修） |
| A4 | 中文显示名 | 页内 fetch multipart UTF-8 无损：导入→ready→删除全链路 |
| A5 | UI 层字号隔离 | lg/md 两档侧栏字号恒 15.2px（`--note-body-size` 解耦设计生效） |
| A6 | 移动端设置页 375×812 | 卡片堆叠/seg 控件/导入行折行/样张/恢复默认全正常，无横滚 |
| A7 | 坏 manifest 启动容错 | 坏 JSON→「加载失败（已跳过）」warn；形状非法→「形状非法，已跳过」warn；服务不拒启、列表 200 |
| A8 | 修复后回归 | 上传→ready→删除 链路照常 |

### 复检结论

R-1 修复引入 2 处小改动（lib +7 行 / Settings +8 行），无契约变更；`fonts.css` content-type 观察记为已知限制不修。

## 执行计划（已全部实施）

### 服务端（3 新文件 + 2 改）

| 文件 | 内容 |
|---|---|
| `server/scripts/split-font.mjs`（新） | 分片子进程 CLI：`--in 源文件 --out 产物目录 --family <名> --base-url <url 前缀>`；`fontSplit({ input: Uint8Array, ... })` 与 M2' build-fonts 同源；剥 CSS 注释、url 重写为 `<base-url>/<分片名>`、清非 woff2 中间产物、落 fonts.css；尾部 `process.exit(0)`（Windows FFI 退出段错误规避，勿删） |
| `server/src/fontLibrary.ts`（新） | 存储 `data/fonts/<id>/`（manifest.json 原子写 + source.<ext> + chunks/）；模块级启动扫描（损坏跳过、pending 残留→failed『导入被服务重启中断』）；`detectFontFormat` 魔数（ttf `\x00\x01\x00\x00` / otf `OTTO` / woff2 `wOF2`，woff/ttc 拒）；分片任务串行 promise 队列 + 子进程 spawn + `activeSplits` 登记表；`removeFont` 先杀在途子进程再 3×250ms 重试 rmSync；`aggregateFontCss` 聚合 ready 字体的 @font-face |
| `server/src/routes.ts`（改） | 顶部 `app.register(multipart, { limits: { fileSize: 30MB } })`（随 LAN 重建重注册）；四端点：`GET /api/fonts`（列表）、`POST /api/fonts`（multipart：name 1–32 字符 + 文件，魔数校验→落盘→pending manifest→立即应答→入队分片）、`GET /api/fonts/css`（聚合 text/css）、`GET /api/fonts/:id/file/:name`（流式回 chunk + immutable 缓存，文件名白名单 + `..` 拒 + 路径包含双保险）、`DELETE /api/fonts/:id`（整目录移除） |
| `server/package.json`（改） | + `@fastify/multipart ^9.0.3`（dependencies，用户 D6 拍板） |
| 根 `package.json` + `web/package.json`（改） | `cn-font-split ^7.4.3` 自 web devDeps **挪根 devDeps**（E6：server 子进程可解析，web 的 build:fonts 经 hoisting 照常） |
| `.gitignore`（改） | + `data/fonts/`（二进制资产不入库；`data/tags.json` 跟踪策略不变） |

### 前端（7 改）

| 文件 | 内容 |
|---|---|
| `web/src/lib/readingFont.ts`（新） | 三键偏好（`en_tool:reading-font/-size/-line`）读写 + 应用；列表缓存照 tagRegistry 模式（Promise 缓存 + `readingFontsRef` shallowRef + mutationSeq 守卫）；BroadcastChannel `en_tool:reading-font` + focus/visibility 5s 节流（非安全上下文降级）；`applyFontFamily` 栈首插入——默认栈串**从 CSSOM 读 tokens.css 的 `--font-serif` 定义**（唯一来源勿硬码），`sans` 引用 `var(--font-sans)`；@font-face 动态 `<style id="dynamic-font-faces">` 注入（fetch `/api/fonts/css`，列表变更重注入）；删除正用字体偏好自动回落 |
| `web/src/api.ts`（改） | + `FontEntry` 类型 + `fonts()` / `importFont(name, file)`（FormData，勿手动设 Content-Type）/ `deleteFont(id)` |
| `web/src/main.ts`（改） | + `applyReadingPreferencesAtBoot()`（幂等重设 data 属性 + 字体族）+ `getReadingFonts()` 预载（失败静默） |
| `web/src/styles/tokens.css`（改） | + `--note-body-size`（默认 `var(--text-body)`）与 `--note-line-height: 1.8`；`[data-reading-size='sm'/'lg']` 0.90625/1.1875rem、`[data-reading-line='tight'/'loose']` 1.6/2；`@media (max-width:767px)` 内 `:root` 1rem + sm/lg 0.90625/1.09375rem（**属性规则置后压过同特异性 :root**）；`--text-body`/`--text-md` 本体不动（UI 层零影响） |
| `web/src/components/MarkdownViewer.vue`（改，仅 2 规则） | `.note-body` 的 `font-size: var(--text-body)` → `var(--note-body-size)`、`line-height: 1.8` → `var(--note-line-height)`（桌面+移动两处）；标题 em 阶梯自动跟随 |
| `web/src/index.html`（改） | 防闪内联脚本扩展：读 size/line 两键，合法值设 `data-reading-size/line`（默认档不设属性）。**E5：字体族不进防闪**（webfont 异步加载固有 FOUT，防闪无收益） |
| `web/src/views/Settings.vue`（改） | 「外观」后新增「阅读排版」卡：正文字体 seg（复用 theme-seg 结构 + 通用化 `segKeydown` 键盘导航）+ 导入字体列表（名称/大小/状态·片数/使用中徽标/删除）+ 导入内联命名表单（禁 window 弹窗，T2 同款）+ 隐藏 file input + 字号/行距 seg + 中英混排样张（实时套用三维偏好）+ 恢复默认；pending 时 2s 轮询列表（watch 驱动、卸载清理） |

## 偏离与理由

1. **413 检测按消息双保险**：`@fastify/multipart` 超限抛错的 code 并非预设的 `FST_PART_FILE_TOO_LARGE`（实测落到通用 400），改为 code 或 `/too large/i` 消息双匹配——实测 31MB 上传返回 413。
2. **removeFont 重试兜底（计划外加固）**：验证中 DELETE 撞 Windows 暂态 EPERM（目录被 AV/索引器/迟释放句柄占用），3 次 250ms 重试后成功。同步 sleep 用 `Atomics.wait`（Node 主线程允许，保同步签名）。
3. **删除竞态修复（计划外发现，重要）**：分片子进程运行中 DELETE 条目，rmSync 删掉的目录会被继续写盘的子进程「复活」（实测 292 个孤儿 chunks）。修复：`activeSplits` Map 登记在途子进程，`removeFont` 先 `kill()` 再删 + 重试。实测「上传 3 秒后删除」目录彻底消失。
4. **@font-face 注入形态**：计划提过 head 静态 `<link href="/api/fonts/css">`，实施改为**动态 `<style>` 注入**——导入/删除后重注入语义可控（静态 link 无法失效缓存），视觉等价。
5. **segKeydown 通用化**：三组 radiogroup 共用一个键盘导航实现（原 theme 键盘模式的参数化），theme 本体未动。
6. **IAB 文件选择器不支持**（capability_unsupported，Codex 同款边界）：上传链路以 curl + **页面内 fetch FormData** 实测（同一 API 面）；UI 的列表/选中/删除/轮询环节全部真实点击验证。
7. **pending UI 窗口捕捉**：思源分片 ~10s，跨工具调用间隔会错过；改用「同一 JS cell 内上传→立即 reload→读」零间隔方案，成功观测「分片中…」置灰 → 轮询翻转。

## 验证矩阵（全部实测通过）

### 服务端（curl）
| # | 项 | 结果 |
|---|---|---|
| 1 | arial.ttf 冒烟（1MB） | ready，39 片，<5s |
| 2 | 思源宋体中文大字体（11.6MB otf） | ready，291 片，~10s |
| 3 | woff2 输入（E3） | ready，2 片 |
| 4 | E4 源文件删除 | ready 后仅 chunks/ + manifest.json |
| 5 | CSS 聚合 | @font-face + unicode-range + url 指向 API |
| 6 | chunk 回源 | 200 / font/woff2 / immutable 缓存头 |
| 7 | 魔数矩阵 | 文本改名 .ttf→400；msyh.ttc→400 |
| 8 | 名称校验 | 空→400；33 字符→400 |
| 9 | 超限 | 31MB→413（修复后） |
| 10 | 路径穿越 | `..%2F`→400 |
| 11 | DELETE | 200→列表空；重复→404 |
| 12 | 分片中删除竞态 | kill 子进程后目录彻底消失 |
| 13 | 重启持久化 | 列表/chunk 回源照常 |
| 14 | pending 孤儿 | 重启扫描→failed『导入被服务重启中断』 |
| 15 | LAN 切换存活 | 192.168.10.4:3000 列表+chunk 200，关闭还原 |

### 前端（IAB，localhost:5173）
| # | 项 | 结果 |
|---|---|---|
| 1 | 卡片渲染 | 预设 seg/导入按钮/档位/样张/恢复默认齐备 |
| 2 | 字号大+行距宽松 | localStorage lg/loose + dataset + 变量 1.1875rem/2 + 样张 19px/38px |
| 3 | 阅读页跟随 | .note-body 19px/38px；h1 28.5px（em 跟随） |
| 4 | 导入字体选中 | 行选中+使用中徽标；inline `--font-serif` = `'inkling-font-<id>', Charter…`（CSSOM 读串栈首插入） |
| 5 | webfont 真实渲染 | document.fonts 40 分片 loaded；fonts.check 拉丁 T/中文 意 均 true |
| 6 | 无衬线预设 | key=sans，inline=`var(--font-sans)`，样张渲染 Inter |
| 7 | 删除正用字体 | key=''、inline 移除、列表清空、样张回 Charter |
| 8 | 跨页签广播 | 页签 B 改「小」→ 页签 A dataset 即时 sm（无需 reload） |
| 9 | 移动端三档（375×812） | sm 14.5px / md 16px / lg 17.5px（D7 等比收窄） |
| 10 | pending UI | 「分片中…」+ dimmed + disabled（同 cell 捕捉） |
| 11 | 轮询翻转 | pending→failed 实测（拼接伪 woff2 被拒）；ready 路径服务端已验 |
| 12 | 编辑页预览同步 | .note-body 17px/30.6px 与阅读页一致 |
| 13 | 恢复默认 | 三键清空，样张 17px/30.6px |
| 14 | 视觉 QA 截图 | 卡片布局/分段控件/ghost 导入按钮/样张无异常 |
| 15 | 构建产物 | npm run build 全绿；dist CSS 含档位规则（压缩去引号形态）；dist/index.html 防闪脚本逐字保留 |

## 已知限制

- **字体显示名不查重**（id 为唯一键，重名两条共存无害）；单 weight 400，粗体浏览器合成（faux bold）。
- **IAB 无法驱动真实文件选择器**：桌面浏览器「选文件→命名→确认」完整人工路径未在 IAB 实测（curl + 页面内 fetch 覆盖同一 API 面）；用户实测若遇 UI 问题再修。
- **防闪范围仅字号/行距**（E5 拍板）：选了导入字体/无衬线时，刷新首帧有一瞬默认衬线（webfont/样式注入异步固有）。
- **data/fonts/ 不入库**：迁移/备份需自行拷贝该目录（与 notes/ 的 git 跟踪策略不同，gitignore 已注明）。
- **偏好不跨设备**：三键 localStorage 每浏览器各自记忆（与主题/TTS 同策略）；导入字体存服务端全设备共用。
- 手机 LAN（http 非安全上下文）无 BroadcastChannel，自动降级为 focus/visibility 重拉。
- 分片期间 CPU 占用明显（cn-font-split 多线程，~10s），串行队列保证不并发叠加。
