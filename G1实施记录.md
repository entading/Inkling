# G1 实施记录 · 数据目录管理

> 日期：2026-09-04。执行会话：ZCode（设计评审 + 实施一体）。
> 设计权威：会话内评审定稿的执行计划（本文件「执行计划」节为快照）。

## 执行计划（批准版快照）

需求：数据目录可配置化——选定目录后：空 → 初始化四板块；合规 → 加载；不合规 → 警告并询问附加式规范化。交付 = 服务端配置/端点 + 设置页数据目录卡。

决策基线（用户拍板）：

| 点 | 结论 |
|---|---|
| D1 | B · 重启生效（apply 只校验+规范化+持久化，不改运行时） |
| D2 | 附加式规范化（只建缺失板块目录），散落文件导入明确排除 |
| D3 | A' · git 尽力而为 + 「启用版本跟踪」入口（git init + 基线提交，单向） |
| D4 | 四态判定 empty/ready/needs-normalize/invalid；.obsidian 等外部条目不算异常 |
| D5 | 数据目录写操作仅限本机（127.0.0.1/::1），LAN 403 |
| D6 | 切走不动原目录；提供恢复默认快捷入口 |

非 md 语义（既有核实）：扫描/监听/API 三层只认小写 `.md`；板块内非 md 文件不构成异常（图片资源共存场景），明细计数提示；板块内 `.md` 一律视为词条。

提交链（7 个）：

1. `dacc31f` docs: T2 独立审查补记回写（前置工作区整理，与本功能无关的既有未提交文档）
2. `0d6d52a` fix: scanner.remove 补 .md 守卫
3. `9d0b332` refactor: NOTES_DIR 参数化为 appConfig 配置读取
4. `b60217b` refactor: git 自动提交跟随数据目录（rev-parse 现探仓库根 + 绝对路径入参）
5. `d90155f` feat(server): 数据目录管理端点（check/apply/git enable + 本机守卫）
6. `bcb0273` feat(web): 设置页数据目录卡
7. `fccc14a` fix: apply 放行 canCreate 目录（偏离 §3，UI 实测揪出）

## 偏离与理由

1. **§1.2 提交里 gitCommit/routes 的 PROJECT_ROOT 处理**：计划写「删常量」，实际先以 `path.resolve(getNotesDir(), '..')` 保持模块级推导（行为等价），随后 §1.3 才整体替换为 rev-parse 仓库根——两个提交各自可构建、可独立审查。
2. **盘根输入缺陷（实施中发现即修）**：`D:/` 清洗尾斜杠后变 `D:` 被 400「需要绝对路径」。`cleanInputPath` 补盘根保底（`/^[A-Za-z]:$/` 血回 `\`）。另明确：程序所在盘的盘根（D:\）命中「包含程序自身」400 守卫（祖先关系），建议性 isDriveRoot 警告仅对其他盘可达——语义正确的层级。
3. **apply 对「不存在的目录」的 400 误拒（UI 实测揪出）**：初版 apply 先判 `verdict==='invalid'` 即 400，canCreate（目录不存在）分支永不可达。curl 矩阵未暴露（用的 t-mixed 已存在），UI 全链路实测「确认切换」时以 `路径不存在` 报错暴露。修复：invalid 且非 canCreate 才 400，canCreate 走 createDir 创建 + 重探。已随 d90155f 之后的 UI 复测闭环（本文件验证矩阵 §3）。
4. **commitNotesBatch 签名**：posix 相对路径 → 绝对路径，仓库根换算收敛进 gitCommit（routes 的 relForGit 删除）；顺带过滤仓库外异常路径 + Set 去重。
5. **data/app-settings.json gitignore**：机器特定绝对路径，缺失回落默认目录（fresh clone 语义正确），与 data/fonts/ 同先例、data/tags.json 继续跟踪。
6. **未建数据目录 lib 响应式模块**：重启生效语义下无跨页签实时同步需求，Settings 本地 ref 即可（计划「不做清单」已列）。
7. **GET /api/data-dir 公开**：server-info 本就向 LAN 下发 notesDir，口径一致；三个 POST 才限本机。

## 验证矩阵（全部实测通过）

### 服务端（curl / API 实测）

- **check 矩阵**：空目录→empty+缺四板块；四板块齐（2md+1png、1md）→ready+计数精确；缺板块+散落 md+.obsidian+node_modules+大写 .MD →needs-normalize+明细全对；不存在→invalid+canCreate；文件路径→invalid；相对路径→400；带引号+尾斜杠+正斜杠粘贴→清洗通过；`NOTES` 大写变体→isCurrent（Win32 大小写不敏感）；程序根本身/祖先（D:\22691）→400 包含程序自身；D:\ 同上（程序所在盘盘根=祖先）；C:\ →needs-normalize+isDriveRoot 建议；E:\ →invalid 路径不存在。
- **apply**：needs-normalize+createMissing→只补缺失板块（createdBoards 精确）；current 目录→400；落盘 app-settings.json 内容正确且 gitignore 生效（git status 无痕）；**UI 全链路**：不存在目录→确认后创建目录+四板块初始化+徽标「已保存新目录…重启后生效」。
- **重启生效（D1）**：完整重启 dev→boot 日志「已扫描数据目录」指向新目录；boards 计数来自新目录（UPPER.MD 未入索引=大小写语义）；GET pendingRestart 复位 false。
- **git enable（D3-A'）**：临时目录 init+基线提交（含 node_modules——数据目录内容如实快照）；重复→409 带仓库根；主仓库零影响。
- **git 跟随数据目录（§1.3）**：切到临时目录后 DELETE 词条→自动提交 `note: 删除 vocab/vone` 落入**临时仓库**（目录即仓库根时 pathspec='.' 边界 ✓）；默认目录下探针标签重命名→提交精确仅含被改写文件、**未卷入工作区 5 个未提交数据文件**（精确 add 防线保持）。
- **本机守卫（D5）**：LAN 开启后经 LAN IP POST check/enable→403；GET→200；localhost POST→200；LAN 复位关闭。
- **配置缺失回退**：删 app-settings.json 重启→默认目录生效、configuredMissing=false、无异常日志。

### §1.0 remove() 守卫（撞名实测）

默认目录造 `vocab/img.png`（非 md）+ `vocab/img.png.md`（词条）→ 词条出现；fs 删 `img.png` → chokidar unlink 触发 remove() → **img.png 词条仍在索引**（修复前会被误踢）；清理后净零（git 无新增变更）。

### 前端（IAB browser-use，DOM 断言）

- 卡片渲染：路径 + 「版本跟踪：git 已启用（仓库根 …）」；tracked 态下「启用版本跟踪」按钮正确隐藏。
- 更改目录面板：needs-normalize 渲染完整（缺失板块中文名、逐板块计数、非 md 提示、散落 md、.obsidian 等外部条目「不影响加载」、目标未启用 git 提示、影响预览、确认可用）；当前目录（大小写变体）→「无需切换」+ 确认按钮整体不渲染（短路）；不存在目录→创建文案+确认可用→**实际点击创建成功**+成功提示+待生效徽标。
- HMR 竞态检查：`.dd-btn` computed 样式为令牌值（scoped CSS 未丢）。
- 回归：/ /vocab /v/vocab/abandon /tags /settings 五路由断言通过、无 error 元素；深色 data-theme=dark + html 背景深色；主题偏好已还原 light。
- 构建：`npm run build` 通过；产物 CSS 含 `color-warning`×7、`dd-panel`×4；preview 4173（web 工作区）首页/SPA 路由/API 代理全 200，特性文案在 bundle 中。

## 已知限制

1. **G1截图/ 本轮为空**：IAB 截图 surface 整场超时（宿主窗口后台遮挡的极重形态，与 T2 记录同款；evaluate/快照全程可用故功能验证未受影响）。窗口回前台后可补拍：设置页数据目录卡（默认态/面板检查态/待生效徽标态）+ 深色变体。
2. 启用版本跟踪的目标恒为**当前生效目录**；pendingRestart 时入口隐藏且服务端 409 兜底——待生效目录需重启后再启用。
3. 基线提交会包含数据目录内全部内容（含 node_modules 等——如实快照）；check 明细已提前展示 fileCount 与 nodeModules 警告。
4. `git enable` 的空目录分支只 init 不提交（避免 nothing-to-commit 报错路径），词条写入后由删除/手术自动提交逐步建立历史。
5. 其他设备在重启后的可见性：数据随请求即时更新，但前端搜索索引缓存需页面刷新重建（apply 成功文案已提示「各设备刷新页面」）。

## 会话沉淀（已回写 AGENTS）

- 服务端改码后完整重启 dev 的既有铁律再次验证（tsx watch 热重载期间 check 端点曾以旧代码应答）。
- apply 的 canCreate 分支 bug 由「UI 全链路实测」而非 curl 矩阵揪出——服务端边界改动（状态机分支顺序类）的验证必须含 UI 端到端一次真实点击。
- Bash 工具 cwd 跨调用持久：`git status notes/` 从 server/ 子目录跑会静默空结果——git 命令一律显式 `cd` 仓库根（本次曾虚惊「数据文件被回滚」）。
