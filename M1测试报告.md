# M1 全维度真实执行测试报告

> 日期：2026-08-29　测试基线：HEAD `4fc6620`（M7 复检修正）
> 测试方式：全维度真实执行（启动服务、curl API、浏览器 DOM 实测、文件监听实测、性能计时）

## 1. 执行概要

**环境**：Windows 10.0.26200 x64 / Git Bash / Node.js v24.19.0 / npm 11.17.0 / Vite 8.2.2 / tsx watch / Fastify 5 / Playwright Chromium（1280×900、375×667 双视口）。本报告修复验证环节另用系统 Edge（playwright-core，独立 profile）复核 DOM 指标，结论一致。

**结论统计**：

| 等级 | 结果 |
|---|---|
| P0 | **26 / 26 通过**（服务启动、四板块浏览、阅读页渲染、无 frontmatter 兜底、文件监听收录、API 契约与 404/400、删除与 git 提交、播报与编辑链路等核心路径） |
| P1 | **27 / 29 通过；2 缺陷**（DEF-1 S-09 非法 YAML 崩溃、DEF-2 U-13 超长内容撑破布局，均在本报告中登记并已修复，见第 7 章） |
| P2 | 全记录，见第 3 章 |

**真实数据口径**：`notes/` 真实词条经文件系统核实为 9 个（vocab 6：abandon/boost/perseverance/runaway/serendipity/twinkle；phrase/sentence/grammar 各 1）。

## 2. 缺陷清单

### DEF-1（S-09，P1）非法 YAML frontmatter 致服务端崩溃

**复现步骤**：任一板块目录放置一个 frontmatter YAML 语法错误的 md（如 `title: [未闭合` 与 `tags: {invalid` 并存），然后（a）重启服务端；（b）服务运行中丢入该文件。

**期望**：坏 frontmatter 不应影响服务；词条按无 frontmatter 规则兜底（title=文件名、tags=[]、日期=文件时间），正文完整可读可改。

**实际**：
- 启动路径：`matter(raw)` 抛 `YAMLException`，服务端启动失败且自锁（无法 listen）。
- watcher 路径：运行中收录该文件时异常未捕获，进程死亡（HTTP 000，服务不可用）。

**根因**：`server/src/scanner.ts` 的 `parseNote` 内 `matter(raw)` 无 try/catch；启动扫描（`scanDir`）与 chokidar 回调（`upsert`）直接传播异常；`getNote`/`getNoteRaw` 同病，后者令编辑接口 500。

### DEF-2（U-13，P1）阅读页超长内容撑破布局

**复现步骤**：桌面 1280×900 打开含不可断词长内容的词条（长表格头/单元格、超长英文词）；移动 375×667 打开超长标题词条。

**期望**：无文档级横向滚动；表格在容器内横向滚动；长标题折行；朗读按钮不被推出视口。

**实际**：
- 桌面：表格宽 838px > 容器 623px，祖先链无 overflow 约束，出现文档级横向滚动。
- 移动：长标题（381px 不可断词）与表格头（798px）双重溢出（docScrollW 798 vs 360），喇叭按钮被推出。

**根因**：`.note-body :deep(table)` 为 `overflow-x: visible` 且无全局约束；`.note-title`/`.note-body` 无 `overflow-wrap` 约束；移动端 `display:block; overflow-x:auto` 规则仅在 `max-width: 767px` 生效（M3 行为），桌面无对应收敛。代码块不受影响（`pre` 已 `overflow-x: auto`）。

## 3. P2 观察项（不阻塞验收，记录备查）

**性能（数据已核实）**：
- 启动扫描 500 词条 → 首个请求 200 = 1357ms；2000 词条 = 2754ms（含约 1.3s npm/tsx 引导）。
- 2018 词条下：`/api/boards` 2ms、列表 13ms（20 次最大 24ms）、详情 2–8ms、`/api/recent` 6ms、search-index 全量 342ms。
- 前端 2018 词条全渲染可滚动（无虚拟化）；服务端 RSS 86→105MB，无爆炸式增长。

**其他**：
- vocab 列表排序用 `title.localeCompare()` ICU 序：中文排在英文前（与纯字母序直觉不同，属实现选择、非缺陷）。
- C-01 手工覆盖为 Playwright Chromium；Edge 未独立验证（本次修复验证补做了 Edge 复核）。
- 空板块计数显示「0 / 0 条」；阅读页「网络错误」与「词条不存在」共用文案——可改进的外观项。
- 修复后新增观察（DEF-2 相关）：极端超长标题在移动端头部显著拉高（`overflow-wrap: anywhere` 下按字符断行），属「折行优先于撑破」的代价，未影响验收项判定。

## 4. 非缺陷现象清单（已确认实现事实，勿作缺陷上报）

1. **删除自动提交扫入未跟踪文件**：`git add -A notes` 会把 notes/ 下所有变更（含用户手工放置的未跟踪笔记）一并提交——M7 决策 8 的既定语义（版本管理「后台静默提交」）。
2. **wiki 链接无 href**：跳转由事件委托（click / Enter）处理，无 href 是有意设计（M5/M7 已确认）。
3. **详情接口 404 三态**：`GET /api/notes/:board/:slug` 对非法 board 返回 404（列表接口 `GET /api/notes?board=` 才是 400）——三态 404/400 分工明确。
4. **recent 参数边界**：limit=-5 → clamp 到 1；0/abc/空 → 回退默认 10。
5. **无 FM 词条 updated = 文件 mtime**：无 frontmatter 时 created 取 birthtime（无效回退 mtime）、updated 取 mtime。
6. **日期双轨无偏移**：frontmatter 值（YAML 裸日期，UTC 午夜 Date）按 UTC 组件输出、文件系统时间按本地组件输出——各归其位，UTC+8 凌晨不偏一天（M1 复检修正）。
7. **容忍性解析**：tags 非数组 → `[]`；ipa/source 非 string 不输出；空文件正常收录（title=文件名）；同 slug 子目录覆盖根目录（后扫者胜，不崩溃）。
8. **XSS/URL 防御**：`html: false` 下 `<script>`/`<img onerror>` 全转义；`javascript:` 链接被 markdown-it 拦截。

## 5. fixture 清理与 git 还原记录

- 测试会话过程中曾因 `git add -A` 把临时词条扫入产生提交 `2cd7acb`；已 `git reset --hard 4fc6620` 还原，HEAD 回到 M7 复检修正基线，临时词条文件已移除。
- 本报告修复验证环节的 fixture 一律 `t_m1_` 前缀（`t_m1_badfm`、`t_m1_badfm2`、`t_m1_long`、`t_m1_nofm`），验收后全部删除；临时验证脚本（`.m1-ui-verify.mjs`、`.m1-edit-save.mjs`）与结果文件已删除；`playwright-core` 以 `--no-save --no-package-lock` 安装、用后移除，`package.json`/`package-lock.json` 零污染（git diff 为空）。
- 修复收尾时 `git status` 恢复为仅 `?? .zcode/`，与测试前基线一致。

## 6. 整体结论

M1 核心功能质量良好：P0 全部通过；P1 仅有的两处缺陷均为**边界输入/极端内容**触发（坏 YAML、超长不可断词），不伤及常规使用路径，且均已定位到明确根因并修复。性能表现符合本地个人应用预期（2018 词条全链路毫秒级、无内存爆炸）。所有「非缺陷现象」经核实为已确认的实现决策或有意的防御行为，无需改动。

## 7. 修复执行记录（2026-08-29，修复后补写）

### DEF-1 修复

**改动**（`server/src/scanner.ts`，仅此一文件）：
- 新增内部工具 `parseFrontmatter(raw, filePath)`：`try matter(raw)`；catch 时 `console.warn`（含相对路径与 YAML 错误摘要，风格同 `gitCommit.ts`）并返回 `{ data: {}, content: raw }`——坏 FM 按无 FM 兜底：title=文件名、tags=[]、日期=文件时间，正文=全文原文（坏 FM 行以文本可见，用户可读可改）。
- 三处调用点换用：`parseNote`、`getNote`、`getNoteRaw`。
- `parseNote` 的 `readFileSync`/`statSync` 包 try/catch，失败 `return null`（读盘竞态防御，沿用 `upsert` 既有模式）。
- `index.ts`（`start().catch + process.exit(1)` 最后防线）、`routes.ts`、API 契约、search-index/backlinks 均未改动。

**真实验证**（全部执行，非推断）：
- 冷启动：`t_m1_badfm.md`（`title: [未闭合` + `tags: {invalid`）在盘上 `npm run dev` → 服务正常监听（未自锁）；日志 2 条 `frontmatter YAML 解析失败（已按无 frontmatter 兜底）：<路径> —— <YAML 摘要>`；`GET /api/notes/vocab/t_m1_badfm` 200，`title=t_m1_badfm`、`tags=[]`、created/updated=文件时间、`body` 含坏 FM 原文、`raw` 字段完整。
- 热收录：删除再写回第二个坏 FM 文件 → 进程存活（后续请求持续 200）、第 3 条 warn 出现、约 200ms 内收录（chokidar awaitWriteFinish 150ms，轮询实测计数 9→10）。
- 索引纠正：修复该文件 frontmatter（`title: badfm2 已修复`）→ 不重启即 `title=badfm2 已修复`、`tags=[修复验证]`。
- 编辑链路：坏 FM 词条编辑页打开（textarea 含坏 FM 原文）；追加内容 + Ctrl+S → UI「已保存 ✓」→ 服务端 GET 确认正文落盘、坏 FM 原样保留、兜底稳定（title 仍为文件名）。
- 关键子集：S-01~S-10、A-01/A-02、W-01~W-03 全部重跑，**15/15 通过**（boards/四板块列表/详情/无 FM/坏 FM/不存在 404/非法板块 404/recent/缺参数 400/路径穿越 404/PUT 非法 body 400/search-index 200）。

### DEF-2 修复

**改动**：
- `web/src/components/MarkdownViewer.vue`：`.note-body :deep(table)` 的 `display: block; overflow-x: auto` 由移动端 `@media` 提升为**全端规则**（`width: 100%` 保留；删除 `@media` 内重复块——移动端块级滚动是 M3 已验收行为，桌面化属收敛，非新语义）；`.note-body` 增加 `overflow-wrap: anywhere`（不可断词内容防御，不影响 CJK 排版）。
- `web/src/views/NoteView.vue`：`.note-title` 增加 `overflow-wrap: anywhere`（父级 `.note-head-main` 已有 `min-width: 0`，未重复加）。
- 负向确认：`pre` 内滚、A–Z 条、悬停预览、wiki 渲染、markdown-it 配置（`html: false` 保持）均未改动。

**真实验证**（playwright-core + Edge 独立实例，DOM 指标实测）：

| 检查项 | 桌面 1280×900 | 移动 375×667 |
|---|---|---|
| 文档级横向滚动 | scrollWidth 1280 = clientWidth 1280 ✅ | scrollWidth 375 = clientWidth 375 ✅ |
| 表格 | overflowX 计算值 auto；长内容由折行吸收（scrollW 622=clientW 622，未撑破）✅ | 同左（269=269）✅ |
| 长标题（超长中英混排） | 折行（h≈180px），宽度受限于容器 ✅ | 折行（h≈660px），未溢出 ✅ |
| 朗读按钮 | right 915 < 1280 ✅ | right 174 < 375 ✅ |
| pre 代码块 | 内部滚动保留（scrollW 824 > clientW 620）✅ | 同左（784 > 267）✅ |
| 短表格/真实词条回归（t_m1_nofm、abandon） | 无溢出，与修复前一致 ✅ | 无溢出，与修复前一致 ✅ |

fixture 验收后全部删除；真实词条未改动。
