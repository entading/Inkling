# M2 测试报告 —— 检索功能面全维度测试

- **测试日期**：2026-08-29
- **测试对象**：M2 功能面（/api/search-index、全局搜索 SearchPanel、板块页就地过滤、A–Z 目录、标签页 /tags 与 /tags/:tag、NoteList/TagBadge 组件契约、M4/M7 缓存失效联动）
- **测试方式**：真实执行（curl/Node fetch 测 API；Playwright Chromium 测 UI，桌面 1280×900 + 移动 375×667；git 核对；2000 词条量级压测）
- **代码基线**：`f91af78`（fix: M1 测试缺陷修复），git 工作区起始干净（仅 `.zcode/` 未跟踪）

---

## 1. 执行概要

### 环境信息

| 项 | 值 |
|---|---|
| 服务端 | Fastify 5 @ 127.0.0.1:3000（tsx watch） |
| 前端 | Vite 8 @ http://localhost:5173（/api 代理到 3000） |
| 浏览器 | Playwright Chromium（桌面 1280×900 / 移动 375×667） |
| 起始数据 | 9 条真实词条（vocab 6 / phrase 1 / sentence 1 / grammar 1） |
| fixture | `t_m2_` 前缀临时词条，峰值 2040 条（含 2000 条压测批次），测后全清 |

**环境说明**：会话开始时发现上一会话残留的孤儿 dev 进程占用了 3000/5173 端口（EADDRINUSE），已全部终止后以当前代码重新启动；会话结束时已停止本会话全部服务进程并释放端口。

### 用例统计

| 优先级 | 总数 | 通过 | 失败 | 阻塞/豁免 | 备注 |
|---|---|---|---|---|---|
| **P0** | 29 | 28 | 0 | 1 附缺陷 | Z-05 核心断言达标，附带 DEF-02（P1，环境相关，详见缺陷清单） |
| **P1** | 24 | 24 | 0 | 0 | 全部通过 |
| **P1（数据约束豁免）** | 2 | — | — | 2 | T-03、N-03（无法在不触碰真实词条的前提下构造前置状态，见豁免说明） |
| **P2（观察项）** | 8 | 8 | — | — | 全部记录在案（第 3 节） |

**结论先行：M2 功能面测试通过。** 全部 P0 契约核心达标（1 条附带环境相关的 P1 视觉缺陷已登记）；P1 全部通过（2 条因数据约束豁免，见 2.2）；P2 观察项 8 条记录在案。缺陷 1 条（DEF-02，P1）+ 潜在边界观察 1 条（OBS-1，P2），均不阻塞。

---

## 2. 失败与缺陷清单

### 2.1 缺陷

#### DEF-02（P1）：移动端 A–Z 点字母跳转后，组标题顶部约 10px 被横向字母条遮挡

- **用例**：Z-05（P0 用例的附带缺陷；Z-05 的核心断言本身全部达标）
- **复现步骤**：
  1. 视口 375×667，打开 `/vocab`（词条数足够产生竖向滚动）；
  2. 点击横向字母条中的字母（如 `P`）触发 `scrollIntoView`；
  3. 观察 P 组标题与字母条的相对位置。
- **期望**：组标题位于字母条下方完整可见（`scroll-margin-top: 44px` 的设计意图是「不被横条遮挡」）。
- **实际**：组标题 top=44px 精确落位（scroll-margin 生效），但横向字母条实测高度 **54px**（含经典滚动条占位），底边盖住标题顶部 **10px**（截图证据已留存后删除，测量数据：titleTop 44 / railBottom 54 / overlap 10）。
- **优先级**：P1（视觉边界，非功能阻塞；点字母定位、置灰、横滚、sticky 均正常）
- **环境相关性**：桌面 Chromium 经典滚动条占据字母条内部高度（54px）；真机移动端为 overlay 滚动条（不占高），字母条约 40px < 44px，预计不触发。本环境（也是 M3 以来一贯的验收环境）下实测存在。
- **初步根因**（只判断不修复）：`AZIndex.vue` 移动端 `.az-group { scroll-margin-top: 44px }` 的 44px 按字母条内容高度估算，未把 `overflow-x: auto` 在经典滚动条环境下占用的滚动条高度计入；`.az-rail` 实际渲染高度（8px padding ×2 + 字母行高 + 滚动条 ~14px + 1px border）超出 44px。
- **建议**（供后续参考，本次未改动代码）：scroll-margin 提高到与 rail 实际最大高度匹配（如 56px），或对 `.az-rail` 使用 `scrollbar-width: none` / overlay 式滚动条样式。

### 2.2 豁免登记（数据约束，非产品缺陷）

| 用例 | 内容 | 豁免原因 |
|---|---|---|
| T-03（P1） | /tags 空库空态文案 | 需要全库零标签；真实词条均带标签且**禁止改动真实词条**，无法在不污染用户数据的前提下构造前置状态。空态渲染路径与同页其他 `v-else-if` 空态分支同构（T-06 同款元素已实测） |
| N-03（P1） | NoteList 空数组显示「暂无词条」 | NoteList 空态仅在「板块无任何词条且无过滤词」时可达；当前四板块全部非空（板块页空结果走 board 级 hint，已由 B-08 验证），不能为构造空板块删除真实词条 |

两条均为「无法真实执行」而非「执行失败」，代码路径极短（`v-if`/`v-else` 分支），风险低。

---

## 3. P2 观察项清单

| 编号 | 用例 | 观察结果 |
|---|---|---|
| PF-01 | search-index 拉取耗时 | **2020 词条**（约 366KB payload）：Node fetch 5 次 330–408ms，浏览器内 292ms。量级：本地数百毫秒，单用户场景可接受 |
| PF-02 | 防抖后出结果耗时 | 输入到下拉渲染 **220ms**（含 200ms 防抖 → Fuse 检索+渲染仅约 20ms，2020 条数据集） |
| PF-03 | aggregateTags 耗时 | 2020 条全量聚合 69 个标签 **1ms**（浏览器内） |
| PF-04 | 大数据量键盘导航 | 2020 条命中集（组头显示「2001 条」）上 20 次 ArrowDown 共 204ms（≈10ms/步，含 scrollIntoView + nextTick），active 高亮渲染正确、无卡顿 |
| G-13 | 「加载中…」死分支 | 确认为死分支：`v-if="groups.length === 0 && !loading"` 将 loading 态排除在列表渲染外；用路由延迟 700ms 构造慢索引期间「加载中…」从未出现，无副作用 |
| B-10 | 板块页输入无防抖 | 确认即时过滤：input 事件后 **1ms** 内 DOM 更新（computed 直算），与全局搜索的 200ms 防抖形成有意对比 |
| T-07 | TagDetail 组内顺序 | 与 search-index 顺序一致（如 t_m2_shared 词汇组 `[t_m2_multi, 中文标题词条]`），按约定未断言字母序 |
| T-10 | /tags 页 document.title | `EN_tool`（router afterEach else 分支），与任务书「非缺陷」注记一致 |

---

## 4. 非缺陷现象清单（与 AGENTS.md 已确认决策一致，防未来误报）

1. **首页加载即出现一次 /api/search-index 请求**：SearchPanel 仅用于 Home 且 `autofocus`——自动聚焦触发 `@focus="ensureIndex"`，即「首次聚焦惰性拉取」的正常路径（G-03）；索引全程只拉一次，后续所有键入/搜索零网络请求（客户端 Fuse）。
2. **zh-CN 环境下标签云同数排序中 CJK 排在 Latin 之前**（如「高频」先于 `cet6`）：浏览器 `localeCompare`（navigator.language=zh-CN）实测 `'高频'.localeCompare('cet6') === -1`，DOM 顺序与 `(count desc, localeCompare asc)` 逐项一致（T-01 用浏览器自身函数严格校验 match=true）——是 localeCompare 升序的正确表现，不是排序 bug。
3. **板块页过滤无防抖、全局搜索有 200ms 防抖**：有意差异（M2 偏离 1 的子串快速收窄语义 + 设计 5.1 防抖），非不一致缺陷。
4. **删除从未被 git 跟踪的词条不产生删除提交**：`git add -A notes` 后无净变更 → nothing to commit → console.warn 分支（M7 复检 7 的既定语义）；本次 t_m2_del 删除实测验证，未污染 git 历史。
5. **wiki 链接元素无 href 属性**：M5 设计为事件委托跳转（M5 复检 4），点击跳转正常（R-02 实测）；`is-missing` 红虚线态同样无 href。
6. **程序化 fill 与「标题 blur 推导 slug」的一次性竞态**：自动化测试中 `fill` 标题后立即 `fill` slug，偶发 slug 变成 `t-m2-i01t_m2_i01`（推导值与手输值拼接）。真实用户逐键输入路径每次 blur 推导都在受控时机发生，且 slug 可见可改——判定为自动化注入伪影（Playwright fill 与 Vue 异步渲染竞争），非产品缺陷；不重复复现（后续 5+ 次同型操作正常）。产物词条已清理。
7. **NoteList 行 `.row-side` 在「一行 3 个长标签」时横向溢出**（OBS-1，潜在边界）：真实词条（短标签）在 375px 下实测不溢出（row-side 最宽 258px，docScrollWidth 360 < 375）；仅当 fixture 注入 3 个 11 字符标签时 row-side 达 361px 触发文档级横向滚动（flex-shrink:0 + max-content 计宽使移动端 flex-wrap 不生效）。当前真实数据不触发，登记为边界观察；若未来出现长标签词条，可考虑给 `.row-side` 加 `min-width: 0` 或允许 badge 换行。
8. **全局搜索 catch 分支的「搜索失败：…」**：仅在 `search()` 本身抛错时出现；SE-03 宕机场景走的是 `ensureIndex` 的「搜索索引加载失败：Bad Gateway」分支（符合实现分工），恢复后重试成功，catch 分支未稳定触发（防御性代码）。
9. **首页搜索面板宽度 296px（375px 视口下两侧有留白）**：面板宽 == 容器 content 宽（296==296），与输入框同宽、未超视口，「全宽」语义指填满容器（`.search-panel { max-width: none }` 已生效）；外层留白是 Home 容器既定排版（M1/M3 已验收范畴）。

---

## 5. fixture 清理与 git 状态确认

- **创建过的 fixture**（全部 `t_m2_` 前缀，峰值 2040 文件）：
  - 主批次 9 个：`t_m2_multi`、`t_m2_bodyword`、`t_m2_中文标题`、`t_m2_123数字`、`t_m2_g5/g6/g7`（vocab）、`t_m2_pct`、`t_m2_pct2`（phrase，特殊字符标签）
  - 专项：`t_m2_del`（删除联动隔离窗口）、`t_m2_si03`（watcher 反映）、40 个 `t_m2_race*`（读盘竞态）、2000 个 `t_m2_perf0001–2000`（压测）、UI 创建的 `t-m2-i01t_m2_i01`（I-01/I-02）与 `t_m2_stub目标`（I-04 stub）
- **清理方式**：文件级 `rm`（不触发 DELETE API 的 git 自动提交）；UI 删除专用词条 `t_m2_del` 在隔离窗口执行（当时 notes/ 下无其他未跟踪文件，`git add -A notes` 无净变更，未产生任何提交）。
- **清理核验**：search-index 恢复 **9 条**（与会话起始一致）、`t_m2` 残留 0；测试截图与 `.playwright-mcp/` 产物目录一并移除。
- **git 状态**：`git status --porcelain` 仅剩 `?? .zcode/`（会话前已存在的工具目录）；HEAD 仍为 `f91af78`，**无任何测试提交混入历史**；真实词条 9 个文件未动。

---

## 6. 整体结论

**M2 功能面测试：通过。**

- **契约核心（P0）**：29 项中 28 项完全通过——search-index 契约（含 filePath 不暴露、条数一致、watcher 即时反映、读盘竞态不报错）、全局搜索（防抖、惰性拉取、精确/前缀优先、四板块分组与 5 条上限、键盘导航含组头行、Escape/点外行为）、板块页子串过滤核心（`ser` 不误中 perseverance）、A–Z 分组归并规则、标签云排序/计数/特殊字符标签编码（%/#/?/空格/中文双入口）、NoteList 无嵌套链接与 3 标签上限、M4/M7 缓存失效六面联动（新建/编辑/删除/stub/索引复用/悬停预览）全部实测通过。
- **唯一缺陷 DEF-02（P1）**：移动端点字母跳转后组标题顶部 ~10px 被字母条遮挡（桌面 Chromium 经典滚动条环境实测；真机 overlay 滚动条预计不触发），视觉边界问题，不阻塞，建议后续调 scroll-margin 或隐藏 rail 滚动条。
- **P1**：24 项全部通过；T-03/N-03 因「不得改动真实词条」的数据约束豁免（登记见 2.2）。
- **P2**：8 项记录在案（2020 词条压测：索引拉取 ~300ms、搜索出结果 220ms、标签聚合 1ms、键盘导航流畅；死分支、无防抖、顺序契约、title 均确认）。
- **回归冒烟**：首页板块卡片/最近添加、wiki 跳转、编辑落盘（PUT 全量源码语义）、TTS 喇叭（speak 调用 + en-US 默认音）、删除两态流程、`npm run build`（vue-tsc + vite 构建通过）全部正常。
- **工作区复原**：fixture 全清、git 历史零污染、dev 服务已停止、端口已释放。
