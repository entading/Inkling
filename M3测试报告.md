# M3 测试报告 —— 移动端与局域网功能面全维度测试

- **测试日期**：2026-08-30
- **测试对象**：M3 功能面（响应式断点、底部导航与「⋯」菜单、横向 A–Z 字母条、移动端阅读体验、选中工具条/悬停预览的移动端叠加表现、局域网开关前端流程与服务端契约、z-index 层级、适配矩阵、性能、安全健壮性；第 12 节回归冒烟覆盖 M1–M7）
- **测试方式**：真实执行（curl/Node fetch 测 API；Playwright Chromium 有头测交互流；无头 Chrome + CDP 精确视口测整数断点与 DOM 断言；git 核对；2011 词条压测与慢速请求在途实测）
- **代码基线**：`42dc025`（fix: M2 测试缺陷修复），git 工作区起始干净（仅 `.zcode/` 未跟踪）

---

## 1. 执行概要

### 环境信息

| 项 | 值 |
|---|---|
| 服务端 | Fastify 5 @ 127.0.0.1:3000（tsx watch，`forceCloseConnections: 'idle'`） |
| 前端 | Vite 8 @ http://localhost:5173（/api 代理到 3000） |
| 局域网 | WLAN，真实 LAN IP **192.168.10.143**（本机唯一非内部 IPv4） |
| 浏览器 | Playwright Chromium 有头（375×667 / 1280×900，交互流与 TTS/选中/悬停）；无头 Chrome + CDP（320/375/767/768/1280 精确整数视口，DOM 断言与截图） |
| 起始数据 | 9 条真实词条（vocab 6 / phrase 1 / sentence 1 / grammar 1） |
| fixture | `t_m3_` 前缀 5 个 + `pf2k-` 压测 2000 个，峰值 2005 条，测后全清 |

**环境说明**：会话开始时发现上一会话孤儿 dev 进程两条链占 3000/5173，已整树终止重启；Playwright MCP 残留托管 Chrome 持锁，清除后解锁。**仪器说明**：本机显示缩放下有头浏览器视口为分数宽（setViewportSize(767) 实际媒体宽 767.3177px，`matchMedia('(max-width: 767px)')` 不命中），整数断点 767/768 的判定一律以无头 CDP 精确视口为准（无头视口为精确整数）；375/320 等非边界宽度分数偏差 ±0.3px 不跨断点，有头/无头交叉使用。

### 用例统计

| 优先级 | 总数 | 通过 | 失败 | 备注 |
|---|---|---|---|---|
| **P0** | 22 | 20 | 2 | R-02/R-03 因 DEF-01 判失败（缺陷已登记并修复，见第 2、7 章） |
| **P1** | 14 | 14 | 0 | 全部通过 |
| **P2（观察项）** | 5 | 5 | — | 全部记录在案（第 3 节） |
| **第 12 节回归冒烟** | 6 | 6 | 0 | 搜索/标签、编辑保存、双向链接、TTS 喇叭、删除流程、`npm run build` 全过 |

**结论先行：M3 功能面测试通过（含修复后复测）。** P0 中 20 项直接通过；R-02/R-03 因 DEF-01（P1）判失败，已在 `0ff65b9` 修复并复测通过（12 个宽度×页面矩阵零横滚）；P1 全部通过；P2 记录在案。M3 九条偏离事实逐条对照，全部确认为既定实现事实，无误报。

---

## 2. 失败与缺陷清单

### DEF-01（P1）：列表行标题/IPA 缺 overflow-wrap，超长不可断词标题致文档级横向滚动

- **用例**：R-02（768↔767 切换无横向滚动条）、R-03（320px 各页无文档级横滚）——两项 P0 因此判失败
- **复现步骤**：
  1. 放入标题为 100+ 字符无空格的词条（fixture `t_m3_longtitle`）；
  2. 打开首页（最近列表）、/vocab（AZIndex→NoteList）、/tags/\<该词条标签\>（TagDetail）。
- **期望**：列表内折行，无文档级横向滚动。
- **实际**：三处页面**文档级横向滚动**，docScrollW=868px（视口 305–753px），且 320/375/767/768 四档宽度全部出现（768 桌面档 docScrollW 1116 > 753）；溢出元素 `.row-title`（右缘 818px）+ `.row-ipa`（868px），无任何 overflow 裁剪祖先，computed `overflow-wrap: normal`。
- **优先级**：P1（触发需标题含宽度超过列宽的不可断 token——超长英文术语、URL 等，对本库属现实可能；常规含空格标题不触发）
- **初步根因**：`NoteList.vue` 的 `.row-title`/`.row-ipa` 无 `overflow-wrap`，不可断 token 的 min-content 沿 flex 链（`.row-main` 的 span 子项 `min-width: auto`）顶开 `.note-row`；M1 的同款修复当时只覆盖阅读页 `.note-title`/`.note-body`，遗漏列表行类（首页 Home→NoteList、板块 Board→NoteList、词汇 AZIndex→NoteList、标签详情共用该组件，一处缺漏四处同病）。非 M3 新增回归，属既有健壮性缺口被 M3 的列表向 fixture 暴露。
- **修复**：见第 7 章（`0ff65b9`，含修复首版引入「abandon 断词」新回归的发现与再修正过程）。

---

## 3. P2 观察项清单

| 编号 | 主题 | 观察结果 |
|---|---|---|
| OBS-01 | 阅读页超长标题窄列 | `.note-title` 的 `overflow-wrap: anywhere` 使标题 flex 项 min-content 坍缩至单字符宽，与不收缩的操作按钮分宽后标题列仅剩剩余空间（375px 实测 **62px 宽、高 1120px 逐字换行**）。无溢出、无横滚，规格「折行不撑破」满足；真实长词标题（>可用宽）也会呈现。属 M1 已验收修复在窄视口的观感代价，**评估后明确不做**（修复需移动端头部换行改版，视觉变化，已记入 AGENTS.md） |
| OBS-02 | sel-bar 极端瞬态 | 选区完全滚出视口上缘时工具条顶边 -7px 未钳到 0（`placeBar` 翻转分支 `y = rect.bottom + m` 可为负）；选区在视口内时始终完整可见。**已随 0ff65b9 修复** |
| OBS-03 | settings 空 body 文案 | 无 body 时返回 400 但为 Fastify 标准 `FST_ERR_CTP_EMPTY_JSON_BODY` 文案（自定义「body 必须为 { lanEnabled: boolean }」只覆盖「解析成功但类型不符」路径）。状态码契约满足，**明确不改** |
| OBS-04 | LAN 切换窗口校准值 | L-01 开启：POST 应答 ~150ms 出地址+二维码、核对完成 ~1.9s；L-02 关闭对称 ~0.9s；L-03 回滚显示 ~0.8s；L-05 拦截 2 次 server-info 失败后 1.7s 照常收敛（≈800ms 核对窗 + 2×400ms 重试，吸收符合设计） |
| OBS-05 | 性能实测 | PF-01：192KB 慢速 PUT（20KB/s ≈10s 在途）横穿 LAN 切换窗口**完整送达 200**——`forceCloseConnections:'idle'` 不断在途连接实证；切换窗口内新请求短暂失败（前端重试吸收）。PF-02：2011 词条（7 组、页高 115,602px）2s 程序化滚动 330 帧、平均 6.1ms/帧、p95 7.1ms、**0 帧 >50ms**；`/api/notes` 8ms / 239KB |

---

## 4. 非缺陷现象清单（与 AGENTS.md 已确认决策一致，防未来误报）

1. **lanIps 关闭态恒为 `[]`**：`settings.ts` 的 `lanEnabled ? getLanIps() : []`——地址列表/二维码仅在开启后展示（M3 偏离 8），关闭时 `qrDataUrl` 仍始终存在（基线响应取证）。
2. **阅读页 `/v/...` 底导航无高亮**：「首页仅 `/` 精确匹配、板块前缀匹配」按字面定义不含 `/v` 路径，桌面侧边栏同样无高亮，行为一致。
3. **Z-02 末组落位 >56px**：点 T/# 等末尾字母组时组标题落位 164/456px——末组之后内容不足一屏，浏览器 max-scroll 钳制所致；railBottom 54 与 titleTop 间距恒 ≥0（零遮挡），S 组（内容充足）精确落位 56px。
4. **M7 `git add -A notes` 删除自动提交会卷入全部未跟踪 fixture**：本次 UI 删除 `pf2k-2000` 时产生提交 `a195725`，经核实**仅含测试产物**（0 个非 fixture 文件），已 `git reset --mixed 42dc025` 摘除复原。既定设计语义的推论（M7 复检 7 同源）——后续批量清理临时词条务必走文件系统删除而非 DELETE API。
5. **删除从未被 git 跟踪的词条不产生删除提交**：add -A 无净变更 → nothing to commit → console.warn 分支（M7 复检 7），与 M2 报告结论一致。
6. **tsx watch 不会重启被杀的服务端子进程**（仅文件变更触发）——测试运维注记，非产品行为。
7. **自动化伪象两则**：(a) 虚拟鼠标无位移时重复 hover 不派发 mouseover（Chrome 不重发无移动事件）；(b) 折行 wiki 链接包围盒中心可能落在两行间非绘制区（elementFromPoint 返回父级），点该死区不触发预览/跳转，为正常行盒行为。真实用户交互不受影响。
8. **指针停留在链接上滚动时预览卡隐藏后立即重建**：Chrome 滚动后重算 hover 目标派发合成 mouseover——正是 M7「隐藏后下次 mouseover 重建」的设计路径；指针移开后的纯滚动则保持隐藏（实测区分）。
9. **SE-02 深层嵌套 body 带合法 `lanEnabled` 时被接受**：额外字段忽略，契约按 `lanEnabled: boolean` 校验（2000 层嵌套不崩溃）；无 `lanEnabled` 的深层嵌套正确 400。5MB payload 413、二进制垃圾 400，全程服务存活。
10. **M3 九条偏离逐条对照确认**（新建实例 listen、构造选项 forceCloseConnections、先应答后台切换、字面量断点、box-sizing reset、AZIndex stretch、虚拟网卡排除、二维码仅开启后显示、SearchPanel 重复声明清理）——全部为既定实现事实，测试中未发现与设计文档/已确认决策冲突的现象。

---

## 5. fixture 清理与 git 状态确认

- **创建过的 fixture**（峰值 2005 文件）：`t_m3_longtitle`（超长无空格标题）、`t_m3_wide表格`（8 列宽表）、`t_m3_长单词`（超长英文词+长代码行）、`t_m3_中文标题`（# 组）、`t_m3_big`（192KB 慢速 PUT 载体）、`pf2k-0001`–`pf2k-2000`（压测）。
- **清理方式与事故记录**：`pf2k-2000` 经 UI 删除流程删除（兼作 R-05 用例），触发 M7 `git add -A notes` 自动提交 `a195725` 把其余 2004 个未跟踪 fixture 一并卷入历史——核实提交仅含测试产物后 `git reset --mixed 42dc025` 摘除；其余 fixture 全部文件级 `rm`（不触发 DELETE API）。修复验证阶段重建的 `t_m3_longtitle` 验收后同样文件系统删除。
- **清理核验**：vocab 索引恢复 6 条真实词条；无头 Chrome（调试端口 9333）、端口占用进程（27816/9436）、临时探针脚本与截图全部移除；会话 dev 进程树停止，终态 3000/5173/9333 无监听。
- **git 状态**：`git status` 仅 `?? .zcode/`；测试后 HEAD 为修复提交 `0ff65b9`（基于基线 `42dc025`），无任何测试产物混入历史；真实词条 9 个文件未动。

---

## 6. 整体结论

**M3 功能面测试：通过（缺陷已修复并复测）。**

- **断点与导航（P0）**：字面量 767/768 分界精确成立（768=桌面侧边栏+无底导航+无「⋯」；767 全翻转，三路样式翻转 3 轮无残留）；底导航仅首页+四板块、首页精确/板块前缀匹配、「⋯」菜单开合/跳转/点外/toggle 全过；菜单仅首页存在（已确认设计）。
- **A–Z 横条（P0）**：sticky top 0 + z=20 + 内部横滚（内容 708px 在 296px 容器内滚动，与设计预期一致）、20 个无词条字母置灰不滚动、快速连点 A→T→S 最后点击获胜（S 组 56px 精确落位、重叠量 -1 即零遮挡）、桌面竖排不变。
- **移动端阅读（P0）**：卡片 20px、正文 15.2px=0.95rem、超长标题/长单词 anywhere 折行不撑破、宽表格与 pre 容器内滚无文档横滚；选中工具条（z=50）clamp/翻下方/滚动跟随/清空隐藏全过；悬停预览 clamp 视口、滚动隐藏、resize 隐藏（M6/M7 叠加面移动端表现正常）。
- **局域网全链路（P0）**：开关时间线（切换中→应答出地址二维码→核对「已开启」，含真实 IP 192.168.10.143）、端口被占回滚（占 0.0.0.0:3000 触发，836ms 显示「切换失败，服务端已回滚」）、并发双切换收敛到最后目标、server-info 契约/非法 body 400/LAN IP 可达性双向/SPA 深链回退/重启复位/终端二维码全部实测通过；S-05 虚拟网卡过滤正向输出正确（本机无虚拟网卡可负向触发，范围注记）。
- **唯一缺陷 DEF-01（P1）**：列表行标题/IPA 缺 overflow-wrap（M1 修复覆盖面遗漏），已修复并 12 矩阵复测零横滚；修复过程发现并同步修正了「仅加 anywhere 导致 abandon 断词」的次生回归（详见第 7 章）。
- **回归冒烟**：全局搜索/板块过滤/标签页、编辑保存（PUT 落盘核验）、wiki 跳转+反向引用、TTS 喇叭（speaking=true）、删除两态流程（404+跳回板块）、`npm run build` 全部通过。
- **工作区复原**：fixture 全清、git 历史零污染（误入提交已摘除）、服务与端口全部释放。

---

## 7. 修复执行记录（2026-08-30，提交 `0ff65b9`）

### DEF-01 修复

**改动**（`web/src/components/NoteList.vue`，基础规则处，桌面+移动端同时生效）：
- `.row-title` / `.row-ipa` 加 `overflow-wrap: anywhere`（同阅读页 `.note-title` 的 M1 同款处理）。
- `.row-source` 加 `flex: 1 1 0`（+`min-width: 0`）成为弹性填充项——**此项为视觉回归逼出的必要补充**：首版只加 anywhere 后截图发现真实词条 abandon 在 375px 列表被挤成「abando/n」断词换行（标题获得「可断到单字符」能力后反而输掉 flex 收缩竞争，被大 max-content 的 source 挤压）；source 独自吸收挤压（省略号截断）后正常标题保持完整单行，病态超长标题被钳到剩余宽度内折行。

**真实验证**（无头 Chrome CDP 精确视口，全部执行）：
- 修复前基线：home/vocab/tagdetail × 320/375/767/768 全部 `hscroll=true`（docScrollW 868）。
- 修复后：**12/12 矩阵单元 `hscroll=false`**（docScrollW == clientWidth）；fixture 行标题 222px 宽多行折行、`overflowWrap=anywhere` 生效。
- 次生回归核验：abandon 行标题高度 46px（断词两行）→ 修正后 **23px（恢复单行）**，IPA 完整、source 正确省略（截图核验）；首页/vocab/阅读页在 375 与 1280 无横滚。

### OPT-02 修复

**改动**（`web/src/views/NoteView.vue` `placeBar()`）：末行补 `Math.max(m, …)` 顶部钳制。

**真实验证**（有头 Chromium 375×667）：双击选中→滚动至选区完全出视口上缘（selTop=-37，精确复现修复前场景）→ 工具条 top=**8**、完整可见（修复前 -7）；选区在视口内时定位行为与修复前一致。

**收尾**：`npm run build` 通过（320ms）；fixture 文件系统删除、索引复原 6 条真实词条、`git status` 仅 `?? .zcode/`。
