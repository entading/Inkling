# M4 功能面全维度测试报告

- **测试对象**：M4 录入（编辑页 / 新建页 / 服务端写接口 / 草稿 / 缓存失效联动）
- **测试日期**：2026-08-30
- **测试环境**：Windows 10 (26200) x64 · Git Bash · Node v24.19.0 · dev 服务（server 127.0.0.1:3000 + Vite 5173）· ZCode 内置浏览器（Chromium，桌面 1280×900 / 移动 375×667）
- **代码基线**：b84211b（M3 测试报告提交），git 工作区干净

## 1. 执行概要

### 环境与准备

- 会话开始时 3000/5173 已有 dev 实例在运行；经核实其响应含 M4 新增 `raw` 字段、板块计数与磁盘一致，**判定为最新代码的运行实例并复用**（git 树干净，无陈旧代码风险）。
- 测试数据：`t_m4_` 前缀 fixture 6 个（完整/无fm/坏fm/特殊标题/草稿载体/拖入目标）经文件系统创建、chokidar 自动收录；另有 POST 创建的 4 个（t_m4_post01/t_m4_special/t_m4_race/t_m4_post03）与 PF-01 用的 t_m4_big。
- 本机显示缩放问题按 M3 运维注记规避：断点验证使用 `setViewportSize` 精确整数视口（1280×900 / 375×667）。
- S-02 需要真实宕机场景：终止 tsx 子进程模拟服务端不可用（详见第 5 节运维注记）。

### 用例统计

| 优先级 | 总数 | 通过 | 失败 | 阻塞 | 豁免 |
|---|---|---|---|---|---|
| P0 | 34 | 34 | 0 | 0 | 0 |
| P1 | 15 | 15 | 0 | 0 | 0 |
| P2（观察记录） | 6 | 6（记录在案） | 0 | 0 | — |
| 回归冒烟 R-01..R-06 | 6 | 6 | 0 | 0 | — |
| **合计** | **61** | **61** | **0** | **0** | **0** |

**结论：M4 功能面通过测试，零缺陷。**

## 2. 失败与缺陷清单

无。全部 P0/P1 用例一次性通过（部分用例经测试脚本/数据修正后复测通过，见第 4 节注记，均为测试侧问题，非应用缺陷）。

## 3. P2 观察项清单（已确认边界，勿报缺陷）

| # | 用例 | 现象取证 | 说明 |
|---|---|---|---|
| 1 | S-05a | 用 fetch 延迟包装（900ms）制造在途窗口：在途输入后 resolve，header 显示「已保存 ✓」但源码含未保存的新输入 | lastSaved 取 resolve 时 draft 值，规格已声明勿报 |
| 2 | S-05b | 改 FM title 保存成功后，头部仍显示载入时旧标题（t_m4_草稿载体） | 用载入时 note.title，规格已声明勿报 |
| 3 | S-05c | 保存后继续输入，header 持续显示「已保存 ✓」直到下次保存 | 同上 |
| 4 | G-04 | 粘贴 md 文本 = 原生 textarea 行为；EditView 模板无 @paste 处理（代码核实 + 编辑输入实测正常） | 已确认设计，P2 记录 |
| 5 | G-05 | `dropError` 在 load() 不重置（代码层面属实）；但 UI 所有离开编辑页路径均经组件卸载（ref 随实例销毁），实测切换词条后**无残留**、无用户可见影响 | 不可达的代码层残留 |
| 6 | PF-01 | ~250KB（125K 字符，CJK 为主）PUT 15ms / GET 详情 21ms / search-index(20条) 12ms | 无性能问题 |
| 7 | PE-01 | 非 JSON → 400（FST_ERR_CTP_INVALID_JSON_BODY）；content 深嵌套对象 → 400 自定义文案；6MB body → 413（FST_ERR_CTP_BODY_TOO_LARGE）；三连击后 /api/boards 200 服务存活 | 优雅降级达标 |

## 4. 非缺陷现象清单（与 AGENTS.md 已确认决策一致，防未来误报）

1. **L-02 附带观察**：错误态（词条不存在/非法板块）下「保存」按钮仍可点击，点按后显示「保存失败：板块不存在…」/「词条不存在…」，PUT 返回 404/400，无崩溃、无写盘。无害 UX 观察，不计缺陷。
2. **坏 FM 词条的两端渲染差异**：阅读页正文 = 全文原文（坏 FM 行以文本可见，M1 缺陷修复 1 兜底语义）；编辑页预览对全文源码 stripFrontmatter（M4 复检 1），坏 FM 围栏被剥除不显示。两端各自语义自洽、规格未覆盖坏 FM 时编辑预览的行为，仅作记录。
3. **C-02 空 body 变体**：body 为空字符串/非 JSON 时返回 Fastify 标准 400（FST_ERR_CTP_EMPTY_JSON_BODY / FST_ERR_CTP_INVALID_JSON_BODY）而非自定义文案——与 M3 修复 3(b) 同款模式（自定义文案只覆盖「解析成功但类型不符」路径），状态码契约满足。body 为 JSON 字符串（非对象）时返回「未知板块：」400，状态码契约满足。
4. **C-03 并发语义**：10 个并发 POST 同 slug，恰好 1 个 200、9 个 409，文件无损坏（写后立即 upsert 保证内存索引即时一致）。
5. **C-04 落盘语义**：PUT 后磁盘文件与请求 content 字节级一致（服务端不重写）；FM 显式 `updated` 保存后不变；无 `updated` 时由 mtime 兜底（实测当日 2026-08-30）。
6. **M-02**：移动端 textarea `overflow-y: auto`（固定 60vh 高度下源码内滚为编辑器固有形态）；预览 pane 无内滚（scrollHeight == clientHeight，pane 随内容增长由页面自然滚动）。符合「源码高 60vh、页面自然滚动、预览无内滚」。
7. **M7 删除为两步按钮**（删除 → 确认删除？），不使用 window.confirm——X-04/R-05 实测确认。
8. **删除触发 `git add -A notes` 的卷入行为**（M3 运维注记 / M7 复检 7 同款）：X-04 经 UI 删除从未跟踪的测试词条时，自动提交把当时 notes/ 下全部未跟踪测试文件（10 个）卷入历史（提交 3a3516f）。**该提交系测试产物，已 `git reset HEAD~1` 复原**，未污染项目历史。此注记补充：UI 删除与 DELETE API 同样存在该风险，后续批量测试清理必须走文件系统 rm。
9. **IAB 自动化伪影（测试环境现象，非应用缺陷）**：`press("Control+s")` 在跨 REPL 调用、依赖 fill 焦点时偶发不达（2 次）；click 聚焦后必达，合成 keydown 事件亦能触发保存处理器（PUT 发出）。以 click 聚焦为标准操作后全部稳定。
10. **S-02 宕机期间**：Vite 代理对已死上游快速返回 502（实测 17ms），前端显示「保存失败：Bad Gateway」；草稿保留、dirty 保持、服务恢复后重试成功。

## 5. 分节执行记录（要点与证据）

### 第 2 节 服务端契约（C-01..C-08，Node fetch 实测）

- **C-01 ✅**：POST 200；骨架 FM 含 title/created（本地当天 2026-08-30）、不含 updated；tags 非空写入、空则无该字段；vocab/phrase 模板各自正确；特殊标题 `M4: 特殊 "引号" title` 被 gray-matter stringify 转义为 `title: 'M4: 特殊 "引号" title'`。
- **C-02 ✅**：缺 body（JSON null）→ 400「body 不能为空」；title 空白 → 400「title 不能为空」；board 非法 → 400「未知板块：foo，可选值为 vocab/phrase/sentence/grammar」；slug 含 `/`、CON、con.md、con.md.md、点结尾、空格结尾 → 400「slug 非法：…（不能含路径分隔符，不能是 Windows 保留名，不能以点或空格结尾）」。
- **C-03 ✅**：重复 slug（fixture + 真实词条 abandon）→ 409「slug 已存在：vocab/…，请换一个」；10 并发恰好 1×200 + 9×409。
- **C-04 ✅**：PUT 全量写源码，磁盘字节级一致；显式 updated 保留（2020-01-01）；无 updated 时 mtime 兜底。
- **C-05 ✅**：不存在词条 404「词条不存在：…」；content 非字符串/缺失 → 400「body 必须为 { content: string }」。
- **C-06 ✅**：PUT 后零延迟（无 sleep）查列表与 search-index，新 title/body token 立即可见（写后 upsert，不等 chokidar）。
- **C-07 ✅**：raw 字段与磁盘文件逐字一致（正常 FM / 坏 FM / 无 FM 三种）。
- **C-08 ✅**：`attrib +R` 只读 fixture 后 PUT → 500「写入失败：EPERM: …」；同服务 POST 其他词条 200 不受影响；权限即时恢复、文件未损坏。

### 第 3 节 编辑页 load（L-01..L-04，浏览器实测）

- **L-01 ✅**：左栏全文源码含 FM 原文；右栏渲染剥 FM（h1/段落/strong/code）；头部标题 = note.title。
- **L-02 ✅**：不存在词条/非法 board → 错误块「词条不存在或加载失败」+ 具体原因，编辑器未渲染。
- **L-03 ✅**：进入编辑页 → 返回阅读页 → 再进 → 侧边栏离开，全程 performance 资源请求零 `/api/notes/undefined/...`（route.name 守卫生效）。
- **L-04 ✅**：坏 FM 词条可打开（raw 全文原样）、Ctrl+S 保存 200、文件原样写回。

### 第 4 节 草稿机制（D-01..D-06，localStorage 断言 + confirm 间谍）

- **D-01 ✅**：输入后立即读取 null；静置 3.6s 后草稿键 === 当前全文源码（防抖准确）。
- **D-02 ✅**：编辑后 2 秒内离开：确认通过后草稿**立即写入**（不等 3 秒防抖，M4 复检 3）；重进恢复 + 横幅「检测到未保存的草稿，已恢复源码…」。
- **D-03 ✅**：草稿 ≠ 磁盘 → 恢复 + 横幅；× 可关闭；草稿 == 磁盘 → 不恢复、无横幅。
- **D-04 ✅**：保存成功后草稿键被移除；刷新不再恢复；磁盘内容确认已落盘。
- **D-05 ✅**：confirm 间谍取证——文案「当前有未保存的修改，确定离开？（未保存内容将暂存为草稿，回到本页可恢复）」；取消 → 停留本页且不落草稿；确定 → 跳转且立即落草稿；无改动直接离开不弹确认（D-02/D-04 等用例中多次自然发生）。
- **D-06 ✅**：连续输入（间隔 1s×2）零写入、停顿 3.4s 恰好 1 次且等于最后内容（防抖）；无 FM 词条同样走草稿；localStorage 重定义为抛错后编辑/预览/保存全部正常（零 JS 错误、PUT 成功），try/catch 静默降级属实。

### 第 5 节 实时预览（P-01..P-05）

- **P-01 ✅**：150ms 时预览未刷新（防抖窗口内）、550ms 已刷新；预览不含 FM 痕迹。
- **P-02 ✅**：改 FM title 后预览正文不变、源码栏可见变化。
- **P-03 ✅**：FM 前插入空行 → `---` 块按正文渲染（hr ×1 + FM 文本作为正文可见）。
- **P-04 ✅**：预览中 `[[abandon]]` 渲染为 `a.wiki-link`（无 href）、`[[t_m4_不存在link]]` 渲染为 `a.wiki-link.is-missing`（红虚线）；点击两者 URL 均不变、Enter 均不跳转（interactive=false，M5 已确认语义）。
- **P-05 ✅**：悬停存在链接 → `.wiki-preview-*` 预览卡出现（标题/ipa/标签/来源/摘要）；mouseout → 隐藏；悬停缺失链接 → 无卡；编辑触发预览刷新（300ms 防抖）→ 旧卡消失。

### 第 6 节 保存流程（S-01..S-05）

- **S-01 ✅**：Ctrl+S preventDefault 实测 true（拦截浏览器默认保存）；fetch 延迟包装放大窗口后三连按 Ctrl+S 仅发 1 个 PUT（saving 中忽略）；按钮与快捷键同一 save() 等价（各实测 PUT 成功）。
- **S-02 ✅**：成功「已保存 ✓」；真实关停服务端后保存 →「保存失败：Bad Gateway」；草稿保留（key 含测试行）、dirty 保持（源码仍含未保存内容）；服务恢复后重试 → 已保存 ✓ + 草稿清除（与 PE-02 合并验证）。
- **S-03 ✅**：保存后阅读页内容与源码一致（含刚保存行）；无 FM 词条保存后阅读页标题回退为文件名 t_m4_无fm。
- **S-04 ✅（严格 SPA 验证）**：哨兵变量（window.__m4sentinel=42）证实全程同一文档：/tags 建缓存 → SPA 进编辑页 → 加全新标签 m4tagfresh2 保存 → SPA 点侧边栏回 /tags **立即**显示新标签；全局搜索（同文档）按新标签词立即命中该词条（invalidateSearchIndex 联动，M4 复检 4 修复有效）。
- **S-05 ✅（P2 取证）**：见第 3 节观察项 1–3。

### 第 7 节 frontmatter 软保护（F-01..F-06）

- **F-01 ✅**：删除 FM → 警示条实时出现（`role=alert`、红色 border rgb(239,68,68)、「恢复」+ × 按钮）。
- **F-02 ✅**：点「恢复」→ FM 块原样拼回开头（`---\ntitle: t_m4_完整…`）、横幅消失。
- **F-03 ✅**：× 关闭后继续编辑（仍无 FM）不再出现；手动补回合法 FM 块 → 横幅实时消失（无需点 ×）。
- **F-04 ✅**：无 FM 词条删除大半内容 → 零警示条；Ctrl+S → confirm 间谍零调用、保存成功。
- **F-05 ✅**：FM 缺失时 Ctrl+S → 二次确认「frontmatter（标题 / 标签 / 来源等元信息）已被删除，保存后阅读页标题将回退为文件名、…」；取消（confirm=false）→ PUT 数 0、警示条仍在；确定 → PUT 1 次、已保存 ✓、警示横幅消失（savedFrontmatter 同步 null）。
- **F-06 ✅**：已确认保存 FM 缺失后，再编辑与再补回 FM 均不再警示。

### 第 8 节 拖入/粘贴（G-01..G-05，合成 DataTransfer + File 实测）

- **G-01 ✅**：dragenter → `.editor-split` 获 `drag-over` 类、outline `dashed 2px`（虚线）；dragleave 恢复；非空拖入 confirm「当前内容尚未保存，拖入文件将替换全部源码，确定？」——取消不替换、确定替换；空编辑器拖入零确认直接替换。
- **G-02 ✅**：拖 note.txt → 「仅支持 .md 文件，收到「note.txt」」+ 源码未被替换。
- **G-03 ✅**：拖入内容（含 tab、行尾双空格、引号特殊字符）保存后磁盘字节级一致（不重写不解析 FM）。
- **G-04 ✅（P2）**：见第 3 节观察项 4。
- **G-05 ✅（P2）**：见第 3 节观察项 5（实测不残留）。

### 第 9 节 NewNote 新建页（N-01..N-06）

- **N-01 ✅**：`M4 Test Word!` → `m4-test-word`；`验证 Test 123!@#` → `验证-test-123`（中文保留、符号折叠、去首尾 -）；手动改过 slug 后 title 失焦不覆盖（slugTouched）。
- **N-02 ✅**：`?board=phrase` → 默认短语；`?board=foo` → 回落词汇；`?slug=t_m4_stub_xy&title=y标题` → 两项预填且 title 失焦不覆盖 slug（wiki stub 链路）。
- **N-03 ✅**：空 title 提交 → 「请填写标题」；全符号 `---` → 「无法从标题推导 slug，请手动填写一个」，均停留表单。
- **N-04 ✅**：tags `n04tag,英文tag，中文标签`（中英逗号混合）→ FM `tags: [n04tag, 英文tag, 中文标签]`；source 全空白 → FM 无 source 字段。
- **N-05 ✅**：提交成功 → SPA 跳转 `/v/vocab/n04创建联动验证/edit`，左栏为骨架全文（FM 含 title/created、不含 updated + vocab 模板正文）。
- **N-06 ✅**：重复 slug（t_m4_完整）→ 停留表单、slug 框红色边框（rgb(239,68,68)）、错误「slug 已存在：vocab/t_m4_完整，请换一个」、title/tags 不丢；非法 slug（con.md）→ 400 原文展示。

### 第 10 节 缓存失效联动（X-01..X-04）

- **X-01 ✅**：/tags 建缓存（sentinel=77）→ SPA 新建 n04 词条成功 → 同文档 /tags 立即含 n04tag；全局搜索立即命中新词条标题。
- **X-02 ✅**：由 S-04 严格版覆盖（编辑改 title/tags → /tags 与搜索同文档即时反映）。
- **X-03 ✅**：保存含 `[[t_m4_完整]]` 的正文后，目标词条阅读页反向引用面板同文档立即出现「词汇 / t_m4_草稿载体」（Promise identity 联动）。
- **X-04 ✅**：删除词条（UI 两步删除）→ 同 key 草稿被清（null）、跳回板块页。附带触发 M7 git 自动提交（见第 4 节注记 8，已复原）。

### 第 11 节 移动端单视图（M-01..M-03，375×667）

- **M-01 ✅**：默认仅源码 pane 可见、源码/预览 chips 显示；点「预览」→ 预览 active；点回「源码」→ 源码 active。
- **M-02 ✅**：textarea 高 400px/667px = 0.6（精确 60vh）；页面自然滚动；预览无内滚；桌面（1280×900）chips 隐藏、双栏并排（flex row，pane 左右相邻）。
- **M-03 ✅**：移动端保存按钮 enabled、点击保存成功（已保存 ✓）；saving 态已在 S-01/S-02 中取证。

### 第 12 节 性能/安全/健壮性（PF-01、PE-01、PE-02）

- **PF-01 ✅**、**PE-01 ✅**：见第 3 节观察项 6–7。
- **PE-02 ✅**：真实关停服务端 → 编辑页错误态（「保存失败：Bad Gateway」）、草稿保留；服务恢复后草稿恢复（重载 + 横幅）且重试保存成功（已保存 ✓ + 草稿清除）。

### 第 13 节 回归冒烟（R-01..R-06）

- **R-01 ✅**：标签云含 cet6、/tags/cet6 合集含 abandon；全局搜索下拉分组正常（S-04/X-01 中多次实测）。
- **R-02 ✅**：设置页开关 → 开启后「已开启」+ 0.0.0.0 监听；关闭后「未开启」+ 127.0.0.1:3000。
- **R-03 ✅**：abandon 阅读页 wiki 链接（跨板块显式 `sentence/2024-text2-sentence3`）点击正确跳转；反向引用已在 X-03 验证。
- **R-04 ✅**：阅读页「朗读标题」喇叭按钮存在，speechSynthesis 可用。
- **R-05 ✅**：删除两步确认 → 删除成功 → 跳回板块页（X-04 覆盖）。
- **R-06 ✅**：`npm run build` 通过（vue-tsc + vite build，154 modules，354ms）。

## 6. fixture / 权限 / 进程清理与 git 状态确认

- **文件**：11 个测试文件（6 fixture + 4 POST 创建 + t_m4_big）全部经**文件系统 rm** 删除，`find notes -name "t_m4*"` 无残留；用户词条 6 个 vocab + phrase/sentence/grammar 各 1 个完好。
- **localStorage**：测试期草稿键全部清除；TTS voice 等用户偏好键未动。
- **权限**：C-08 的 `attrib +R` 已即时恢复，无残留。
- **git**：测试期间 X-04 触发的 M7 自动提交（3a3516f，卷入 10 个测试文件）已 `git reset HEAD~1` 复原；最终 `git status` 仅 `?? .zcode/`，与会话开始时一致。
- **进程**：会话开始复用的既有 dev 实例在 S-02 宕机测试中被终止（tsx supervisor 未自动重启；终止 supervisor 触发 concurrently 连带终止 Vite，进程组行为）；随后由本会话重启服务端（3000）与 Vite（5173）继续测试。**测试结束时本会话的服务端与 Vite 进程已全部停止，端口 3000/5173 释放。**

## 7. 整体结论

**M4 功能面通过测试。** 61 项用例中 P0 34 项、P1 15 项全部通过，P2 6 项观察记录在案（其中 G-05 实测无用户可见影响），回归冒烟 6 项全部通过。核心契约（骨架生成、全量写源码原样落盘、409/400 校验矩阵、raw 字段、写后即时索引）、草稿机制（防抖/恢复/清除/离开确认/localStorage 降级）、frontmatter 软保护全链路、拖入替换、NewNote 表单与错误处理、缓存失效联动（搜索/标签/反向引用同文档即时反映）、移动端单视图均符合设计与 AGENTS.md 已确认决策，**未发现需要修复的缺陷**。
