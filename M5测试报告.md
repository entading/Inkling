# M5 功能面全维度测试报告（双向链接）

> 测试日期：2026-08-30 ｜ 测试对象：EN_tool M5 功能面（wiki 链接渲染 / 跳转 / 反向引用 / stub 创建闭环 / 失效链接提示条与悬停预览叠加面）
> 测试方式：真实执行（dev 服务 + curl API + Playwright DOM 断言 + Network 观察 + 文件系统/git 核对），无凭代码阅读推断的结论
> 代码基线：测试 `85cc228`（test: M4 报告），工作区起始干净（仅 `.zcode/` 未跟踪）；缺陷修复 `2adc9dc`（fix: M5 测试缺陷修复）

---

## 1. 执行概要

### 1.1 环境信息

| 项 | 值 |
|---|---|
| 服务 | `npm run dev`：server 127.0.0.1:3000 + Vite http://localhost:5173（API 经 Vite 代理） |
| 浏览器 | Playwright Chromium；桌面 1280×900、移动 375×667 |
| 数据基线 | vocab 6 / phrase 1 / sentence 1 / grammar 1（全部真实词条，零改动） |
| fixture | 9 个 `t_m5_` 前缀词条 + 2000 个性能词条（`t_m5_perf/` 子目录）+ D 系列创建的 `grammar/不存在xyz` + R-05 专用 `t_m5_del_target` |
| 构建核验 | `npm run build` 通过（vue-tsc + vite build，350ms） |

### 1.2 用例统计

| 优先级 | 用例数 | 通过 | 缺陷 | 阻塞/豁免 |
|---|---|---|---|---|
| P0（契约核心） | 21 | 21 | 0 | 0 |
| P1（重要边界） | 12 | 11 | 1（DEF-01，已修复 `2adc9dc` 并复测通过） | 0 |
| P2（观察项） | 7 | 6 | 1（DEF-02，由 PF-01 发现定级 P1，已修复 `2adc9dc` 并复测通过） | 0 |
| 冒烟（R-01~R-06） | 6 | 6 | 0 | 0 |
| **合计** | **46** | **44** | **2（均已修复）** | **0** |

---

## 2. 失败与缺陷清单

### DEF-01（P1，已修复 `2adc9dc`）失效链接提示条对 `[[/x]]` 类非法显式目标误报（E-04）

- **复现**：词条正文含 `[[/x]]`（板块为空、slug 非空），打开阅读页。实测用例：fixture `t_m5_词法` 同时含 `[[x/]]`、`[[/x]]`、`[[/]]`、`[[ / ]]`、`[[foo/bar]]`、`[[a/b/c]]`。
- **期望**：`[[/x]]` 按 M5 复检修正 2 的词法校验渲染为**字面文本**（实测渲染层确实如此，`[[x/]]`、`[[/]]`、`[[ / ]]` 同样字面）；提示条只应列出「正文中真实渲染为失效链接」的目标，不应包含字面文本。
- **实际**：提示条显示「本文有 4 个失效链接：**/x**、foo/bar、a/b/c、y」。其中 `/x` 的书写形式 `[[/x]]` 在正文中是纯文本（不可点击、无红色虚线样式），却被计为失效链接并可点击跳新建页——渲染语义与提示条语义不一致。
- **精确范围**：仅「板块为空、slug 非空」的 `[[/x]]` 误报；`[[x/]]`、`[[/]]`、`[[ / ]]`（slug 为空）被正确跳过；`foo/bar`、`a/b/c`、`y`（嵌套括号中成链部分）在正文确实渲染为红色缺失链接，计入正确。
- **初步根因**（只判断不修复）：`NoteView.scanMissingLinks` 的跳过条件为 `if (target.resolved || !target.slug) continue`，只挡「slug 为空」；而 `markdown.ts` inline rule 对含 `/` 目标要求**板块与 slug 均非空**（`!text.slice(0, sep).trim() || !text.slice(sep + 1).trim()`）。两处词法校验不一致——渲染层拒绝、扫描层接受，`[[/x]]` 落入缝隙。
- **修复方向建议**：`scanMissingLinks` 对含 `/` 的目标补「board 非空」判断；或将 inline rule 的空段校验抽为共享函数（如让 `parseWikiTarget` 返回 invalid 标记）供渲染与扫描两处复用，杜绝第三处漂移。
- **修复**：见第 8 节（`2adc9dc`，采用共享函数方案，inline rule 等价重构经渲染快照逐字节 diff 证明）。

### DEF-02（P1，由 P2 用例 PF-01 发现，已修复 `2adc9dc`）失效链接提示条无 overflow-wrap 防护，超长 display 文本导致文档级横向滚动

- **复现**：词条正文含 5000 字符无空格的失效 wiki 目标（`[[AAAA…5000…]]`），375px 视口打开阅读页（fixture `t_m5_性能`）。
- **期望**：提示条文本折行、不产生文档级横向滚动——与 M1 修复（`.note-title`/`.note-body` 的 `overflow-wrap: anywhere`）和 M3 修复（列表行三件套）的既定防溢出语义一致。
- **实际**：唯一溢出元素为提示条内 `a.missing-target`（宽 43052px、textLen=5000），`document.scrollWidth=43117` vs `clientWidth=360`，**整页横向滚动 42757px**。对比证据：正文内同一超长链接因 `.note-body` 的 `overflow-wrap: anywhere` 正常折行（实测 longLinkWrapped=true、宽度 246px），防溢出遗漏面仅在提示条组件；computed 样式确认 `.missing-banner` 与 `.missing-target` 的 `overflow-wrap` 均为 `normal`。
- **初步根因**：`.missing-banner` 虽有 `flex-wrap: wrap`，但 `.missing-target` 作为 flex item `min-width: auto`（按 max-content 计宽），不可断词长文本不收缩也不折行。
- **修复方向建议**：`.missing-banner`（或 `.missing-target`）加 `overflow-wrap: anywhere`（M1 先例同款），必要时对 flex item 配 `min-width: 0`。
- **修复**：见第 8 节（`2adc9dc`，单加 `overflow-wrap: anywhere` 于 `.missing-banner`，评估无需 M3 的 flex 配套）。

---

## 3. P2 观察项清单（记录不阻塞）

1. **A-09 索引未就绪闪变（取证通过）**：路由拦截将 search-index 延迟 800ms 后逐 20ms 采样——页面加载后约 20ms 链接先按「存在」渲染（蓝色 `wiki-link`，missingCount=0），约 620ms 索引就绪后自动修正为红虚线（missingCount=1）。与设计注释「未就绪按存在渲染，就绪后重渲染修正」一致；本地窗口亚秒级。局域网（1-2s）下窗口会等比变长，机制相同（未在真机实测）。
2. **C-05 4 空格缩进代码块残留（已知局限确认）**：双向取证——t_m5_目标B 面板**无** t_m5_代码引用（围栏/行内代码剥离生效），boost 面板**有** t_m5_代码引用（缩进块中 `[[boost]]` 生成了反向引用）；同时该词条正文渲染 0 个链接（代码内不成链）。与 AGENTS.md M5 复检修正 1 的已知残留完全一致，不报缺陷。
3. **C-08 面板顺序 = search-index 顺序（通过）**：boost 面板 [目标B, 代码引用, 性能] 与 `/api/search-index` 中三者相对顺序一致（2003 条规模复核）。注意 search-index 顺序与 `/api/notes?board=xxx`（title 排序）本就不同，面板对照物按任务书定义取 search-index。
4. **X-01 无幻影引用（通过）**：正常流程（C-01/C-07）与放大竞态窗口（C-06：search-index 延迟 1.5s 下 A→boost→serendipity 快速切换）均未见旧词条引用数据写入新词条页。
5. **X-02 `![[x]]` 计入反向引用（确认）**：t_m5_词法 的 `![[abandon]]` 渲染为字面 `!` + 正常链接，abandon 的面板因此出现 t_m5_词法——链接确实渲染了，计入与渲染语义一致（已确认决策）。
6. **PF-01 性能数据**：121 个 wiki 链接词条（约 7.4KB 正文）渲染 domContentLoaded 65ms，链接计数 121/121 准确。
7. **PF-02 性能数据**：2015 词条（search-index 427KB）下，打开被 2000 词条引用的 boost，从导航到反向引用面板 2003 条全部渲染完成共 **1016ms**（本地，含索引拉取 + Fuse 建立 + 反向图构建 + DOM 渲染）。
8. **chokidar 批量写入事件丢失（新观察，边界外场景）**：Windows 下一次批量写入 2000 个 md 后，增量收录停在 482/2015 并不再增长（等待 >1 分钟、单条 API 404）；**重启服务全量扫描后完整恢复 2015**。设计场景（5.7「随手往文件夹丢手写 md」，单/少数文件）未受影响；批量导入属设计边界外，不报缺陷。若未来支持批量导入，需评估 `usePolling` 或引导重启。

---

## 4. 非缺陷现象清单（与已确认决策一致，防未来误报）

1. **abandon 面板显示 2 条**（sentence + t_m5_词法）：后者来自 `![[abandon]]`（见 X-02），非重复计数缺陷。
2. **D-02 URL 保留 `board=foo` 参数而页面下拉回落 vocab**：NewNote 的 `isBoard` 守卫回落默认板块是 M5 偏离 5 既定行为。
3. **D-05 提交 409 时 console 出现网络错误日志**：浏览器对非 2xx 响应的正常记录。
4. **E-04 提示条中 foo/bar、a/b/c、y 三项计入正确**：它们在正文确实渲染为红色缺失链接；误报范围仅 `/x` 一项（DEF-01 已精确圈定并修复）。
5. **wiki 链接无 `href`**：设计如此（跳转由 `.note-body` 事件委托处理），键盘可达由 `tabindex=0` + `role=link` + Enter 承担。
6. **编辑页预览点击 wiki 链接不导航**：`interactive=false` 设计（所见即所得但不劫持编辑流程）。
7. **全局搜索下拉出现 t_m5_perf 系列词条**：测试 fixture 大批量在库时的正常匹配行为，非缺陷。
8. **A-01 用例中 `[[phrase/abandon-oneself-to]]` 同页验证**：显式形式命中与裸形式回落（A-02）均为设计 4.3 语义。

---

## 5. 各节执行结果明细

| 节 | 用例 | 结果 | 关键证据 |
|---|---|---|---|
| 2 渲染 | A-01 | ✅ | `a.wiki-link` 无 href、data-board/slug/tabindex/role 齐全、显示文本=原文、无 data-title、蓝色虚线 |
| 2 渲染 | A-02 | ✅ | 裸形式回落 phrase（data-board=phrase）、子目录词条命中、`[[t_m5_不存在]]` → vocab 兜底 + is-missing 红色 + data-title |
| 2 渲染 | A-03 | ✅ | `[[grammar/非谓语动词]]` 命中蓝色；`[[grammar/不存在xyz]]` is-missing + data-title=不存在xyz（不带板块前缀） |
| 2 渲染 | A-04 | ✅ | `[[foo/bar]]` 恒 is-missing（board=foo 不回落） |
| 2 渲染 | A-05 | ✅ | `[[x/]]`、`[[/x]]`、`[[/]]`、`[[ / ]]`、`[[   ]]` 全部字面渲染（textContent 原文可见、无 a 标签） |
| 2 渲染 | A-06 | ✅ | `![[abandon]]` → 字面 `!` + 正常链接（文本 "!abandon"） |
| 2 渲染 | A-07 | ✅ | 围栏/波浪线/缩进/行内代码中 `[[...]]` 全部纯文本，正文 0 个 wiki 链接 |
| 2 渲染 | A-08 | ✅ | `[[a/b/c]]` → board=a、slug=b/c、data-title=b/c（只切第一个 /） |
| 2 渲染 | A-09 | ✅(P2) | 闪变取证见观察项 1 |
| 3 跳转 | B-01 | ✅ | 点击 → `/v/phrase/abandon-oneself-to` |
| 3 跳转 | B-02 | ✅ | 点击缺失 → `/new?board=vocab&slug=t_m5_不存在&title=t_m5_不存在`（中文正确编码），新建页预填齐全 |
| 3 跳转 | B-03 | ✅ | Tab 聚焦 wiki 链接、`:focus-visible` 生效（底色 #eff6ff + 实线下划线）、Enter 同款跳转 |
| 3 跳转 | B-04 | ✅ | 编辑页预览点击/Enter 均不导航（URL 保持 /edit）、悬停预览卡照常弹出 |
| 3 跳转 | B-05 | ✅ | 普通链接正常带 href 渲染；点击锚点链接未被劫持（URL 仅追加 #anchor） |
| 4 反向引用 | C-01 | ✅ | 「长难句 · 2024 Text2 第三句」在面板、位于正文之后（条数说明见非缺陷 1） |
| 4 反向引用 | C-02 | ✅ | serendipity（无引用）面板整节不渲染 |
| 4 反向引用 | C-03 | ✅ | 同目标引用两次，面板只记 1 条 |
| 4 反向引用 | C-04 | ✅ | 自引用链接正常渲染、面板不渲染（自引用排除） |
| 4 反向引用 | C-05 | ✅ | 双向取证见观察项 2（缩进残留为已知局限 P2） |
| 4 反向引用 | C-06 | ✅ | 1.5s 延迟竞态窗口快速切换，无幻影写入 |
| 4 反向引用 | C-07 | ✅ | 编辑 B 加 `[[t_m5_目标A]]` Ctrl+S 保存 → 纯 SPA 导航回 A → 面板出现 B（Promise identity 联动） |
| 4 反向引用 | C-08 | ✅(P2) | 面板顺序 = search-index 顺序（见观察项 3） |
| 5 stub | D-01 | ✅ | 预填 board=grammar、slug/title=不存在xyz；title 改动 blur 后 slug 不被覆盖 |
| 5 stub | D-02 | ✅ | URL `/new?board=foo&slug=bar&title=bar`、页面下拉回落 vocab（见非缺陷 2） |
| 5 stub | D-03 | ✅ | 「创建并编辑」成功 → 进 `/v/grammar/不存在xyz/edit` |
| 5 stub | D-04 | ✅ | SPA 返回宿主页：链接红→蓝、提示条消失（闭环完整） |
| 5 stub | D-05 | ✅ | slug 改 abandon 提交 → 409 停在表单、错误文案「slug 已存在：vocab/abandon」、slug 框红色边框 |
| 6 提示条 | E-01 | ✅ | role=alert、计数「本文有 1 个」、原始书写文本、可 × 关闭 |
| 6 提示条 | E-02 | ✅ | 同一缺失目标引用两次，去重 1 条 |
| 6 提示条 | E-03 | ✅ | 正文缺失 1 条 + 代码内缺失 1 条 → 提示条只显示正文那条（stripCodeText 生效） |
| 6 提示条 | E-04 | ❌→✅ | **DEF-01**，已修复 `2adc9dc` 并复测：提示条 4→3 条（/x 消失）、渲染层 innerHTML 逐字节不变（见第 8 节） |
| 6 提示条 | E-05 | ✅ | 拦截 search-index（abort）→ 正文照常渲染、提示条静默不显示 |
| 7 边界 | X-01 | ✅(P2) | 见观察项 4 |
| 7 边界 | X-02 | ✅(P2) | 见观察项 5 |
| 7 边界 | X-03 | ✅ | 存在目标弹卡（title/ipa/tags≤2/source/摘要，字段齐全：非谓语动词卡验证 tags/source/excerpt）、缺失不弹、编辑页预览生效（B-04 交叉证据） |
| 8 移动端 | M-01 | ✅ | 375×667 下蓝链跳转、红链 stub 流程可用 |
| 8 移动端 | M-02 | ✅ | 提示条与面板视口内正常显示、scrollWidth 360=360 无横滚（普通内容） |
| 9 性能/安全 | PF-01 | ❌→✅ | 渲染性能本身 65ms 良好，但发现 **DEF-02**；已修复 `2adc9dc` 并复测：375/1280 双宽度零横滚（见第 8 节） |
| 9 性能/安全 | PF-02 | ✅(P2) | 2015 词条 1016ms 全链路（见观察项 7） |
| 9 性能/安全 | PE-01 | ✅ | 嵌套括号 `[[x [[y]] z]]` 不崩（y 成链、其余字面）；5000 字符目标不崩（渲染折行正常，横滚问题归 DEF-02） |
| 10 冒烟 | R-01 | ✅ | 全局搜索 twinkle 命中（词条+分组）；标签云 9 标签计数正确 |
| 10 冒烟 | R-02 | ✅ | LAN 开关往返：应答契约=目标状态，server-info 终态一致（0.0.0.0 ↔ 127.0.0.1），与 M3 偏离 3 一致 |
| 10 冒烟 | R-03 | ✅ | 编辑页 Ctrl+S「已保存 ✓」→ 阅读页显示新增行（PUT 全量落盘） |
| 10 冒烟 | R-04 | ✅ | 喇叭按钮存在（aria-label=朗读标题）、点击后 speechSynthesis.speaking=true |
| 10 冒烟 | R-05 | ✅ | 两态确认（删除→「确认删除？」→执行）、点外取消回常态、删除后回板块页；git 无副作用（见第 6 节） |
| 10 冒烟 | R-06 | ✅ | `npm run build` 通过（vue-tsc + vite，350ms） |

---

## 6. fixture / 进程清理与 git 状态确认

- **fixture 清理**：9 个基础 fixture + `notes/vocab/t_m5_perf/`（2000 文件，整目录 rm）+ D 系列创建的 `notes/grammar/不存在xyz.md` 全部走**文件系统 rm**（未用 DELETE API，避免 git add -A 卷入未跟踪文件）；`t_m5_del_target` 为 R-05 专用，经 UI 删除流程移除（其从未被 git 跟踪，`git add -A` 后无净变更 → 不产生提交，与 M7 复检 7 已确认行为一致）。清理后四板块仅剩真实词条：vocab 6 / phrase 1 / sentence 1 / grammar 1，API 计数核对一致。
- **进程清理**：启动前端口 3000/5173 被前会话遗留 node.exe（PID 36764/23732）占用，树杀后启动（已记录）；测试中因 PF-02 需全量扫描重启服务一次；结束后停服务并清理残留 node 进程，两端口已确认释放。
- **git 状态**：`git status --short` 测试前 `?? .zcode/` → 测试后 `?? .zcode/`，**完全一致**；`git log` 无新增提交；真实词条零改动（含内容与文件时间无关的 mtime 亦未触碰——全部写操作均针对 t_m5_ 前缀文件）。

---

## 7. 整体结论

**M5 功能面：通过测试（两项缺陷已修复并复测）。**

- P0 契约核心 21/21 全部通过：wiki 链接解析五类形态（裸形式回落/显式精确/非法板块/词法非法/代码不成链）渲染语义与设计 4.3 及 AGENTS.md 已确认决策逐条吻合；点击与键盘双通道跳转、stub 创建闭环（预填→创建→索引失效→返回修正→提示条消失）端到端无断点；反向引用的构建、去重、自引用排除、代码剥离、缓存失效联动、竞态守卫全部实测成立。
- P1 12 项中 11 项直接通过，1 项为 **DEF-01**（提示条对 `[[/x]]` 误报）；P2 的 PF-01 发现 **DEF-02**（提示条超长文本文档级横滚，定级 P1）。两项缺陷均已在 `2adc9dc` 修复并按测试同标准复测通过（修复执行记录见第 8 节）。
- 两项缺陷均为 M7 提示条叠加面与 M5 渲染语义之间的缝隙（DEF-01）及样式防护遗漏（DEF-02），M5 本体（markdown.ts inline rule / backlinks.ts / MarkdownViewer 跳转与预览）未发现契约级缺陷——DEF-01 修复对 inline rule 的重构经渲染快照逐字节 diff 证明零回归。
- P2 观察项 8 条记录在案。

**运维注记（供后续测试会话参考）**：① Windows 下 chokidar 对批量写入（2000 文件）存在增量事件丢失（482/2015），重启全量扫描可恢复，批量类 fixture 测后需核计数；② run_code 环境无 `setTimeout`/`URL`/Node `location`，需用 `page.waitForTimeout` 与 `page.evaluate` 内的浏览器 API 替代；③ `/tmp` 路径在 Git Bash 与 Windows 原生 node 之间不互通，curl 落盘文件勿用 `/tmp` 承接后由 node require。

---

## 8. 修复执行记录（2026-08-30，提交 `2adc9dc`）

修复遵循项目「解析规则只写一份」哲学（`stripCodeText`/`WIKI_LINK_RE` 共享先例）与 M2/M3 修复流程先例；验证方式与测试同标准（真实启动服务 + Playwright DOM 断言 + 快照 diff + `npm run build`）。

### DEF-01 修复：提示条词法校验对齐 inline rule

- **改动**：
  1. `web/src/lib/markdown.ts` 新增导出 `isLegalWikiText(text)`（显式形式板块与 slug 均须非空，非法目标渲染为字面文本），inline rule 原两段条件（text 空 + 含 `/` 空段）等价替换为 `if (!text || !isLegalWikiText(text)) return false`；
  2. `web/src/views/NoteView.vue` 的 `scanMissingLinks` 在 trim 后加 `if (!display || !isLegalWikiText(display)) continue`，并从 markdown.ts 导入该函数。
- **等价性证明（渲染快照 diff）**：修复前抓取 t_m5_词法 / t_m5_目标A / t_m5_代码引用 三页 `.note-body` innerHTML 存基线（`.zcode/m5_fix_baseline.md`），修复后重抓对比——**三个页面正文 innerHTML 逐字节一致**，inline rule 重构零渲染回归；M5 偏离 1（state.pos 契约）与复检 2（校验在 silent 分支之前）均未受影响。
- **提示条预期差异精确成立**：词法页提示条 4→3 条（仅 `/x` 消失，foo/bar、a/b/c、y 保留）；目标A 页（1 条）与代码引用页（1 条）提示条逐字节不变。
- **backlinks 零影响**：`buildBacklinksMap` 的 `!target.resolved` 已排除非法/缺失目标，未改动。

### DEF-02 修复：提示条 overflow-wrap 防溢出

- **改动**：`web/src/views/NoteView.vue` 的 `.missing-banner` 基础规则加 `overflow-wrap: anywhere`（单行，桌面/移动端同时生效）。
- **方案取舍**：`overflow-wrap` 为继承属性且 `anywhere` 计入 min-content 计算，flex item `.missing-target` 随之可收缩折行，无需对 flex item 额外设值；**M3 的 `flex: 1 1 0` 配套在此不需要**——banner 是 flex-wrap 流式布局，各 target 独立换行、无剩余宽度竞争，不存在 M3 场景「正常单词被拆」的排版代价（警告条优先不撑破，与 M1 `.note-title` 同款选择）。
- **复测数据**：375×667 下 docScrollW 360 == clientW 360、1280×900 下 1265 == 1265，**双宽度零横向滚动**（修复前移动端 43117 vs 360）；`.missing-target` computed overflowWrap=anywhere，5000 字符文本折行（移动端宽 229px × 高 3860px 多行）；正常短文本提示条（目标A/代码引用页）渲染零变化。

### 修复后回归冒烟

A-01（wiki 链接属性齐全无 href）、B-02（缺失跳转 /new 预填）、B-03（Tab 聚焦 + Enter 跳转）、E-05（索引不可得提示条静默）、X-03（悬停预览弹卡）、C-05（boost 面板含 t_m5_代码引用，缩进残留语义不变）——全部通过；`npm run build` 通过（vue-tsc + vite，362ms）。

### 修复验证环境清理

修复验证 fixture（词法/性能/目标A/目标B/代码引用/跨板块/子目录 共 7 个）全部文件系统 `rm`；`git status` 复原（`?? .zcode/`）；dev 服务与残留进程全部终止、端口释放；基线快照存于 `.zcode/m5_fix_baseline.md`（工具目录，不入库）。
