# M6 功能面全维度测试报告（发音：IPA 渲染 + 词条朗读 + 选中朗读）

> 测试日期：2026-08-30 · 测试基线：`1ea605a`（M5 测试报告提交，工作区干净）
> 角色：测试工程师（只测试，不修复）· 设计权威：`产品设计方案.md` 3.2 / 5.4 / 6 节 + AGENTS.md 已确认决策

---

## 1. 执行概要

### 1.1 环境信息

| 项 | 值 |
|---|---|
| 服务 | `npm run dev`：Fastify `127.0.0.1:3000` + Vite `http://localhost:5173`（浏览器一律用 localhost） |
| 主浏览器 | Playwright MCP Chrome 151（Win64，**有头/音频栈可用**——voices 非空且 `onstart` 真实触发、`synth.speaking=true`），dpr 1.5 |
| mock 浏览器 | playwright-core 1.62.1 + 系统 Edge（**有头**，`addInitScript` 在应用加载前注入 stub speechSynthesis；临时目录安装，用后即删） |
| 语音库 | **3 个 voice，全部 zh-CN**（Microsoft Huihui / Kangkang / Yaoyao），**无 en-US voice** —— 该机器条件天然构成「无 en-US → `utterance.lang='en-US'` 兜底」的真实路径，en-US 相关分支另用 mock 数组纯函数验证 |
| 视口 | 桌面 1280×900（C-02 翻转/钳制分支测试中临时压到 380/300 高制造滚动余量）；移动 375×667 |
| 环境准备 | 发现并清理 **2 个上次会话遗留的孤儿 dev 进程**（tsx 服务端 + vite，从 concurrently 顶层整树 `taskkill //T //F`） |

### 1.2 用例统计

| 优先级 | 总数 | 通过 | 失败 | 阻塞 | 豁免 |
|---|---|---|---|---|---|
| P0（契约核心） | 20 | **20** | 0 | 0 | 0 |
| P1（重要边界） | 23 | **23** | 0 | 0 | 0 |
| P2（观察取证） | 6 | **6**（全部按取证记录） | 0 | 0 | 0 |
| 冒烟（R-01~R-06） | 6 | **6** | 0 | 0 | 0 |
| **合计** | **55** | **55** | **0** | **0** | **0** |

用例清单之外发现缺陷 **1 项（DEF-01，P1）**、P2 观察项 1 项（与 DEF-01 同根因家族）。缺陷只登记不修复。

### 1.3 分节结果一览

| 节 | 用例 | 结果 |
|---|---|---|
| 2 tts.ts 单元（console 注入 + `?inst=N` 全新模块实例） | U-01~U-07 | 7/7 通过 |
| 3 词条朗读喇叭 | B-01~B-06 | 6/6 通过（B-03 在 mock 无 TTS 环境补跑）|
| 4 选中朗读工具条 | C-01~C-14 | 14/14 通过 |
| 5 设置页发音卡片 | D-01~D-10 | 10/10 通过（D-01/02/03/04/10 在 mock 环境跑）|
| 6 IPA 四处渲染 | E-01~E-03 | 3/3 通过 |
| 7 M7 交互点 | F-01~F-03 | 3/3 通过（F-03 为 P2 取证）|
| 8 移动端 375×667 | M-01~M-03 | 3/3 通过 |
| 9 性能/安全 | PF-01 / PE-01 / PE-02 | 3/3 通过（PE-01 为取证项）|
| 10 回归冒烟 | R-01~R-06 | 6/6 通过 |

---

## 2. 失败与缺陷清单

### DEF-01（P1）：阅读页 `.ipa` 缺 `overflow-wrap`，超长不可断 IPA token 造成文档级横向滚动

- **发现途径**：B-05 取证时（`t_m6_长ipa` 阅读页），标题折行与喇叭尺寸断言均通过，但 `document.scrollWidth 1287 > clientWidth 1265`（1280 视口 + 经典滚动条）。
- **复现步骤**：词条 frontmatter `ipa` 为 76 字符无空格 token → 打开阅读页 `/v/vocab/t_m6_长ipa`。
- **期望**：与 M1（`.note-title`/`.note-body`）、M3（`.row-title`/`.row-ipa` + `.row-source`）、M5（`.missing-banner`）已三次确立的「折行不撑破」不变量一致。
- **实际（取证数据）**：`.ipa` 元素 `scrollWidth 854 > clientWidth 476`；音标文本 Range 右缘 **1287.1px = 文档 scrollWidth**；全页无任何元素盒子越界（`getBoundingClientRect` 无 offender）——典型的「块级元素内不可断文本溢出布局盒」形态。文档出现 7px 横向滚动（1287 − 1265 ≈ 7 + 取整）。
- **优先级**：P1。正常长度 IPA 完全不受影响；触发需病态输入。但该不变量已在 M1/M3/M5 三轮测试中作为缺陷修复过，阅读页音标是同族遗漏面，故按既有尺度登记 P1 而非 P2。
- **初步根因（只判断不修复）**：`web/src/views/NoteView.vue` 的 `.ipa` 样式块只有 `margin/color/font-family/font-style/font-size`，无 `overflow-wrap`。M1 修复覆盖 `.note-title`/`.note-body`，M3 修复覆盖列表行，M5 修复覆盖提示条——阅读页 `.ipa` 是历次「anywhere」防溢出改造均未覆盖的元素。
- **关联观察（见 3.1 OBS-1）**：M7 悬停预览卡 `.wiki-preview-ipa`（`flex-shrink: 0`、无 anywhere）在同款输入下溢出卡片视觉边界，属同一根因家族（IPA 承载元素缺防溢出），修复时可一并评估。

**本节无其他失败/阻塞用例。**

---

## 3. P2 观察项清单（取证记录，不阻塞）

### 3.1 OBS-1：预览卡内超长 IPA 视觉溢出卡片（与 DEF-01 同根因家族）
`[[t_m6_长ipa]]` 悬停预览卡（`.wiki-preview` 固定 260px）内，`.wiki-preview-head` `scrollWidth 734 vs clientWidth 235`，IPA 文本右缘 1295.4px 越过卡右缘 808.6px。因预览卡 `position: fixed`，**不产生文档级横滚**，仅为墨迹越出卡片边界的视觉溢出。病态输入触发，记录供修复 DEF-01 时一并评估。

### 3.2 OBS-2：Chrome ~15 秒长文本截断在本环境未复现
`PE-01`：1770 字符英文长文本朗读，`start@109545 → end@200334`，**连续 90.8 秒自然结束**（AGENTS.md M6 复检 6「Chrome 长文本约 15 秒截断留待 M7」按已知现象不判缺陷——本环境 Chrome 151 + 本地 SAPI 语音组合不触发该现象，如实记录）。无崩溃、应用存活。

### 3.3 OBS-3：C-11 朗读跨页面继续（规格内行为取证）
播放中 SPA 离开阅读页：cap 零新增（无 cancel）、`speechSynthesis.speaking === true` 持续——与 M6 偏离 4「隐藏/路由离开不 cancel」一致。

### 3.4 OBS-4：C-14 失效链接 banner 参与选区朗读（规格内行为）
banner 在 `article.note` 内，跨 banner + 正文选区正常触发工具条，朗读文本含「本文有 1 个失效链接：」。按规格记录。

### 3.5 OBS-5：F-03 预览卡与工具条的滚动策略并存（规格内行为）
同一页面验证：预览卡滚动即隐藏（scroll capture 监听），工具条滚动跟随重定位（C-06a 取证 `barTop = selBottom + 8` 精确命中）——两种既定策略无冲突。

### 3.6 OBS-6：性能数据（PF-01）
`speak()` 同步调用开销 **0.10ms**（含 cancel + 构造 utterance + 同步取 voice + synth.speak）；缓存命中 `getVoices()` **0ms**（二次调用）。选中朗读交互无可感知延迟。

---

## 4. 非缺陷现象清单（与 AGENTS.md 已确认决策一致，防未来误报）

1. **播放中再点喇叭/朗读 = 重读而非停止**：U-06、C-13、D-09 三处独立取证（cancel → speak 新文本，且首段播放中无 onend）。M6 复检确认项，勿当 bug 修。
2. **播放中离开页面/切词条朗读继续（不 cancel）**：OBS-3。M6 偏离 4 已确认。
3. **本机无 en-US voice 时默认朗读 `utterance.voice = null`、`lang = 'en-US'`**：pickVoice 回落 null 分支的设计兜底（B-06/U-06 双重取证）。「voice 为 null」不是缺陷。
4. **voiceschanged 迟到事件被忽略**：mock 测试中假事件晚于 1.5s 超时到达，`settled` 守卫正确忽略、空结果不缓存（U-04 行为的天然复现）。实现正确。
5. **未跟踪词条的 UI 删除不产生 git 提交**：R-05 实测 HEAD 不变（`git add -A notes` 后无净变更 → nothing to commit → console.warn 分支）。M7 复检 7 已确认语义。
6. **设置页 voice option 文本有尾随空格**（`{{ v.name }} ({{ v.lang }})` 模板换行折叠所致）：纯外观、比较均按 trim 处理，无功能影响，不阻塞。
7. **D-02 加载窗口极短**：本机 voices 同步就绪（Chrome 冷启动为空的环境才有 1.5s 等待），真实浏览器难以肉眼看到「加载语音列表…」；mock 环境已确定性取证该 UI 态存在且可切换。

---

## 5. 关键取证摘录（证据驱动，非推断）

| 用例 | 证据 |
|---|---|
| U-01 无 window 分支 | **module Web Worker** 内 import tts.ts：`isTtsSupported() === false`（真实非浏览器全局执行）；页面内分支 `=== true` |
| U-03 并发共享 | `getVoices()` 两次调用 `p1 === p2`（同一 Promise）；空表期间 100ms 仍 pending；事件后 resolve 3 个 voice |
| U-04 超时兜底 | 首调 1503ms resolve 空表；`removeEventListener('voiceschanged')` 被超时路径调用（spy 取证）；二次调用再等 1504ms（空表不缓存） |
| C-02 顶部钳制（M3 修复回归） | 选区完全滚出上缘（`selBottom = -54.2`）后工具条 `barTop = 8`（修复前该分支可为 -7） |
| C-05 首显不闪烁 | MutationObserver 记录：插入时 `left/top: -9999px` → 同帧内样式更新为实测坐标 |
| C-06 内部容器滚动 | 选区在 `pre` 内、`pre.scrollLeft = 200`（不冒泡）后工具条 left 599.8 → 399.8 并重新贴齐选区——**capture 监听生效的直接差分证据** |
| C-08 prevent 生效 | 真实拖选 + 真实点击后 `getSelection().toString()` 仍等于原选区（pointerdown 未折叠选区）；兜底路径：pointerdown（`defaultPrevented=true`、预存文本）→ 模拟折叠 → click → 朗读预存文本「M6 测试词条，含」 |
| C-12 监听清理 | 卸载时 removeEventListener 日志恰 7 条：NoteView 四监听（selectionchange/scroll/resize/pointerdown）+ 删除确认 pointerdown + MarkdownViewer scroll/resize |
| D-03 en-* 排前 | mock 语音库（en-US/en-GB/zh-CN×3）渲染顺序：默认（美音）→ en-US → en-GB → zh-CN×3 |
| D-04 重复 key | 两个同名 voice 正常渲染 2 个 option，console 零 Vue 警告（整体 warnings 为空数组） |
| D-08 非 en voice | 已保存 Huihui 后喇叭与选中朗读的 utterance 均为 `voice.name='Microsoft Huihui…'`、`lang='zh-CN'` |
| R-02 局域网 | 开：`host 0.0.0.0` + LAN IP 192.168.10.143 + 二维码；关：复原 `127.0.0.1` |
| R-05 删除闭环 | 两态确认 → 文件消失 → HEAD `1ea605a` 不变、零新提交 |

---

## 6. fixture / 进程清理与 git 状态确认

- **fixture 全部经文件系统 `rm` 清理**（从未使用 DELETE API 批量清理，避免 M3 运维注记所述 `git add -A notes` 卷入）：`t_m6_ipa.md`、`t_m6_无ipa.md`、`t_m6_长ipa.md`、`t_m6_missing_xyz.md`（R-04 stub 创建产物）、`t_m6_del.md`（R-05 专用，UI 删除）。
- **R-05 排序偏离说明**：任务书顺序为「回归 → 清理」，实际把 R-05 放在其余 fixture 清理之后单独执行——否则 UI 删除触发的 `git add -A notes` 会把当时仍存在的未跟踪 fixture 一并卷入提交历史（M3 测试运维注记的既定约束）。其余 R 项不受影响。
- 测试中 fixture 的内容增补（均属 fixture 自身完善，不触碰真实词条）：`t_m6_长ipa` 补超长 title（B-05）；`t_m6_ipa` 补 `[[t_m6_无ipa]]` 跨链接与长代码行（E-02/C-06）；`t_m6_无ipa` 补失效链接 `[[t_m6_missing_xyz]]` 与 `[[t_m6_长ipa]]`（C-14/R-04）。
- 进程：dev 服务已停（TaskStop 后 tsx/vite 子进程孤儿化，已按历史教训整树 `taskkill //T //F` 补杀，端口 3000/5173 复核为空闲）；playwright-core 临时目录与日志已删。
- **git 终态**：`git status --porcelain` 仅 `?? .zcode/`（与测试前一致）；HEAD 保持 `1ea605a` 无新提交；`notes/vocab/` 仅剩 6 条真实词条（abandon/boost/perseverance/runaway/serendipity/twinkle，均为用户数据未动）。
- 浏览器侧：`localStorage` 的 `en_tool:tts:voice` 已在用例后清除复原；本次会话注入的所有 spy/mock 均随页面生命周期消散。

---

## 7. 整体结论

**M6 功能面通过测试。**

55 项用例（P0×20 / P1×23 / P2×6 / 冒烟×6）全部通过，零失败、零阻塞、零豁免；TTS 链路（tts.ts 单元语义、喇叭、选中朗读工具条、设置卡片）、IPA 四处渲染、与 M7 的交互点、移动端与回归冒烟均以真实浏览器证据验证。用例之外发现缺陷 1 项：

> **DEF-01（P1）**：阅读页 `.ipa` 缺 `overflow-wrap: anywhere`，超长不可断 IPA token 在阅读页造成文档级横向滚动（M1/M3/M5 同族「折行不撑破」面的第 4 处遗漏）；关联 OBS-1（预览卡 `.wiki-preview-ipa` 同根因视觉溢出，P2）。

按测试边界未做任何代码改动，缺陷修复留给后续会话。
