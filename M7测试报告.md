# M7 功能面全维度测试报告（最后一个里程碑）

- **测试对象**：EN_tool M7 打磨（删除流程、git 自动提交、z-index 令牌与键盘可达、失效提示条、悬停预览）
- **测试日期**：2026-08-30
- **测试工程师视角**：只测试不实现；与已确认决策冲突的现象按任务书约定记录、不判缺陷
- **环境**：Windows 10.0.26200 x64 / Git Bash；`npm run dev`（server 127.0.0.1:3000 + vite localhost:5173，浏览器必须 localhost）；Playwright MCP Chromium（桌面 1280×900、移动 375×667）；curl / git 核对
- **启动前置**：发现前一会话遗留 dev 进程（server:3000 / vite:5173 两棵进程树）占端口，已整树终止后重启（记录见第 5 节）
- **工作区起始状态**：仅 `?? .zcode/` 未跟踪；真实词条未动

## 1. 执行概要（用例统计）

| 级别 | 通过 | 失败 | 阻塞 | 豁免 | 小计 |
|---|---|---|---|---|---|
| P0 | 16 | 1 | 0 | 0 | 17 |
| P1 | 16 | 1 | 0 | 0 | 17 |
| P2 | 2 | 0 | 0 | 0 | 2 |
| 冒烟 | 6 | 0 | 0 | 0 | 6 |
| **合计** | **40** | **2** | **0** | **0** | **42** |

- P0 失败：**A-06（DEF-01）**——删除在途点「编辑」后仍被拉回板块页，M7 复检 4 声称修复的场景实测未修复。
- P1 失败：**E-04（DEF-02）**——慢速/步进移动鼠标从链接进入预览卡时卡片消失（链接与卡片间 8px 间隙触发 mouseout）。
- P2 观察 7 项（见第 3 节）；非缺陷/与已确认决策一致现象 7 项（见第 4 节）。

## 2. 失败与缺陷清单

### DEF-01（P0，A-06）：删除请求在途时点「编辑」，DELETE resolve 后仍被拽回板块页

- **用例**：A-06 条件跳转（场景一：删除在途 → 点编辑跳走 → 不应被拽回；场景二：在阅读页删除 → 正常跳板块页）
- **复现**：POST 词条 → 阅读页（Playwright route 拦截 DELETE 挂起 1.5s）→ 点「删除」→「确认删除？」→ 250ms 后点「编辑」→ 进入 /v/vocab/t_m7_del/edit → 等待 2.2s（DELETE resolve）→ **URL 变为 /vocab，编辑页消失（textarea count=0）**。对照组（不拦截）：删除成功 → 正常跳 /vocab、GET 404，场景二通过。
- **期望**：停留编辑页（AGENTS.md「M7 复检修正 4」：doDelete 成功后仅当仍在该词条阅读页才 router.push 回板块页，「删除请求在途时用户点『编辑』跳走的话，不把人从别的页面拽回来」）
- **实际**：被拽回 /vocab（`urlAfterDeleteResolves = http://localhost:5173/vocab`）
- **初始根因（只判断不修复）**：`NoteView.vue` 第 203 行守卫只比较 `route.params.board === board && route.params.slug === slug`；编辑页路由 `/v/:board/:slug/edit`（`route.name === 'note-edit'`）的 params 与阅读页完全相同，守卫恒真，条件跳转退化为无条件 push。修复方向（未实施）：增加 `route.name === 'note'` 校验。
- **证据链**：4fc6620 提交 diff 即 params-only 守卫；实测时序 `urlAtEdit=/v/vocab/t_m7_del/edit → urlAfterDeleteResolves=/vocab`。

### DEF-02（P1，E-04）：慢速移动鼠标进预览卡时卡片消失

- **用例**：E-04 卡片交互（鼠标移入卡片保持显示）
- **复现**：桌面 1280×900 悬停 [[abandon]]（卡片出现在链接下方 8px 处，实测 link bottom=579.07、card top=587.07）→ 用 10 步「细致移动」把鼠标移入卡片中心 → **卡片消失（count=0）**；同一链路用单步快速移入 → 卡片保持（count=1）；移出卡片 → 隐藏（符合预期）；is-missing 链接悬停 → 不弹卡（符合预期）。
- **期望**：「移入预览卡：保持显示（鼠标可移到卡片上阅读）」（MarkdownViewer.vue onMouseOut 设计注释）
- **实际**：慢速移动时鼠标在链接与卡片之间的 8px 间隙上先触发 `mouseout`（relatedTarget=段落 P，非卡片）→ `hidePreview()` → 卡片在指针到达前消失。
- **初始根因（只判断不修复）**：`onMouseOut` 只对「移出目标（relatedTarget）恰好是预览卡/链接内部」放行；跨越间隙的中间元素不可见。快速单帧移动时 relatedTarget 直接为卡片故可通过。真实影响：trackpad/慢速移动（含高分辨率指针逐帧越过间隙）必然触发。
- **修复方向（未实施）**：对「指针目标进入预览卡矩形或与卡片/链接矩形连线」情形延迟/抑制隐藏（如离开链接后短窗口内若进入卡片则恢复），或增大判定为矩形相交。

## 3. P2 观察项清单（记录不阻塞）

1. **A-03 confirm 按钮挂起期 opacity 实测不均为 0.5**：光标停在按钮上时 `getComputedStyle(opacity)=0.9`（`.delete-btn.confirm:hover` 特异性 3 类 > `.delete-btn:disabled` 2 类，hover 优先）；移开光标 = 0.5（规格值）。disabled 语义完整（isDisabled=true、cursor:default、连点仅 1 次 DELETE），判 P2 视觉观察，非功能缺陷。
2. **M-03 聚焦缺失目标链接时下划线保持红色虚线**：`a.wiki-link:focus-visible` 的「实线下划线」规则在源码顺序上先于 `a.wiki-link.is-missing`（同特异性后者胜出）；实测聚焦 `vm_real_missing123` 时 borderBottom=dashed（红色）、但 outline 与 #eff6ff 底色正常（accent outline+底色=有效焦点指示）。与 M7 复检 6「实线下划线」字面略不一致；意图（焦点醒目）达成，P2 观察不判缺陷。
3. **A-08 斜杠 slug 的 404 文案为 Fastify 默认「页面不存在」**：`DELETE /api/notes/vocab/a/b`（三段路径）不匹配任何路由，未进入 `isSafeSlug` 校验器，返回 Fastify 路由级 404 而非自定义「词条不存在」；状态码契约 404 满足（含 git 未触发、文件系统未碰三态均验证）。
4. **「400×800 视口悬停不弹卡」根因 = Playwright 自动滚动悬停序列（over→scroll）**：scroll 事件按 M7 偏离 5 明确设计隐藏预览（沿箭头 onScrollHidePreview）；1280×500 复现（末链接在折叠下）同样成立，与视口宽度无关。真实用户滚轮滚动悬停同语义；不弹卡属设计行为（见第 4 节非缺陷 1）。
5. **编辑页离开确认对话框（M4 既有）出现在 A-05 链路中**：`confirm`「当前有未保存的修改，确定离开？」由 EditView 离开守卫弹出，非删除流程；删除全流程（A-12 专项，含失败+重试）零对话框。
6. **移动端真实 tap 通常看不到预览卡**：tap 合成 mouseover 后约 100-200ms 即 click 导航（先于 300ms 防抖），卡片来不及显示；合成 hover 验证弹卡与钳制正常（M-02 通过），悬停预览为桌面特性、移动端交互设计未定义。M-02 按「tap 合成 hover 弹卡且 clamp 视口内」验证：卡 [107,253]-[367,396] 全部落在 375×667 内。
7. **B-04b 的 console.warn 不含 git stderr 细节**：`gitCommit.ts` 记录 `commitErr.message`（仅 "Command failed: git commit …"），注入 pre-commit hook 失败时日志看不到 "pre-commit hook declined" 字样（靠因果对照确认 hook 生效：hook 在场失败、移除后同一删除手动提交成功）；可诊断性小缺点。

## 4. 非缺陷现象清单（与 AGENTS.md 已确认决策一致）

1. **滚动即隐藏预览卡/取消在途预览**：M7 偏离 5「预览新增 scroll 隐藏（capture 监听）…直接隐藏，下次 mouseover 重建」——解释了观察 4 的自动滚动悬停不弹卡（含 D-04 场景一：拦截 search-index 延迟 + 悬停后移出 → 卡不弹；驻留 → 卡弹，两项均通过）。
2. **未跟踪词条删除产生 console.warn 而非提交**：`git add -A notes` 无净变更 → `nothing to commit` → 「git commit 失败（文件已删除，仅未提交）」（M7 复检 7 行为澄清）；A-01~A-12 各次未跟踪删除均如此，B-02 专项验证 HEAD 不变 + warn 共存。
3. **桌面 `.az-rail` computed z-index=auto**：`position: sticky + z-index: var(--z-rail)` 仅在移动端媒体查询内；桌面 rail 为流内非定位元素，无层叠上下文需求。令牌值链（20/30/40/50）以移动端 sticky 场景计算值验证齐全。
4. **5000 字符缺失目标在提示条内多行折行**（桌面 621==621、移动 253==253 内零横滚，文档 scrollW==clientW 双宽度 0 横滚）：overflow-wrap:anywhere（M5 DEF-02 修复）生效，符合「折行不撑破」。
5. **摘要代码行行为**：`t_m7_hover_code`（标题后紧跟代码围栏）卡摘要为「js」（``` 围栏行被选中并粗剥反引号）——与 M7 偏离 4「第一个非空、非 # 标题行、行级处理」已确认决策一致；任务书 E-03 的「代码行跳过」与其冲突，按任务书约定停止该子断言、记录不判缺陷。（空行跳过 ✓；首行 # 跳过 ✓；>120 截断加 …✓（120+…=121 字符）；无可用行不渲染 ✓（t_m7_hover_ex 卡无 excerpt 行）。）
6. **git add -A notes 卷入未跟踪 fixture**：M7 偏离 8 既定语义——PF-01 删除时同时把 D/E 节的未跟踪 fixture 一并提交进 `35598d3 note: 删除 vocab/t_m7_sub_big`（属测试排布失误——PF-01 应在 D/E fixture 清理后执行，非产品缺陷；已通过 e94ca52 清理提交复原工作树）。
7. **A-04 搜索「未找到匹配」同会话生效**：删除后 SPA 切首页即无命中（invalidateSearchIndex 生效，无需整页刷新）；boards 计数 7→6、list/search-index 同步、文件系统消失四态一致。

## 5. fixture / PATH / 进程清理与 git 状态确认

- **fixture**：全部 `t_m7_*`（t_m7_del/del_tracked/del_untracked/addfail/commitfail/async/nogit/sub/sub_big/d1/d2/hover/hover_code/hover_ex/ipa）已按纪律移除——**批量清理走文件系统 rm；UI/API 删除用例按 A 节单 fixture 清洁态执行**；`.zcode/` 下测试用临时 json（tmp_a11.json、tmp_d1.json）已删除。
- **git 状态**：工作树干净，仅剩会话开始时即存在的 `?? .zcode/`（工具目录，未动未清）；真实词条（abandon/boost/perseverance/runaway/serendipity/twinkle 及 phrase/sentence/grammar）内容零改动。boards 计数复原（vocab 6 / phrase 1 / sentence 1 / grammar 1）。
- **git 历史新增提交**（机制验证需要的产物，均属自动化提交语义）：
  - `c4bed75`（temp 入库 B-01）→ `69036c7`（note: 删除 vocab/t_m7_del_tracked）
  - `b33219d`（temp 入库 B-04b）→ `5f86dd1`（测试补提交，注入失败后的 staged 删除）
  - `d44a9ef`（temp 入库 B-05）→ `2caa07c`（note: 删除 vocab/t_m7_del_async）
  - `35598d3`（note: 删除 vocab/t_m7_sub_big；git add -A 卷入 D/E 未跟踪 fixture——见非缺陷 6）
  - `e94ca52`（test: 清理 M7 fixture——最终文件系统移除的提交）
- **PATH 复原**：B-03 结束后每次命令均为用户 profile PATH；已复核 `which git` = /mingw64/bin/git。
- **进程**：会话初始发现并终止了前一会话遗留 dev 进程两棵（tsx+vite）；本会话 dev 服务（含 PE-01 期间两次重启、B-03 的 no-git 服务端）均已停止；3000/5173 端口无残留监听。Playwright 浏览器已关闭。
- **测试运维失误注记**（防后续会话重犯）：① PF-01（子目录大词条删除）执行时 D/E fixture 仍存在未跟踪，**API 删除会触发 git add -A 全量卷扫**——后续凡是「带其他未跟踪词条时的删除用例」须先清场；② B-03 用 `npm` 直接以剥离 PATH 启动会因缺 sh 找不到 npm 脚本，改用 node 直启 tsx loader 成功（过程记录供参考）。

## 6. 整体结论

**M7 功能面 42 项用例：P0 16/17、P1 16/17、P2 2/2、冒烟 6/6 —— 发现 2 个真实缺陷，未达「全通过」。**

- **DEF-01（P0，必须修复）**：A-06 条件跳转失效——删除在途点「编辑」仍被拽回板块页；M7 复检 4 的守卫（params-only）无法区分阅读页与编辑页（两路由 params 相同），已登记带完整复现与根因。
- **DEF-02（P1，建议修复）**：E-04 慢速/步进移入预览卡时卡片消失——链接与卡片间 8px 间隙触发 mouseout→hidePreview，trackpad 用户真实受影响；快速单帧移动可保持。
- 其余各节（删除流程本体 11/12、git 自动提交 6/6、视觉 focus/z-index 4/4、修复项回归 5/5、悬停预览本体 3/4、移动端 3/3、性能/健壮性 2/2、全里程碑冒烟 6/6）证据齐备、通过。
- 与 AGENTS.md 已确认决策无「停止并报告」级冲突；两处轻微不一致（聚焦缺失链接下划线保持虚线、E-03 代码行摘要）按任务书约定列为 P2 观察/记录不判缺陷。
