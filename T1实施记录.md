# T1 实施记录 · v1.1 标签实体化（精简版）

> 状态：**已定稿（T1 + 复检 + 8 轮体验迭代全部完成，2026-09-02）**。设计权威 = 根目录 `v1.1标签实体化设计方案.md`。本记录为 2026-09-02 精简整理版：保留核心要点与决策，逐轮过程细节以提交历史、`T1截图/` 与 git diff 为准。

## 定稿形态

- **服务端**（首个服务端变更）：`data/tags.json` 注册表（仓库根、notes/ 外，纳入 git）+ `GET/POST /api/tags`（upsert，无删除/重命名）；原子写（同目录 tmp + renameSync，**先写盘后改内存**）；启动读盘损坏/缺失 → 空表 + 告警不拒启；两端点注册于 `registerRoutes()`（LAN 切换重注册实测存活）。校验：tag trim 非空 ≤32、color 0–7 整数、body 须对象；无 409 分支。
- **前端**：`lib/tagRegistry.ts` = Promise 缓存 + `shallowRef` 响应式镜像（未就绪 `{}` 回落 djb2，就绪自动重上色）+ `upsertTag` 序列号守卫（过期响应丢弃，防乱序回退）+ 跨页签/回焦同步（BroadcastChannel + focus/visibility 5s 节流重拉，`invalidateTagRegistry` 供其使用）；`lib/tagColor.ts` 取色 = **注册表 > djb2 回落**（djb2 算法零改动，查询键 NFC 规范化）；`main.ts` 启动预载（失败静默可重试）。
- **标签页色卡墙**：union(笔记聚合, 注册表) 全量标签一卡一位。
  - 卡体全染（`.tag-pair-N` 洗底 + 文字色经继承）+ `--card-sheen` 渐层（浅 = 165° 对角淡出向 surface；深 = 左上 `--card-hue` 17% 辉光 + 洗底×文字色 color-mix 25/75 增饱和层——深色防发黑定稿解法，零新颜色令牌）。
  - 两段式卡头：名称左 · 计数或「未使用」徽标右（「自动」徽标已删）。
  - surface 白托盘色板：色点主题感知（浅 `-bg` 粉彩 / 深 `-c` 亮档）、`pulse` 点击脉冲（`--duration-pulse` 600ms，无常亮环）、`space-between` 均分。
  - ghost 虚线卡承担创建（upsert，成功清筛选收起，Enter/Esc）；`min-height: 81px` 与标签卡自然高度对齐（卡面结构变更需同步）。
  - 页头合并排序 seg（名称/常用，`en_tool:tag-sort` localStorage）+ 筛选框；整卡 stretched-link 进详情；入场 stagger（stagger-arm 门控）。
- **标签详情页**：页头取色行（同款色板，ring 模式）；其余页面经 `TagBadge` 全站跟随注册表色。
- **口径**：板块页 chips 仅词条携带标签（不含 count=0），勿接 tagRegistryRef；颜色仅 0–7 色板索引；零新依赖。

## 迭代演进史（八轮 + 修补）

| 轮 | 触发 | 决定 | 提交 |
|---|---|---|---|
| T1 | 立项实施（首次服务端变更） | 注册表 + 两端点 + 色卡墙初版 | feat `1c62aea` / docs `4b44bf1` |
| 复检 | 流程 | 全量复检零缺陷 | docs `2949eaf` |
| ① | 展开式改色「未生效」（取证：机制正常，病灶 = 色板视口外 + 空注册表区块不渲染） | 「自定义颜色」常显色板行 + 详情页取色行 | feat `413d0e9` / docs `f3a3f9d` |
| ② | 三痛点：找不到 / 排版膨胀 / 鸡生蛋（只列已调色标签） | 三方案拍板方案 A：色卡墙整页重构 + 全染 | feat `f3b1ceb` / docs `801b0e0` |
| ③ | 排布待优化 + 色板模糊 + 常亮环噪音 | 白托盘 + 两段式卡头 + pulse（`--duration-pulse` 600ms）+ 排序/筛选入页头 | feat `4fd6b21` / docs `bc7e11b` |
| ④ | 自定义排序名实不符 / 自动徽标冗余 / 深色发黑 | 中性色卡（彩条 + 色名 + 饱和点），删「自定义」排序档 | feat `342acf6` / docs `6816215` |
| ⑤ | 否决中性色卡（饱和过高 / 与标签形式脱节 / 难看） | 回退方案 A 原全染 | revert `57774aa` / docs `0b68535` |
| ⑥ | 附③截图指名恢复该排布（无自动徽标） | 恢复两段式卡头 + 白托盘 | feat `a838f0e` / docs `f6c3730` |
| ⑦ | 全染加渐变；深色保持全染下防发黑 | `--card-sheen`：浅 165° 对角淡出 / 深辉光 17% + 增饱和 25%（color-mix） | feat `992c9dd` / docs `a4ba896` |
| 修补 | 第四行卡比前三行高 | 幽灵卡 min-height 104→81（对齐标签卡自然高度） | fix `5704268` / docs `2cf0020` |
| ⑧ | 深色色点也发黑 | 主题感知色点：浅 `-bg` 粉彩 / 深 `-c` 亮档 | feat `c0c4052` / docs `3c8066d` |

**被否决勿再提**：独立改色区块、点击展开式色板、双表面重复展示、中性色卡（彩条/色名着色/饱和色点 `.tag-dot-N` 旧版）、迷你徽章色板、单主题填充（饱和点或洗底点单轨）。

## 偏离与理由（留档）

1. upsert **先写盘后改内存**：写失败内存与磁盘保持一致（内存恒为磁盘镜像）。
2. upsert 成功用响应直接 `applyTagRegistry` 生效，不 invalidate+重拉。
3. 「已存在」判定域 = union（笔记聚合 ∪ 注册表），避免笔记已有标签首次调色误报「已创建」。
4. 深色防发黑 = color-mix(洗底×文字色) 增饱和 + 角落辉光，而非饱和色系或中性卡（均被否决）；`color-mix` 需 2023+ 现代浏览器，不支持时优雅降级为平色洗底。
5. 色点主题双轨（浅 `-bg` / 深 `-c`）：单主题填充在另一主题必出问题（④饱和点浅色刺眼、⑤洗底点深色发黑）。
6. 幽灵卡 `min-height: 81px` 与标签卡自然高度对齐（卡面结构变更需同步此值）。
7. 排序 seg 无「自定义」档（名实不符已删）；「自动」徽标已删（当前色由卡面自表达）。

## 验证矩阵（累计结果）

| 项 | 结果 |
|---|---|
| `npm run build`（server tsc + vue-tsc + vite） | ✅ 每轮全过 |
| API 契约：合法 / 非法 8 连（缺 tag、color=8/"2"/3.5、空白、超 32、字符串/数组 body）/ upsert 仅改色 | ✅ 全过 |
| created 跨日期保留（植入 2025-01-01 实测） | ✅ |
| 并发 10 连 POST 一致性（同步串行、后写赢） | ✅ |
| 持久化重启 / 损坏兜底（坏 JSON → 空表+告警）/ watcher 零噪音 / LAN 切换回归 | ✅ |
| 前端取色三处一致（云/列表/阅读页）+ hash 回落（djb2 预计算核对） | ✅ |
| 标签页交互：创建 / 改色 / 「已存在，已更新颜色」/ 边界拦截 / Enter/Esc | ✅ |
| XSS（HTML 形标签名纯文本渲染） | ✅ |
| 12 页 × 浅深 24 项零横滚 + 移动端 375 | ✅ 每轮复测 |
| preview 4173 生产构建复核 | ✅ |
| 真实点击路径（cua）交互链 | ✅ |

## 审查

- **复检（2026-09-01）**：静态走查（feat diff 范围、零改动清单、invalidateSearchIndex 调用点、铁律 grep）+ API 边界强化（32/33、color 0/7/-1、trim、特殊字符往返、并发 10 连、跨日期 created 保留）+ 前端复测——**零缺陷零修复**。
- **全面审查（2026-09-02）**：基于当前代码复查，无阻断级新 bug；发现 8 项隐患/边界待用户拍板（见下）。

### 审查发现（2026-09-02 报告 → 同日按严重度修复，B1 关闭为既定语义、B7/B8 知悉）

| # | 级别 | 发现 | 处理结果 |
|---|---|---|---|
| B1 | 中 | 全局搜索 / Ctrl+K / 板块 chips 找不到「仅存于注册表」的标签（count=0） | **关闭（用户决策）**：未使用标签无内容可检索，当前实现即该语义；被词条使用后自然进入索引 |
| B2 | 低 | `applyTagRegistry` 后，更早发出的预载 GET 返回旧态会覆盖 ref | **已修**：mutationSeq 守卫——GET 发起记录快照，期间发生过 upsert 则响应只回填缓存不覆盖 ref |
| B3 | 低 | 无跨页签/跨设备实时同步：他端改色需刷新可见 | **已修（轻量方案）**：BroadcastChannel 页签互通知重拉 + 回到页签（focus/visibility，5s 节流）重拉兜底；`invalidateTagRegistry` 获得调用方（B5 就地解决）；实测静默改色 → 页面保持旧值 → focus 后自动更新 |
| B4 | 低 | 大小写与 Unicode 不归一（CET6 ≠ cet6、NFC ≠ NFD），服务端仅 trim | **已修（NFC）**：server upsert 键 NFC 规范化（规范等价、视觉零变化，实测 NFD 输入存为 NFC 键）+ 前端 `tagColorIndex` 查询键 NFC；**大小写保持敏感**（与笔记标签语义一致，静默小写会破坏颜色绑定），列为既定语义 |
| B5 | 极低 | `invalidateTagRegistry` 为死导出 | **已解**：B3 同步机制使其获得调用方，保留 |
| B6 | 极低 | `recoloring` 全局锁：跨卡快速连点第二次被忽略 | **已修**：per-tag 锁（跨卡可并发）+ lib 序列号守卫（同时消除并发响应乱序回退的次生隐患） |
| B7 | 知悉 | /api/tags 在 LAN 模式无鉴权可写——与既有全部端点一致，非新增暴露面 | — |
| B8 | 知悉 | tags.json 手工编辑需停服（模块级内存）；写中 kill 有 tmp 残留风险（窗口极小） | — |

## 已知限制

1. 无删除/重命名端点：错误条目唯一清理途径 = 停服手工编辑 `data/tags.json`。
2. 多端并发改色后写赢（单用户既定语义）。
3. 「未使用」标签详情页为空列表（TagDetail 既有行为）。
4. count=0 标签字号恒为 0.85rem 下限（fontSize 线性加权在零计数域的退化）。
5. IAB 验证 quirk：遮挡态 locator click 超时 → evaluate 派发 click；fullPage 截图伪影 → 交付用视口截图。
6. `color-mix` 需 2023+ 现代浏览器，不支持时优雅降级为平色洗底。
7. 多端/多页签不同步（见审查 B3）；大小写与 Unicode 不归一（见 B4）。
8. 板块页顶部标签 chips 中单枚超长标签（32 字上限）在窄视口自身超宽会溢出——**既有问题**（chips 聚合本就展示全部标签，与本次列表行展开无关），本次仅修复列表行内徽章；chips 侧修复待议。

## 附：列表行标签溢出展开（独立小功能，用户拍板方案二）

**触发**：用户为 abandon 添加第 4 个标签「雅思」后发现列表行不显示——根因 = `NoteList.vue` 行内徽章 `note.tags.slice(0, 3)`（v1 紧凑性决策，标签≤3 时从未暴露）；阅读页/板块 chips 均为全量，非实体化引入。

**拍板**：三套方案（悬浮卡片 / 行内展开 / 行级速览条，另链接基线）中选**行内展开**；用户追问展开是否会横向溢出——实测证实桌面端会（`.row-side` nowrap + shrink:0，模拟展开 scrollWidth 815 > 670），随后实施换行补丁根除。

**实施（NoteList.vue）**：前 3 徽章照旧；超 3 时渲染 `.tags-toggle` 中性 ghost 胶囊（`+N`/`收起`，aria-expanded，`@click.stop` + `position:relative` 浮于拉伸层，点击不触发整行导航）；展开 = `.note-row.tags-expanded` 启用 `flex-wrap`（承接溢出徽章换行，桌面亦生效）+ 剩余徽章接龙。展开状态为组件内 Set（key = board/slug），筛选重建列表时复位（可接受）。

**压力测试发现两层溢出并修复**：① 桌面多徽章超宽（见上，wrap 补丁根除）；② 移动端单枚 32 字徽章自身宽于视口（约 436px > 375px，`white-space: nowrap` 不可折断）——移动端媒体查询内补 `.row-side :deep(.tag-badge) { max-width: 100%; white-space: normal; overflow-wrap: anywhere }`（短标签单行照旧，长标签胶囊内折行；亦修复既有「前 3 徽章含长标签时移动端溢出」问题）。

**验证**：压测临时词条（6 标签含 32 字长名）折叠 3 徽章 + `+3`、展开 6 徽章换行、row/page 零溢出（桌面+移动端双态实测，行高 81→118/160px）；abandon 折叠 `+1` → 展开显示雅思；浅深截图 `27`/`28`/移动端 `29`；12 页 × 浅深零横滚；preview 4173 复核（abandon `+1` 在案）；`npm run build` 全过。临时词条已删。

## 独立审查（第三轮，2026-09-02，审查会话）

对已提交状态（HEAD = ac10b29）独立复验，**结论：通过验收，零代码缺陷**。本轮为 rAF 健康环境，全程 API 实测 + 浏览器断言：

- [x] **提交隔离与禁区**：T1 区间（e99e76e..HEAD）27 提交 46 文件 +1259/-64；server 侧仅 routes.ts + 新建 tagRegistry.ts（scanner/settings/gitCommit/index 零改动）；前端 lib 五件套与 CommandPalette/NoteView/MarkdownViewer/Skeleton/FloatingActions 等零触碰；notes/ 提交区间零变更（工作区 abandon.md/tags.json 未提交改动为用户实测数据，审查全程未触碰、仅快照还原）。
- [x] **代码核对**：tagRegistry.ts（模块级加载、损坏/缺失→空表+告警不拒启、原子写 tmp+renameSync、upsert 先写盘后改内存、键 NFC/大小写敏感、created 保留）；routes.ts（两端点在 registerRoutes 内 + 全套 400 校验）；lib/tagRegistry.ts（Promise 缓存 + shallowRef 镜像 + mutationSeq 双守卫 + BroadcastChannel + focus/visibility 5s 节流）；tagColor.ts（注册表 > djb2、查询键 NFC）；Tags.vue 色卡墙 + ghost 卡 upsert；NoteList +N 展开。设计方案 §3 已由实施会话更新为色卡墙定稿形态、§5 待补完成标注（本审查补记后由收官提交处理）。
- [x] **独立实测（API 直测 + 浏览器）**：
  - **契约矩阵 8/8**：新增 200、upsert 改色保 created、color=8/字符串/缺 tag/空白/超 32 全 400（错误文案明确）；
  - **持久化**：杀进程树重启后注册表完整（含用户数据 名词性从句=4、雅思=1）、零告警；
  - **损坏兜底**：手工写入坏 JSON → 服务正常启动 + GET `{}` + 日志精确输出「标签注册表加载失败（已按空表兜底）」告警 → 还原；
  - **LAN 切换存活**：lanEnabled true→false 往返，`GET /api/tags` 全程 200（registerRoutes 重注册语义实证）；
  - **取色三层**：cet6 徽章 = tag-pair-7 与页面内 djb2 计算一致（hash 回落）；雅思徽章 = tag-pair-1（注册表）而 hash 应为 7（**注册表 > hash 活体实证**）；浅色下雅思卡 #f0fdfa 命中 `--tag-1-bg` 浅档（主题双轨正确）；
  - **标签页**：色卡墙 10 卡 = union(8 聚合 + 2 注册表)、每卡 8 色点、ghost 卡在位；`__t1_unused__` POST 后出现「未使用」徽标卡；点色即改（名词性从句 4→3，API 确认持久）后还原；**审查操作失误致该标签一度变 color 0（幽灵卡表单默认选中色），已即时还原为 4**——顺带实证了 upsert-on-existing 全链路（表单提交→改色→收起）；
  - **chips 口径**：/vocab 6 枚 chips 不含 `__t1_unused__`（count=0 排除实证）；雅思因 abandon 携带而进入 chips（union 活体）；
  - **列表 +N**：abandon 4 标签折叠 3 + `+1` → 点击展开 4 徽章 + tags-expanded 换行类 + 「收起」。
- [x] **残留清理**：审查测试条目 `__t1_unused__` 已除（停服 → 快照还原 → 重启确认）；`data/tags.json` 工作区回到用户未提交数据原样；审查标签页已关闭。
- [备注] 「已存在，已更新颜色」反馈文案未捕获（审查探针选择器未命中瞬态提示），但 upsert-on-existing 行为链路已行为级实证；`npm run build` 产物 index-CE5Zk79t.css / index-DJKrZ-ok.js。
