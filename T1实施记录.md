# T1 实施记录 · v1.1 标签实体化

> 状态：实施中（2026-09-01）。设计权威 = 根目录 `v1.1标签实体化设计方案.md`。本记录边做边记，结构：执行计划 → 偏离与理由 → 验证矩阵 → 已知限制。

## 执行计划（动手前定稿，精确到文件 × 改动点）

### S. 服务端（2 文件，其中 1 新建）

1. **新建 `server/src/tagRegistry.ts`**
   - `REGISTRY_PATH = path.resolve(import.meta.dirname, '../../data/tags.json')`（仓库根 `data/`，notes/ 之外；与 scanner 的 `NOTES_DIR` 同款定位模式）。
   - 类型 `TagRegistry = Record<string, { color: number; created: string }>`。
   - 模块级加载（import 时即读盘，routes.ts 引入即生效，**index.ts 零改动**）：存在则 `JSON.parse` 载入内存；缺失/解析失败/非对象 → 空表 + `console.warn` 告警，**不抛错不拒启**。
   - `getTagRegistry()`：返回当前内存对象（GET 响应直接序列化）。
   - `upsertTag(tag, color)`：**先原子写、后改内存**——构造 next 副本（已存在仅换 color、保留 created；不存在补 `created = dateOfLocal(new Date())`，复用 scanner.ts 封装勿另写日期格式化）→ `mkdirSync(data, { recursive: true })` → 写 `tags.json.tmp` → `fs.renameSync(tmp, 目标)`；写盘抛错则向上传（路由转 500），**内存不更新**（内存恒与磁盘一致）；成功后才 `registry = next`。
2. **`server/src/routes.ts`：`registerRoutes()` 内追加 2 端点**（LAN 切换重建实例后随函数重注册）
   - `GET /api/tags` → 200 全量注册表对象。
   - `POST /api/tags`：**手写校验**（理由：与文件内既有 9 端点的手写校验风格与中文错误消息统一，schema 收益低）——body 非对象/数组 → 400；`tag` 非字符串或 trim 空 → 400；trim 后 >32 字符 → 400；`color` 非 0–7 整数（`typeof === 'number' && Number.isInteger && 0..7`，天然拒 `"2"`/`8`/`3.5`）→ 400；`upsertTag` 抛错 → 500（error 日志）；成功 200 → 全量注册表对象。
   - 其余 9 端点、scanner、settings、gitCommit **零改动**。

### F. 前端（7 文件，其中 2 新建）

3. **`web/src/api.ts`**：新增 `TagRegistryEntry`/`TagRegistry` 类型 + `api.tags()`（GET）与 `api.upsertTag(tag, color)`（POST）两个方法。
4. **新建 `web/src/lib/tagRegistry.ts`**（照 `getSearchIndex` Promise 缓存模式）
   - `tagRegistryRef = shallowRef<TagRegistry>({})`：响应式镜像，未就绪为 `{}`；就绪/失效重载/upsert 后整体替换 `.value`，**依赖它的 computed 自动重算**（MarkdownViewer linkVersion 同款响应式修正思路）。
   - `getTagRegistry()` Promise 缓存（失败清缓存允许重试）+ `invalidateTagRegistry()` + `applyTagRegistry(reg)`（upsert 响应直接生效：更新 ref 并把缓存置为已解决的 Promise，**省一次 GET**）。
5. **`web/src/lib/tagColor.ts`**：djb2 `tagPairIndex` **逐字符零改动**；新增 `tagColorIndex(tag)` = 注册表命中取 `entry.color`、未命中回落 `tagPairIndex`（覆盖层语义；读 shallowRef，在 computed 中使用即获响应式）。
6. **`web/src/main.ts`**：`void getTagRegistry().catch(静默)` 启动预载（类比 theme.ts 模块级初始化；失败不阻塞应用，各消费方回落 hash）。
7. **`web/src/components/TagBadge.vue`**：`tagPairIndex` → `tagColorIndex`（唯一逻辑改动；列表/阅读页微章全站跟随）。*补充：经 grep 全站消费方仅 TagBadge 与 Tags.vue 两处。*
8. **新建 `web/src/components/TagPalette.vue`**：8 色板复用组件（新增区块与改色展开两处共用）。**结构 = button(透明) > span.swatch-dot(tag-pair-N)**——scoped 类特异性（0,2,0）压过全局 `.tag-pair-N`（0,1,0），故 dot 自身不得设 background/color（M2'「基类不得自带底色」铁律的按钮变体）；选中环走 button `box-shadow`（不触底色）。
9. **`web/src/views/Tags.vue`**（重做 script + 扩模板）
   - 数据：`aggregateTags()`（既有）+ `tagRegistryRef`；`unionTags` computed = Map 合并（聚合 count 优先，注册表独有补 count=0），沿用「count 降序、同数标签升序」排序（count=0 自然沉底）。
   - ① 顶部新增区块：名称输入 + `TagPalette` + 提交按钮；前端先拦（trim 空 / >32 字符）；成功 `api.upsertTag` → `applyTagRegistry(响应)`；**存在性判定在 POST 前对 union 取**：union 已含 → 「已存在，已更新颜色」，否则「已创建」（无 409 分支）；非法/失败内联反馈。
   - ② 云标签 = union 渲染：全部保持 RouterLink（导航习惯零破坏）；取色走 `tagColorIndex`；count=0 挂「未使用」徽标 + title 说明；字号沿用既有 fontSize（0 → 0.85rem 下限自然成立）。
   - ③ 改色入口 = 云标签下方独立「注册表标签」区块：每个注册表标签一枚 chip（button），点击展开 `TagPalette`，点 swatch 即时 POST → `applyTagRegistry` → 收起。（决策：不入云内联展开——云内 chip 是 RouterLink，嵌套交互元素/点击语义冲突，且基线对齐 flex 内做浮层需绝对定位+钳制，复杂度不值；独立区块点击语义唯一、文档流展开、移动端友好。）
   - 空态调整：新增区块恒显示；仅云区在 union 空时显示 EmptyState（文案补「可在上方直接创建」）。
10. **`web/src/views/Board.vue`**：`boardTags` computed 仅加**语义注释**（chips 口径 = 笔记携带标签，不含 count=0 注册表标签），逻辑零改动。
11. **UI 纪律**：全部令牌（8 色板用既有 `--tag-{0..7}-bg/c` 经 `.tag-pair-N`、字号 `--text-*`、间距 `--space-*`）；**不新增动效规则**（色板展开用 v-if 即时显隐，无新增动画则无 no-preference 块需求）；无 hex/rgba 散写、无 ms 字面值、断点字面量、零新依赖、`prefers-color-scheme` 零分支。

### 验证与交付（提示词 12 项全跑）

- `npm run build` → curl API 契约矩阵（中文 body 用 `--data-binary @file` 规避 Git Bash 坑）→ 重启持久化 → 原子写走查（+可测则实测）→ 坏 JSON 兜底 → watcher/git 零噪音 → LAN 切换回归 → browser-use 前端取色/交互（IAB 节流预案备好；**不用 Playwright**）→ 12 页×浅深走查 → preview 4173 复核（先清孤儿进程）。
- 交付：feat（代码 + 清理后的 `data/tags.json`）+ docs（本记录、T1截图/、设计方案 §5 标注、AGENTS 三条改写+状态）；测试条目提交前停服清理 JSON。

## 偏离与理由

1. **`upsertTag` 采用「先写盘、后改内存」顺序**（提示词表述为「内存更新 + 原子写」未规定顺序）：若先改内存后写盘，写盘失败时内存与磁盘不一致（GET 报告了未持久化的颜色）。先写盘成功后才提交内存，失败路由转 500、内存不变，内存恒与磁盘一致。
2. **注册表服务端模块级加载**（import tagRegistry.ts 时即读盘），index.ts 零改动；损坏/缺失兜底不抛错。
3. **TagPalette 色点加了 hover scale(1.1)/active scale(0.98) 微反馈**（计划原文写「不新增动效规则」）：色板展开仍为 v-if 即时显隐（无动画），但 swatch 是全新可点控件，零反馈手感突兀，按 M3' 铁律连同规则包进 `no-preference` 块，与全站按压反馈语言一致。
4. **`fontSize` 加零计数防护**（`Math.max(...counts, 1)` + `Math.max(0, count)`）：既有函数在「云标签仅剩 count=0 注册表标签」的零计数域下会除零产生 `NaNrem`，属 count=0 引入的新域，必须防护（语义不变）。
5. **POST 成功后用响应直接 `applyTagRegistry` 生效**（不调 invalidate+重 GET）：响应即全量注册表，省一次网络往返；缓存同步置为已解决 Promise，后续 `getTagRegistry()` 命中。
6. **「已存在」判定域 = union（笔记聚合 ∪ 注册表）**：方案只说「已存在标签」，若仅查注册表，则「笔记里有但注册表没有」的标签首次指定颜色会误报「已创建」，与用户感知不符。

## 验证矩阵

| # | 验证项 | 结果 |
|---|---|---|
| 1 | `npm run build`（server tsc + web vue-tsc + vite） | ✅ 通过（vite 8.2.2，178 modules，built in 1.64s） |
| 2 | API 契约：GET 空 `{}` | ✅（首次启动 data/tags.json 不存在 → `{}` HTTP 200） |
| 2 | API 契约：POST 合法 `{tag:"机器学习",color:4}` | ✅ 200 + 全量 `{"机器学习":{"color":4,"created":"2026-09-01"}}` |
| 2 | API 契约：非法 8 连（缺 tag / color=8 / color="2" / color=3.5 / tag 空白 / tag 超 32 / body 字符串 / body 数组） | ✅ 全部 400，错误消息中文精确 |
| 2 | API 契约：已存在标签 POST 仅改色 | ✅ color 4→6，`created` 保持 2026-09-01 不变 |
| 3 | 持久化：kill 进程树（taskkill //T）→ 重启 dev | ✅ GET 仍返回 `机器学习` color=6；data/tags.json 内容正确 |
| 4 | 原子写 | ✅ 代码走查：`mkdirSync(recursive)` → 写 `tags.json.tmp` → `renameSync` 覆盖目标（Windows rename 语义覆盖已存在文件）；写盘成功才提交内存。kill 中途实测：IAB 环境无法安全模拟写中 kill，以走查为准（见已知限制） |
| 5 | 损坏兜底：tags.json 写入 `{{{ not json }}}` → 重启 | ✅ 服务正常启动、GET `{}`、日志告警「标签注册表加载失败（已按空表兜底）…Expected property name」；随后恢复正确内容重启正常 |
| 6 | watcher/git 零噪音：POST 改色后 | ✅ data/ 与 notes/ 平级；改色后 `/api/boards` 计数 6/1/1/1 不变；服务日志「已扫描」仅启动 1 次；git status 中 `data/` 独立未跟踪 |
| 7 | LAN 切换回归：开 LAN → 测端点+改色 → 关 LAN | ✅ 切换期间 GET/POST /api/tags 均正常（registerRoutes 重注册语义），关闭后仍正常 |
| 8 | 前端取色：三处一致 + 回落 | ✅ 云标签/词汇板 TagBadge/阅读页 TagBadge：`t1临时哈希标签`（未注册）= tag-pair-5（=djb2 预计算值），`机器学习`（注册表 color 6）= tag-pair-6（hash 本应为 3，覆盖层生效）；浅深双主题色值均正确（浅 `#fffbeb`/深 `rgba` 变体等逐一核对） |
| 9 | 标签页交互 | ✅ 创建→立即显示+「未使用」标记；改色→即时生效（chip/cloud/服务端三端同步）；空白输入拦截「标签名不能为空白」；聚合已有标签与注册表已有标签提交均反馈「已存在，已更新颜色」；板块页 chips 不含 count=0 注册表标签（含临时词条携带标签） |
| 10 | 走查矩阵：12 页 × 浅/深 零横滚 | ✅ 24/24（scrollWidth=clientWidth=1425 全过）；移动端 375×812 标签页零横滚、create-row 正常换行 |
| 11 | browser-use（IAB） | ✅ 全程 browser-use；rAF 探针健康（41–43 帧/250ms）；中途 2 次 locator click 超时（hasFocus=false 遮挡态下 actionability 检查停滞），按预案改 evaluate 派发 DOM click 后通过 |
| 12 | preview 4173 生产构建复核 | ✅ 预案清杀 4173 孤儿进程（PID 11636）→ `vite preview`（继承 server.proxy，/api 200）→ 浏览器实测生产构建标签页：新增区块 8 swatches、union 云标签「未使用」×2、注册表覆盖色 tag-pair-6、注册表区块 3 chips 全部正确 |

### 截图交付（T1截图/，6 张）

1. `01-新增区块与云标签union-浅色.png`——新增区块 + 云标签 union + 两个「未使用」count=0 标记 + 注册表区块
2. `02-注册表标签改色展开-浅色.png`——机器学习 chip 展开色板，当前色（色板 7）带选中环
3. `03-新增区块与云标签union-深色.png`——深色同景
4. `04-注册表标签改色展开-深色.png`——深色改色展开
5. `05-词汇板TagBadge取色-深色.png`——板块 chips 含临时词条标签、不含 count=0 注册表标签；列表徽章取色
6. `06-阅读页TagBadge-深色.png`——阅读页 meta 区 TagBadge 回落色

### browser-use 过程备注

- fullPage 截图在 IAB 有拼接伪影（sticky 侧栏重复/内容错位），交付截图全部改用视口截图（1440×900）。
- 临时词条 `notes/vocab/T1临时测试词.md`（tags: t1临时哈希标签）由文件系统创建验证回落色，验证后文件系统删除，未走 DELETE API。

## 审查

### 复检（2026-09-01，独立复检会话）

**方法**：以挑剔视角重走各功能模块——① 静态走查 feat 提交全量 diff；② API 边界强化矩阵（超出首验的用例）；③ browser-use 前端交互与反应式复测；④ 反因式排查（竞态/一致性）。测试条目复检后已全部清理（注册表恢复空 `{}`，dev 重启终验）。

**① 静态走查（全绿）**
- `1c62aea` 恰好触及 11 个预期文件；零改动清单（scanner/settings/gitCommit/index/templates/types/search.ts/tokens.css）diff 为空。
- `invalidateSearchIndex()` 调用点仍为 EditView/NewNote/NoteView 三处，tagRegistry 线未混入。
- routes.ts diff = 1 行 import + 两端点注册，无其他触碰；新增文件零散写 hex/rgba/ms/prefers-color-scheme。

**② API 边界强化（首验未覆盖的新用例，全部通过）**
- 长度边界：恰 32 字符 → 200；33 字符 → 400。颜色边界：0 → 200、7 → 200、-1 → 400。
- trim 语义：`"  边界测试  "` → 存储键为 trim 后的 `边界测试`。
- 特殊字符往返：emoji（`🏷️ emoji标签`）与 HTML/引号（`<b>bold</b>&amp"q"`）键精确存储、GET 原样返回；空 tag / 无 body → 400。
- **并发 10 连 POST**（同标签 5 色 + 异标签 5 个同时发）：全 200，磁盘 JSON 合法、同标签收敛单一确定色（后写赢）、GET 与磁盘一致；`upsertTag` 读-改-写全程同步无 await（事件循环天然串行）。
- **跨日期 created 保留真测试**：停服手工植入 `created: 2025-01-01` → 重启 → upsert 改色 → created 保持 2025-01-01 不变（首验仅同日验证，此为真跨日期证明）。
- data/ 目录全程仅 tags.json，无 `.tmp` 残留。

**③ 前端交互复测（全部通过）**
- 云标签 union：21 = 9 聚合 + 12 注册表 count=0；「未使用」组整体沉底且组内 localeCompare 升序；fontSize 零 `NaN`（零计数域防护生效）。
- XSS：HTML 形标签名在云标签/注册表 chip 均以纯文本渲染（0 个注入元素），Vue 插值转义成立。
- 前端拦截：33 字符输入 → 内联报错；Enter 键提交 32 字符 → 命中 API 已建标签，反馈「已存在，已更新颜色」（跨入口 upsert 语义正确）。
- UI 改色链路：chip 展开 → 点色板 → 服务端 color=5 落盘、色板自动收起、created 不变。
- 注册表色全站跟随：聚合标签「高频」指定色 4 后，词汇板 TagBadge 立即 tag-pair-4（#f5f3ff）；板块 chips 口径不变；c766bd8 整行可点拉伸层（row-main::after absolute）共存未破坏。

**④ 反因式排查（记录为已知语义，非缺陷）**
- **预载 GET 与 POST 竞态窗口**：`applyTagRegistry` 后若存在更早发出、尚未返回的预载 GET，其响应（旧态）会把 ref 覆盖回旧值。窗口仅存在于页面加载后数毫秒内（预载本地 ~10ms 返回），人工操作不可达；单用户本地场景记录为已知语义，不做加锁复杂化。
- 并发写：见 ②，同步串行安全。

**结论：复检通过，零缺陷、零修复、零代码变更。** 测试条目清理路径（停服手工编辑 JSON）再次演练确认。

### 复检后体验迭代（用户反馈，2026-09-01）

**用户反馈两条**：① 「创建标签并指定颜色」与底部改色区块功能重叠、边界模糊，一般用户不知道如何改色，要求提供其他改色设计；② 底部注册表改色区块「并未生效」，要求实现并截图核查。

**取证结论（真实点击路径）**：以 cua 坐标点击（原生输入事件，区别于此前兜底的合成事件）在播种注册表后全链路复测——chip 展开、点色 POST、云标签跟随**全部正常**（elementFromPoint 命中按钮本体，FAB 为 40/48px 定位按钮且仅 ≤767px 显示，无遮罩拦截）。「未生效」最可能为两种体验形态：① 用户查看时点注册表恰为清理后空 `{}`，区块 `v-if` 整体不渲染（区块消失被感知为未生效）；② 展开式色板在视口外展开（页面底部最后一行的色板落在折叠线下），点击后看不到任何变化。两者均为**交互设计缺陷**而非数据/请求缺陷——仍按反馈重做设计。

**新设计（常显色板，替换点击展开式）**：
- 标签页底部区块更名「**自定义颜色**」：每个注册表标签一行 = 色片（当前色 + 词条数/未使用徽标）+ **8 色板常显**，点色即改，无任何展开/收起步骤；空注册表时显示引导文案（「尚无自定义颜色的标签——在上方为标签命名并选色后，可在此随时调整」），区块不再凭空消失。
- **标签详情页页头新增取色行**（同款 TagPalette）：点任一云标签进入详情即可改色——最短路径；未注册标签色环落在 djb2 回落位，点色即创建注册表条目并全站生效。
- 顶部区块加边界澄清文案：「新名称将创建标签并应用所选颜色；输入已有名称则把该标签更新为所选颜色」（upsert 桥接语义保留——它是笔记携带标签获得自定义色的唯一入口，但不再是唯一改色入口）。
- 移除展开态代码（expandedTag/toggleExpand/registry-palette），失败反馈改为行内 `.row-error`。

**验证（真实点击 + 截图核查）**：
- 常显色板行渲染：3 行 × chip + 8 swatches，当前色环位正确，未使用徽标与词条数与云标签一致。
- 真实点击「搭配」行色板 1：server color 5→0、行 chip/色环/云标签三者即时变蓝（截图 `08-点色即改-改色后-浅色.png` 对比 `07`）。
- 详情页 `/tags/高频`：色环初始落 djb2 回落位（色板 7）→ 真实点击色板 5 → 注册表创建 `{color:4}`、列表徽章即时 tag-pair-4（截图 `10-详情页取色行-浅色.png`）。
- 浅深双主题截图：`07`/`08`/`10` 浅色 + `09` 深色；`npm run build` 全绿；测试条目清理后注册表恢复 `{}`。

**设计取舍记录**：曾评估「云标签内联色点 + 浮层色板」方案（触达最短），因基线对齐 flex-wrap 云内做绝对定位浮层需钳制、移动端无 hover 需常显 21 个色点噪音大而放弃；「云内浮层」模式在本仓 M5' `[[` 补全已有先例但复杂度不值。常显色板行零隐藏交互、移动端友好，为本选。

### 复检后体验迭代 · 第 2 轮（用户反馈，2026-09-02，整页重构）

**用户反馈**：自定义颜色区仍可优化——① 标签多时找不到要改的标签；② 一行一卡的排版随数量线性膨胀；③ 区块只列已自定义过色的标签（鸡生蛋：笔记携带标签必须先经顶部输入框建条目才有修改栏）；要求**从整页排布**出发提方案。提出三套整页方案（色卡墙统一画布 / 云+整理模式双态 / 词云+色卡册非对称构图）与两版美观向变体，用户拍板：**方案 A 色卡墙 + 全染**。

**实施（Tags.vue 整页重写 + TagPalette 增强）**：
- **云标签与自定义颜色区合并为色卡墙**：union 全量标签一卡一色，卡体全染（`.tag-pair-N` 洗底 + 文字色继承，scoped 零 background/color）；卡三职 = 颜色身份 + 浏览导航（stretched-link 整卡可点，NoteList 同配方，`--z-rail` 抬升色板）+ 改色（卡内常显 `size="sm"` 色板）。
- 徽标体系：`未使用`（count=0）/`自动`（未自定义，title 说明可点色自定义）描边小徽标，继承卡体文字色。
- **顶部创建卡片取消** → 网格末尾虚线幽灵卡：点击原位展开表单（autofocus 输入 + 色板 + 创建/取消 + Enter/Esc），upsert 语义保留（已有名称 = 更新颜色）；成功后清空筛选并收起（新卡/换色由墙自显，不再走文字反馈）。
- 页头保留标题+计数，新增**筛选输入框**（大小写不敏感子串过滤整墙，无匹配显示居中提示行）。
- TagPalette：新增 `size: 'sm'|'md'` 档（sm 14px 供三列网格卡内使用）；选中环缺口抽为 `--swatch-ring-gap` 变量（默认 `--color-bg`，全染卡上置 `transparent` 透出卡体洗底）——零新令牌。
- 交互细节：hover = 轻浮起 + `--tag-hover-overlay` 叠加（TagBadge 同语言）；入场 stagger 复用 `useStaggerArm` + `card-in`（cap 12、no-preference 门控，过滤重建零重播）；全染卡 recolor 后卡体即时换色。

**验证（真实点击 + 截图）**：结构断言（11 卡/ghost/筛选/全染 bg 命中 `--tag-4`）；真实点击色板 → server color + 卡类 + 卡体洗底 + 自动徽标消失四者同步；真实点击卡名 → 详情页导航（含取色行回归）+ back 返回；幽灵卡真实点击展开 → 键入 + 点色板 2 + 创建 → server 建条目 + 新染卡入墙 + 表单收起 + 筛选清空；筛选 "cet" → 仅 cet6/cet4；12 页 × 浅深 24 项零横滚；移动端 375 单列零横滚；preview 4173 生产构建复核通过；`npm run build` 全绿。截图：`11-色卡墙-浅色` / `12-色卡墙-深色`（夜光墨滴墙）/ `13-幽灵卡新建表单-深色`。

**本轮决策与范围说明**：词云字号加权随云退场（使用频率由排序与计数徽标表达）；排序切换（上轮模块③）与「恢复自动色」（模块①，需服务端新端点）用户未拍板，**均未实施**；标签详情页取色行不受影响照常保留。测试条目已清理，注册表交付态 `{}`。

### 复检后体验迭代 · 第 3 轮（用户反馈，2026-09-02，色卡墙微调）

**用户反馈三条**：① 各控件排布仍待优化；② 全染使卡内色板色点模糊；③ 选中色点的蓝环常亮（每卡一个），建议点击后亮起随即熄灭。评估：②③ 成立且有共同根因——全染后**卡体本身就是「当前色」的指示器**，色点与卡体同源令牌导致 pastel-on-pastel 模糊、常亮环职责已被卡体接管成为纯噪音；① 成立（卡面三段松散、筛选独占一行）。

**拍板方案与实施**：
- **两段式卡面**：信息行（名称 flex:1 左 + 元信息「N 条 ·自动/未使用」右，baseline 对齐）+ 控制区（色板），信息/控制视觉分组，卡高收窄。
- **surface 托盘**：`.card-palette` 包进 `--color-surface` 底胶囊（圆角 full、内边距 6px 10px、`justify-content: space-between` 均分铺满）——色点脱离卡体洗底回到中性底，模糊根除，控件暗示增强。
- **常亮环 → 点击脉冲**：TagPalette 新增 `pulse` 模式（色卡墙用）；点击后选中环亮起并沿 `--duration-pulse` 衰减熄灭（keyframes `swatch-pulse` 连同规则包 no-preference；JS 定时从令牌 parseFloat 推导）；`aria-pressed` 恒反映当前色（视觉与语义解耦）。ring 模式（幽灵卡表单/详情页取色行，无洗底宿主）维持常亮环不变。新增令牌 `--duration-pulse: 600ms`（tokens.css 动效块）。
- **筛选并入页头 + 排序切换**：页头一行 = 标题·计数 + [排序 seg] + [筛选框]；排序 seg（Board `.seg` 同族视觉）三态 = 自定义优先/名称/常用，偏好 `en_tool:tag-sort` localStorage 记忆（密度同款 try/catch 守卫），默认常用序。

**验证**：结构断言（11 卡两段式/托盘 bg= surface/卡内常亮环 0/seg 默认常用/页头含筛选）；真实点击高频卡色板 3 → `.pulsing` 即时出现 + aria-pressed 移位 + 900ms 后熄灭 + 卡体洗底换 `--tag-2`；排序三态（名称序/自定义优先首组 4 张自定义卡/常用默认）+ localStorage 持久化（重载保留）；12 页 × 浅深 24 项零横滚；移动端 375 零横滚；preview 4173 复核（11 卡 11 托盘 0 常亮环 + seg）；`npm run build` 全绿。截图 `14-色卡墙微调-浅色` / `15-色卡墙微调-深色`。测试条目清理后注册表 `{}`。

### 复检后体验迭代 · 第 4 轮（用户反馈，2026-09-02，中性色卡）

**用户反馈三条**：① 「自定义」排序名实不符（并无自定义顺序入口）；② 「自动」徽标不必留，仅标未使用即可；③ 浅色下白托盘 + 粉彩色点"累于辨认"，深色下卡体颜色发黑怪异、色点更难认（附双主题截图）。评估：①② 成立（命名与冗余问题）；③ 根因两层——**色点误用 `--tag-N-bg` 洗底令牌**（本为文字底色设计，小尺寸圆点上饱和度不足），**深色洗底令牌被放大到大卡面**（`rgba(色,0.12~0.14)` 本为近黑背景上的小徽章微光设计，成面后发闷发黑且各色发闷程度不一），调 alpha 不可救。

**拍板：方案一「中性色卡」+ 删「自定义」排序档**。实施：
- **卡体中性化**：`.tag-card` 回归 `--color-surface` + 1px 边框（列表行同族），颜色身份 = 左 3px 彩条 + 色名着色，两者经内联 `--card-accent: var(--tag-N-c)` 指向**饱和文字色**；全染洗底退场（hover 的 overlay 叠加随之移除，回到列表行同款 lift+shadow）。
- **tokens.css 新增 `.tag-dot-{0..7}` 应用类家族**：色点填充 `--tag-N-c`（饱和中调，浅 `#2563eb`/深 `#60a5fa` 档），TagPalette 色点由 `tag-pair-N` 改挂 `tag-dot-N`——浅深托盘上皆高辨识；`--swatch-ring-gap` 托盘上指向 `--color-surface-2`。
- 托盘改 `--color-surface-2` 在中性卡上形成凹槽层次；「自动」徽标删除（仅存「未使用」）；排序 seg 删「自定义」档（`SortMode = 'name' | 'used'`，localStorage 残留 `custom` 值被守卫拒绝回落默认）。
- TagBadge / 板块 chips / 详情页取色行等其余消费方零影响（tag-pair 家族未动）。

**验证**：结构断言（卡 bg=surface、border-left/色名=`--tag-4-c` 精确值、托盘=surface-2、自动徽标 0、常亮环 0、排序 chips=名称/常用、残留 `custom` 存储值回落默认）；真实点击高频卡色板 3 → 脉冲 + server=2 + 彩条/色名换 `--tag-2-c`（#b45309）；12 页 × 浅深 24 项零横滚；移动端 375 零横滚；preview 4173 复核通过；`npm run build` 全绿。截图 `16-中性色卡-浅色` / `17-中性色卡-深色`。测试条目清理后注册表 `{}`。

### 复检后体验迭代 · 第 5 轮（用户决定，2026-09-02，回退全染）

**用户决定**：中性色卡经实看后认为「色系处理后有点难看」——色点饱和度过高、与实际标签（粉彩洗底 + 饱和文字的 TagBadge，用户附图示其更好看）展示形式差距大；色点周围模糊似有噪点；整体观感不佳。**决定将标签卡回退到原来的全染设计**。另注：本轮实施时发现工作区存在一份并发会话未提交的「胶囊本尊卡」实现（identity-chip + 迷你徽章 2×4 色板，对应上上轮提案），已被本轮回退决定取代、未进入提交历史。

**回退口径（重要）**：恢复**迭代②的全染卡面视觉**——卡体 `.tag-pair-N` 洗底（文字色经继承）、色点 pastel 填充直接排布于洗底、hover 为 overlay 叠加 + 浮起、脉冲环缺口 `transparent` 透出洗底；**保留此后明确拍板的行为决策**：pulse 脉冲（无常亮环）、无「自动」徽标（仅存「未使用」）、排序仅名称/常用、页头合并（排序 seg + 筛选）、ghost 创建卡、整卡导航与入场 stagger。实施 = TagPalette 色点挂载回 `tag-pair-N`、tokens.css 移除闲置 `.tag-dot-{0..7}` 家族、Tags.vue 卡面结构与样式还原（card-name/card-meta/card-palette 三段堆叠）。

**验证**：结构断言（高频卡洗底 `#fff1f2`=--tag-5-bg、色名 `#e11d48`=--tag-5-c、色点 tag-pair 填充、自动徽标 0、常亮环 0、排序两态）；真实点击 cet6 卡色板 5 → 脉冲出现→熄灭 + server=4 + 洗底换 `--tag-4-bg` 薰衣草；12 页 × 浅深 24 项零横滚；移动端 375 零横滚；preview 4173 复核通过；`npm run build` 全绿。截图 `18-全染回退-浅色` / `19-全染回退-深色`。测试条目（含并发会话残留）清理后注册表 `{}`。

**已知取舍**：全染下色点直接排布于洗底，其辨识度弱于中性卡的对比（用户在迭代③曾报「调色盘模糊」）——本轮用户在「全染观感」与「色点对比」间明确选择前者，已知悉该取舍；若后续再议，候选方向为迷你徽章两 tone 色板（本轮并发实现已验证可行）。

### 复检后体验迭代 · 第 6 轮（用户指名，2026-09-02，恢复③排布形态）

**用户需求**：附迭代③时期截图指名「标签卡片调整为原本图中这个样式」，**明确不带「自动」徽标**（仅存「未使用」）。

**实施（Tags.vue 微调）**：卡片由迭代⑤的三段堆叠恢复为**迭代③形态**——① 两段式卡头（`.card-head`：名称 flex:1 左 · 元信息右 baseline 对齐）；② 色板装入 **surface 白托盘胶囊**（space-between 均分、圆角 full），色点脱离洗底。脉冲/排序两态/页头合并/ghost 卡/筛选等行为层零变动；「自动」徽标确认不复现。

**验证**：结构断言（11 卡 card-head 齐备/托盘 bg= surface 白/自动徽标 0/未使用徽标 2/常亮环 0）；真实点击首卡色板 6 → upsert 生效、脉冲后熄灭；12 页 × 浅深 24 项零横滚；移动端 375 零横滚；preview 4173 复核通过；`npm run build` 全绿。截图 `20-迭代六卡面-浅色` / `21-迭代六卡面-深色`。测试条目清理后注册表 `{}`。

### 复检后体验迭代 · 第 7 轮（用户提案+拍板，2026-09-02，全染渐层）

**用户需求两条**：① 全染卡面加渐变效果；② 深色下在保持全染的前提下让色彩不发黑（明确否决饱和色系路线）。评估：① 成立——洗底平色缺纵深，渐变端点用「透明 → surface」即可随卡自适应；② 根因是深色洗底（`rgba(色,12~14%)`）叠近黑页底亮度不足，解法 = **`color-mix` 让每张卡用自己的 `--tag-N-c`（文字色）与 `--tag-N-bg`（洗底）现场混合出「同色相、增亮度」的增强层**，再叠左上角文字色低透明辉光——不出现裸露高饱和大面积色（方案④教训），色相由自己的令牌携带。

**拍板**：对角 165° 渐变 + 深色标准档（辉光 17% / 增饱和 25%）。

**实施**：
- tokens.css 新增 `--card-sheen` 双主题定义：浅色 = `linear-gradient(165deg, transparent 38%, color-mix(surface 62%, transparent) 100%)`；深色 = `radial-gradient(左上辉光, --card-hue 17%) + linear-gradient(color-mix(--card-hue 25%, --card-wash))`（增饱和洗底层）。`--card-hue`/`--card-wash` 由卡内联注入（`--tag-N-c`/`--tag-N-bg`），零新颜色令牌。
- Tags.vue：`cardStyle()` 内联注入两变量 + stagger delay；`.tag-card` 增 `background-image: var(--card-sheen)`（只叠 image 层，不碰全局洗底 background-color）；hover 强化从 background-image 改为 **`::before` 透明度过渡覆盖层**（避免整体替换渐层，pointer-events 穿透）。
- 已知依赖：`color-mix` 需 2023+ 现代浏览器（项目基线内）。

**验证**：结构断言（11 卡内联 `--card-hue/--card-wash` 与各自 hash/注册表色一致、computed background-image 含 radial+linear 渐层）；真实点击脉冲正常；12 页 × 浅深 24 项零横滚；移动端 375 零横滚；preview 4173 复核通过（sheen/hue 注入全量生效）；`npm run build` 全绿。截图 `22-渐层卡-浅色` / `23-渐层卡-深色`（深色「夜光墨滴」：色相亮度显著抬升，不再发黑）。测试条目清理后注册表 `{}`。

## 已知限制

1. **原子写以代码走查为准**：tmp+rename 模式走查确认（Windows `renameSync` 语义为覆盖已存在目标）。「写大 JSON 途中 kill」的实测在 IAB + dev 环境无法安全定时注入，未实测；风险窗口极小（注册表当前仅数十字节，写入瞬时完成）。
2. **无删除/重命名**：按方案出界。注册表错误条目当前唯一清理途径 = 停服手工编辑 data/tags.json（本次测试条目即此途径清理）。
3. **多端并发改色后写赢**：内存态各实例独立、最后落盘者胜（单用户可接受，方案 §6 既定语义）。
4. **「未使用」标签仍可点入标签详情**：详情页显示空列表（0 条），属既有 TagDetail 行为，未为本期 specially 适配。
5. **count=0 标签字号恒为下限 0.85rem**：fontSize 线性加权在零计数域的退化为预期行为。
6. **IAB locator click 在遮挡态（hasFocus=false）下可能超时**：actionability 检查停滞，evaluate 派发 DOM click 可靠（已记入 AGENTS 浏览器验证节）。
7. **fullPage 截图在 IAB 有拼接伪影**（sticky 侧栏重复/内容错位）：交付物一律视口截图（1440×900）。
