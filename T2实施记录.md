# T2 实施记录 · v1.1 标签管理功能（删除 + 重命名）

> 状态：**已完成（2026-09-02）**。设计权威 = 根目录 `v1.1标签实体化设计方案.md`「T2 · 管理功能」章节。核心语义（深度删除 / 同步重命名 / 影响面三段式 / frontmatter 手术红线）由交接提示词定稿；入口落点、API 细节、交互形式等自由度归本会话。本记录边做边记，最终整理版保留全部关键事实。

## 执行计划（动手前定稿，实施未变）

### 核心设计决策（自由度部分）

| # | 决策点 | 采用方案 | 理由 |
|---|---|---|---|
| D1 | 管理入口落点 | **标签详情页（TagDetail）管理区**：取色行下方新增「管理」卡片，内联展开「重命名表单」与「删除确认面板」（互斥，至多展开一个）；色卡墙（Tags.vue）**零改动** | 详情页已持有 search index——页面本体（按板块分组的词条清单）即删除影响面清单，天然满足三段式预览；色卡墙八轮迭代定稿形态零回归风险 |
| D2 | 影响面数据来源 | **不设预览端点**：前端从 getSearchIndex() 计算（TagDetail 已加载）；服务端响应回报**实际** removedFrom/renamedNotes + warnings | API 面最小化；预览为参考、响应为事实，偏差由 warnings 机制如实上报 |
| D3 | API 形态 | `DELETE /api/tags/:tag` + `POST /api/tags/:tag/rename`（body `{ newTag }`）；响应统一 `{ registry, removedFrom?/renamedNotes?, warnings }` | 交接提示词推荐形态；Fastify find-my-way 对 `%2F` 按 raw URL 匹配后解码，含 `/` 标签可寻址（NFD/NFC 与中文路径参数实测通过） |
| D4 | tags 项删空处理 | **移除整个 `tags` 键行**（含该行行尾注释）；flow 单项与 block 唯一项同理；键与项之间的注释/空行一并移除，项后空行保留 | 留 `tags: []` 残留空键；移除键后 gray-matter 可解析、scanner tags=[] 兜底（实测 `---\n---\n` 空围栏合法） |
| D5 | 重命名新条目序列化 | `serializeTagScalar`：bare-safe 判定（非指示符开头、无结构字符/冒号/引号、首尾无空白、非布尔/数字/日期形）→ 裸值；否则**双引号 + 转义** | 新条目是手术点本位，不继承旧项引号风格；含逗号/空格等标签必须引号才合法（`__t2 新名,带引号` 实测写入 `"…"`） |
| D6 | invalidateSearchIndex 第 4 处 | **`lib/tagRegistry.ts` 的 `deleteTag`/`renameTag` 成功路径内统一调用**（mutationSeq 守卫之外无条件失效——词条已变更的事实不受守卫影响），并广播注册表 | 视图层无法遗忘；AGENTS「三处」已改写为「新建/编辑/删除词条 + 标签深度删除/重命名」 |
| D7 | git 自动提交 | gitCommit.ts 新增 `commitNotesBatch(message, relFiles)`：**精确 `git add -- <files>`** 本次操作实际改写的文件（posix 分隔符）；`commitNoteDeletion`（`-A notes` 旧语义）原样保留 | 精确 add 防止把用户未提交的真实数据（notes/vocab/abandon.md）卷进自动提交，也防验证期临时词条交叉污染；注册表 json 在 notes/ 外天然不入提交。**偏离「-A notes 既定语义」仅限 T2 新路径**，实测每次自动提交只含被操作文件（含 1 文件/5 文件多例） |
| D8 | 索引即时性 | 手术写盘后：`utimesSync` 恢复原 mtime → **立即调 scanner 重索引**（新增导出 `reindexNote`），不等 chokidar | writeNote 先例「写完即刻可读」：前端收到响应即失效重拉，等 chokidar 有 stale 窗口。**验证中升级为唯一路径**（见新发现 N1——mtime 保护使 chokidar 对手术写入失明） |
| D9 | 原型键防护 | 新端点对 tag/newTag 拒绝 `__proto__`（400）+ 注册表判存一律 `Object.prototype.hasOwnProperty.call` | `next[newTag] = ...` 对 `__proto__` 会写原型而非自有键，JSON 落盘静默丢条目 |
| D10 | ~~重命名后导航需 route watch~~ | **取消**：App.vue 路由 `:key="route.path"` 保证换标签整组件重挂载，`router.replace(新名)` 后全新拉取 | 实施前核查 App.vue 确认，无需补 watch |

### 文件 × 改动点

| 文件 | 改动 |
|---|---|
| `server/src/tagSurgery.ts`（**新建**） | frontmatter 手术唯一实现（红线见设计方案）：围栏定位 → 缩进 0 `tags:` 键行（嵌套同名键不算、重复键不安全）→ flow 单行 / block 列表分词（引号感知；嵌套/空项/跨行 flow/缩进续行 = 不安全）→ `parseScalar`（裸/单引号/双引号转义）NFC 整项匹配 → 删除（邻接逗号吸收、删空移除键行）/重命名（精确 span 原位替换）→ 按行段回拼（行尾风格逐字节保留）。`surgeryNoteFile` 封装 读→手术→恢复 mtime→立即重索引 |
| `server/src/tagRegistry.ts` | 新增 `removeTagFromRegistry` / `renameTagInRegistry`（先写盘后改内存、原子写复用 persist、hasOwnProperty 判存）；`upsertTag` 未动 |
| `server/src/scanner.ts` | 新增导出 `noteFilePath(board, slug)`（索引 filePath → 绝对路径，子目录词条安全）与 `reindexNote(filePath)`；其余零改动 |
| `server/src/gitCommit.ts` | 新增 `commitNotesBatch(message, relFiles)`（D7）；`commitNoteDeletion` 零改动 |
| `server/src/routes.ts` | 两端点（注册于 `registerRoutes()`，LAN 存活）+ `operateAffectedNotes` 共用手术循环（warnings 收集 + git 路径收集）+ NFC/carriesTag/`__proto__` 工具 |
| `web/src/api.ts` | `TagNoteRef`/`TagWarning`/`TagDeleteResult`/`TagRenameResult` + `api.deleteTag` / `api.renameTag` |
| `web/src/lib/tagRegistry.ts` | `deleteTag` / `renameTag`（mutationSeq 守卫 + apply + broadcast + **invalidateSearchIndex 无条件调用**） |
| `web/src/views/TagDetail.vue` | 管理区（重命名内联表单：预填/前端同规则校验/NFC 冲突早检/Enter-Esc/确认执行；删除内联面板：danger 面板 + 跨板块影响面逐条清单 + 实底红确认钮）+ 互斥面板状态 + 全令牌样式；**索引加载失败时不渲染管理区**（宁缺勿错删） |

## 偏离与理由

1. **「零额外索引代码」偏离为「立即重索引」（D8）**，且验证升级为**唯一索引更新路径**：探针实证手术写入后 chokidar **零事件**（见 N1）——若按原计划只依赖 watcher，索引将永久 stale。
2. **git 精确 add 偏离 `-A notes`**（D7）：会话中 `abandon.md` 存在用户未提交改动，`-A notes` 会将其卷入 T2 自动提交，违反「只读勿提交」红线；`commitNoteDeletion` 旧语义未动。
3. **手术实现两处字节保真 bug 在自测中发现并修复**（首版集成测试的字节 diff 抓到）：① `FLOW_KEY_RE` 未把冒号后空白收进捕获组，重建行丢 `tags: [` 的空格；② flow 项 span 含前导分隔空白，重命名/删除吞掉前项后的空格。修复 = 前缀组收编空白 + FlowItem 增加 trim 后精确 span（rawStart/rawEnd）用于手术。修复前曾产生的自动提交（`f58caa1`）以 `git reset --mixed a10c7d2` 丢弃、临时词条字节与注册表重置后**全量重测**。
4. **block 列表「顶格新键」曾被误判不安全**（单测抓到）：顶格（缩进 0）非项行 = 下一个顶层键，列表正常结束；缩进 >0 的非项行才可能是多行标量续行（不安全）。已修并加回归用例。
5. **测试期望值三处笔误**（算法行为正确）：不规则空白下「剩余字节原样拼接」、项后空行在删除区外保留、FM 仅剩 tags 时删空留下空围栏 `---\n---\n`（合法可解析）。单测最终 **20/20 通过**。
6. tags 删空选「移除整键」而非保留 `tags: []`（D4）；重命名写入词条的是 **NFC 形式新名**（与注册表键同源）。
7. `surgery-unit-test.mts`（20 例单测脚本）为会话内验证工具，验证后移除不入库；全部用例与结果记录于本文件。

## 新发现（边做边记）

- **N1 · chokidar 对手术写入失明（重要）**：mtime 保护把文件 mtime 恢复原值后，chokidar 将写入判定为无变化、**不触发 change/upsert**（探针日志仅 1 条 = 立即重索引，等待 3s 无第二条）。故 `reindexNote` 立即重索引不是优化而是**唯一**索引更新路径；后续任何「绕过 writeNote 直接改写词条文件」的功能都必须沿用此配方。
- **N2 · tsx watch 热重载竞态**：改 scanner.ts 后 tsx 重启子进程时撞旧子进程未释放端口（`listen EADDRINUSE`），**旧代码继续服务**（探针首次 0 命中的根因）。预案：改服务端代码后完整重启 `npm run dev`（杀树）而非依赖热重载。
- **N3 · 旧会话孤儿进程**：开场 `npm run dev` 时 3000/5173–5178 全被占（含一个旧代码服务端实例在响应 API，返回了内存中的旧注册表），按预案 `taskkill //PID <pid> //T //F` 清场后重测。
- **N4 · 用户数据基线中途变化**：会话首次读取 `data/tags.json` 为 2 条，数分钟后快照为 3 条（新增「高频 color 6」——旧实例在跑期间用户侧写入落盘）。以「当前磁盘」为用户数据基线并全程只读；收尾恢复后 sha256 与快照一致（`318e9884…`），abandon.md 同（`bc220ab4…`）。
- **N5 · IAB 遮挡节流重度发作**：本会话 rAF 探针 promise 挂死、截图 surface 超时、`setViewportSize` 超时、标签页被重置 about:blank 各发生；evaluate/快照全程可用。降级验证路线 = 功能断言全走 evaluate + domSnapshot，截图间歇重试（窗口回前台后恢复，全部补齐）。路由跳转冻结在 out-in 中间态（URL 已变、视图未换）为环境 quirk，一律以 goto+reload 呈现终态后再断言。

## 验证矩阵（全部真实执行）

| 项 | 结果 |
|---|---|
| 1 `npm run build`（server tsc + vue-tsc + vite） | ✅ 两轮全过（最终产物 `index-4eHZywUn.js` / `index-BQTXvvqn.css`） |
| 手术函数单测（20 例：flow 删/改/删空/引号/尾注释/不规则空白、block 删/删空/顶格键/空围栏、CRLF、子串安全、NFD 匹配、重复项改名/全删、无围栏/跨行 flow/无匹配不安全分支） | ✅ 20/20 |
| 2 删除矩阵 | ✅ 未使用注册表标签 200 卡片消失（removedFrom=[]）；幽灵 404；携带中标签深度删除 removedFrom=5 且逐文件仅 tags 行/项行变更（git diff HEAD~1 HEAD 为准：flow `tags: [keep1, 动词]`、block 项行删除、solo 整键移除、引号项 `'keep two'`/`動詞` 字节原样）；影响面清单与实际移除清单一致 |
| 3 重命名矩阵 | ✅ flow 裸项/flow 引号项/block/多板块（vocab×3+phrase+grammar）全部正确改写且其余逐字节不变、mtime 七文件全部未变；409 撞注册表 / 撞词条携带；400 同名/缺失/空白/33 字符/非字符串/数组 body/NFC 相等；404 幽灵；warnings 部分成功实测（跨行 flow 词条跳过、其余 5 条成功、文件零触碰） |
| 4 持久化 | ✅ 重启后注册表 = 用户 3 条 + `__t2_b`（精确一致）；删空键词条 gray-matter 可解析（GET tags=[]、FM 段无 tags 键） |
| 5 LAN 切换 | ✅ true→false 往返，`DELETE /api/tags/:tag` 双向 404 存活（registerRoutes 重注册实证）；NFD 路径参数归一命中 NFC 键 |
| 6 git | ✅ 自动提交出现且**只含被操作词条文件**（1 文件与 5 文件两例 `git show --stat` 核对）；`data/tags.json` 与 abandon.md 均不在列 |
| 7 前端全链路 | ✅ 影响面面板（数量+逐条清单）→ 确认 → 执行 → 色卡墙（卡片改名保色 pair 2 / 删除后消失）/列表徽章（`__t2_ui_re`）/搜索三处同步（板块筛选新名命中、消亡旧名 `__t2_a` 仅剩 warnings 词条持有属部分成功语义）；冲突早检内联报错；全程零 window.confirm（原生对话框探针为空） |
| 8 回归 | ✅ 12 页 × 浅深 24 项零横滚（/, 4 板块, 词条, 编辑, 新建, 标签, 标签详情, 设置, 404）；色卡墙定稿形态（全染/两段卡头/白托盘/pulse/ghost 卡）与用户数据（名词性从句 4/雅思 1/高频 6、abandon +1 展开）零破坏 |
| 9 browser-use | ✅ 按 quirk 预案：evaluate 派发点击、goto+reload 终态、截图重试；零 Playwright |
| 10 preview 4173 | ✅ 生产构建复核：色卡墙 19 卡 + ghost 卡 + 详情页管理区 |
| 收尾清理 | ✅ 8 个临时词条 fs 删除、注册表测试条目随 tags.json 快照恢复（停服手工）、终态 API 复核零 `__t2` 残留、用户数据哈希与快照一致 |

截图（`T2截图/`）：01 删除影响面预览-浅色 / 02 删除影响面预览-深色 / 03 重命名内联表单-深色 / 04 删除确认后色卡墙-深色 / 05 列表徽章同步-浅色 / 06 色卡墙改后状态-浅色。词条 frontmatter diff 佐证以 git diff 文本形式收录于上文验证矩阵 2/3 行（`git diff HEAD~1 HEAD` 全文见提交 `22f9d65`）。

## 已知限制

1. **部分成功语义**：文件手术失败（不安全形态/IO 错误）→ 跳过并计入 warnings，注册表侧照常完成；被跳过词条保留旧标签（djb2 回落色），重试同请求可收敛（404 前提已不成立时按「注册表或词条仍持有」继续）。注册表 persist 失败 → 500 时词条已改写（内存未变，重试收敛），未做跨文件事务回滚。
2. 不支持的 FM 形态一律**保守跳过 + warnings**：跨行 flow（实测）、嵌套结构、多行标量续行（缩进>0 非项行）、空项、重复 tags 键、无围栏/未闭合。`FLOW_KEY_RE` 贪婪取最后一个 `]`，行尾注释含 `]` 的罕见写法会被判不安全跳过（保守方向）。
3. 大小写保持敏感（B4 既定）；合并语义不支持（409 文案提示手工合并）。
4. ~~子目录词条实现上经索引 filePath 定位可正确手术，但本轮验证矩阵未含子目录样本。~~ **复检已关闭（见下）**。
5. 跨页签搜索索引无实时同步（既有限制，注册表部分有 BroadcastChannel）；他页签需重新进入页面才会拉新索引。
6. 板块页 chips 仍为「词条携带标签」口径（不含 count=0 注册表标签，T1 既定）。
7. 环境类：IAB 遮挡节流下截图/转场冻结（N5，非应用缺陷）；tsx 热重载竞态（N2，改服务端码后应完整重启）。
8. **复检补充**（低危观察，未修）：① `surgeryNoteFile` 中 `utimesSync` 若在 writeFileSync 后失败（概率≈0），文件已改写但按失败上报（warnings 口径偏保守）；② 详情页影响面/计数沿用页面级**精确**匹配（`tags.includes(tag)`，T1 语义），与服务端 NFC 匹配在「词条存 NFD 形、URL 传 NFC」的病理输入下预览可能少计——响应 `removedFrom` 始终为实际值，无数据风险；③ 前端 lib `deleteTag`/`renameTag` 在服务端 500（如注册表 persist 失败、词条已改写）时不失效搜索索引——UI 可能 stale 至下次进入页面。

## 复检（2026-09-02，本实施会话第二轮）

对 T2 全部功能模块独立复验：静态走查 + API 对抗矩阵 + 前端复测。**结论：核心功能零缺陷；发现并修复 2 个 git 自动提交缺陷（低危，`7e962f4`）；首轮已知限制④（子目录）关闭。**

### 静态走查

- [x] T2 提交区间（`a10c7d2..HEAD`）文件清单核对：仅 server 五件 + 前端三件 + 文档截图，**禁区零触碰**（markdown.ts / theme.ts / fonts / CommandPalette / EditView / tokens.css / tagColor.ts / NoteList / Tags.vue）。
- [x] 铁律 grep：`window.confirm`/`alert` 零命中；TagDetail 无 `prefers-color-scheme`、无 ms 字面值、无散写色值；新端点确认注册于 `registerRoutes()` 内。
- [x] `invalidateSearchIndex()` 全站 4 处调用点核对（NoteView/NewNote/EditView + lib/tagRegistry×2，后者不受 mutationSeq 守卫影响）。
- [x] tagSurgery/routes 代码重读：无阻断缺陷；两条低危观察记入已知限制⑧。

### API 对抗矩阵（首轮未覆盖面，全部实测）

| # | 用例 | 结果 |
|---|---|---|
| R1 | **斜杠标签** `a/b`：POST 建条 + `%2F` 路径寻址改名 → 文件 `tags: [a_x, keep_slash]`（`/` 无需引号） | ✅ |
| R2 | **日期形改名** → `2026-01-01`：文件写入带引号 `"2026-01-01"`，gray-matter 解析回 **string**（非 Date，类型漂移防线生效）；注册表键同名 | ✅ |
| R3 | **布尔形** → `true`：带引号写入，解析 `"true"` | ✅ |
| R4 | **空格名** → `two words`：bare 写入（内部空格合法），解析正确 | ✅ |
| R10 | trim 后同名（`two words` vs ` two words `） | ✅ 400 |
| R5 | **子目录词条**（notes/vocab/sub/）：noteFilePath 索引定位 + 手术正确改写 | ✅ **已知限制④关闭** |
| R6 | **重复项**：同一标签出现两次 → 重命名双改、删除双删，第三项字节原样 | ✅ |
| R7 | **幽灵携带**（不在注册表 + 唯一携带者为不安全形态）：改名 200 `renamedNotes:[]` + warnings 如实；删除同；链尾幽灵 404 | ✅ |
| R8 | `__proto__`：DELETE 与 rename newTag 双双 400 | ✅ |
| R9 | **CRLF 文件**：改名/删除后 `\r\n` 行尾逐字节保留、tags 行正确替换/邻接逗号吸收 | ✅ |
| — | 字节 diff + mtime：5 个被手术文件仅 tags 行变更、mtime 全部未变、不安全形态文件零触碰 | ✅ |

### 发现并修复（git 自动提交，`7e962f4`）

- **N6 · git stat 缓存失明（同 chokidar 失明 N1 同根因）**：mtime 恢复（updated 保护）+ **等长内容变更**（如 `sub2→sub3`）→ git 的 mtime+size 缓存完全命中 → `git add` 暂存空集 → `commit` 报 "no changes"。实测复现后定案：**两段 add**——普通 `git add -- <paths>`（收 untracked）+ `git add --renormalize -- <paths>`（强制重哈希已跟踪文件；其隐含 -u 不收 untracked，故两段缺一不可）。
- **N5b · 异步 add/commit 链交错**：连续快速操作时上一提交捎带下一操作已暂存的文件（复检实测 `48fb1c5` 捎带），并发 commit 还会撞 index.lock。修复：模块级 promise 队列串行化 + `git commit -m msg -- <paths>` pathspec 限定（--only 语义）。
- **修复复验**：停服重启后零间隔三连等长改名 → 三提交全落地、**每提交恰好只含当次操作文件**、零告警。

### 前端复测（合并卡）

- [x] 交互链：双行结构 + 嵌线（CSSOM 计算值）；重命名面板 Esc 关闭；冲突早检（输入「高频」→ 内联报错）；删除面板开/取消关，影响面计数正确。
- [x] 移动端 375×812：合并卡正常换行、零横滚。
- [x] 12 页 × 浅色走查零横滚（深色已于合并当日截图核查 07/08/09）。
- [x] 清理走 dogfood 路径：4 个测试注册表条目经 `DELETE /api/tags` 移除（顺带复验纯注册表删除路径），`data/tags.json` 哈希与用户基线快照逐字节一致（`318e9884…`），abandon.md 同（`bc220ab4…`）。

### 复检提交

`8094773`（第一版修复）→ `7e962f4`（完整修复：两段 add + 串行化 + pathspec）→ `471338a`（临时词条清理）。

## 独立审查（第三轮，2026-09-02，审查会话）

对已提交状态（HEAD = ed3aaa7）独立复验，**结论：通过验收，零代码缺陷**。全程 API 实测 + 临时词条五形态样本（flow 裸项/flow 引号/block list/唯一项/跨行 flow 不安全形态）+ 多板块携带：

- [x] **提交隔离与禁区**：T2 区间（ac10b29..ed3aaa7，27 提交 46 文件）服务端仅 routes/tagRegistry/tagSurgery（新）/gitCommit/scanner 五件 + 前端 api.ts/lib/tagRegistry/TagDetail 三件；markdown.ts、theme.ts、tagColor.ts、EditView、CommandPalette、NoteList、Tags.vue（色卡墙）、tokens.css 零触碰；notes/ 提交区间零变更。
- [x] **代码核对**：tagSurgery.ts（行段切割保 CRLF、围栏/顶层键/重复键防御、flow 引号感知分词含转义与单引号双写、精确 rawStart/rawEnd span、serializeTagScalar bare-safe 判定含布尔/数字/日期形、删空移除整键含邻接注释空白、block 顶格键终止列表、删除逗号吸收先后随再前置、NFC 整项匹配、64 次重复项守卫循环、utimesSync mtime 保护 + reindexNote 立即重索引）；routes（NFC/hasOwnProperty/__proto__ 400/carriesTag）；gitCommit commitNotesBatch（**两段 add：普通收 untracked + --renormalize 强制重哈希**、pathspec 限定 commit、commitChain 串行化）；lib deleteTag/renameTag（mutationSeq 守卫 + **invalidateSearchIndex 无条件调用在守卫外** + 广播）。
- [x] **独立实测**：
  - **重命名**：`__t2r_a → __t2r_b` 200，renamedNotes = 4（flow/quoted/block/solo），warnings = 1（跨行 flow 跳过、文件零触碰）；四文件仅 tags 项变更（引号项 `"__t2r_a"` → 裸 `__t2r_b`，serializeTagScalar 不继承旧引号风格为既定语义）、mtime 全部未变；**幽灵标签不入注册表**（ghost 携带者改名后注册表键集不变）；自动提交 `b0aec8f` **恰好含 4 个被操作文件**（无 tags.json/abandon.md/不安全文件）；
  - **边界**：409 撞词条携带（文案「合并请手工进行」）；400 同名 NFC/空；404 重命名与删除幽灵均正确（重命名 404 文案含标签名）；
  - **深度删除**：`__t2r_b` 200，removedFrom = 4 与实际一致；solo 整键移除（空围栏 `---\n---\n` gray-matter 可解析 tags=[]）、block 保留 keep3 项、flow/quoted 邻接逗号吸收正确；不安全文件零触碰；自动提交 `3c9cdd3` 恰 4 文件 2+/4-；
  - **持久化与 LAN**：重启后注册表与词条 tags 状态完整；lanEnabled true→false 往返端点存活；
  - **前端全链路**：详情页管理区（重命名面板预填旧名、NFC 冲突早检输入「高频」内联报错「已被」、提交后 router.replace 跳新详情页 `:key` 重挂载即新数据；删除面板「取消/确认删除」两态、确认后回 /tags 且卡片消失）；全程零原生 confirm；
  - **回归**：色卡墙定稿形态（全染/两段卡头/白托盘/ghost 卡）零回退；12 卡 union 显示正常。
- [x] **审查操作事故与修复（如实记录）**：① 清理临时词条时误用 `git add -u notes/` 将用户未提交的 abandon.md 改动卷入清理提交——**立即 `reset --soft` + `restore --staged` 修正**，终态提交 `24687d1` 仅含 5 个临时词条删除、用户 abandon.md 改动原样保留在工作区；② 超长标签管理测试中经幽灵卡表单误将用户标签「名词性从句」改色 4→0（表单默认选中色）——**即时经 API 还原为 4**，顺带行为级实证 upsert-on-existing 链路。两起均为审查操作失误，非应用缺陷，应用侧表现完全符合设计。
- [x] **残留清理**：5 个临时词条文件系统删除并 chore 提交（`24687d1`）；注册表无审查测试条目残留（终态 = 用户数据 名词性从句 4/雅思 1/高频 6）；审查标签页已关闭。
- [备注] 「已存在，已更新颜色」类瞬态反馈文案与 T1 同样未被审查探针捕获（选择器未命中），行为链路已实证；`npm run build` 产物 index-Iu2onne_.js。
