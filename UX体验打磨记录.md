# UX 体验打磨记录

> 体验陪测阶段（UI 焕新 M1'–M5' 收官后）：用户指位 → browser-use 取证 → 提案确认 → 实施 → 前后对比 → 记账。
> 每轮改动记一行；红线：零服务端变更、零新依赖、已定稿交互不改语义、样式走令牌。

| 日期 | 页面 | 问题 | 取证要点 | 改动 | 状态 |
|---|---|---|---|---|---|
| 2026-09-01 | 板块页 | 筛选控件排版（上会话遗留，用户已确认效果） | 筛选药丸 26px 高 / 12.5px 字号偏小；排序/密度控件与标签筛选项视觉语言未区分；控件组右对齐 | `.fchip` 放大（padding 4px 13px / `--text-sm`）；排序与密度改分段控件 `.seg`（灰底轨道 + 活动段白底浮起）；去 `margin-left:auto` 改左对齐 | ✅ 已提交 `6d62d7e` |
| 2026-09-01 | 全站侧栏 / 首页 | 首页「＋ 新建词条」漂浮于 720px 内容列右缘（右侧悬 256px 空白），与标题/搜索框无对位关系；实心蓝块权重最高而锚定最弱 | `.home-top` flex space-between 内 `.home-actions`；按钮 111×38 @ x=1073（视口 1440）；取证纠错：板块页本有「＋ 新建」小按钮，非"全站唯一入口" | 方案 A：按钮移入侧栏搜索框下方（`.new-entry` 全宽实心 accent，与 `.search-entry` 同宽 159×39 对齐、8px 间距），Home.vue 摘按钮保移动端 ⋯ 菜单；浅深双主题走令牌验证通过 | ✅ 已提交 `2340e22`（用户拍板采用，A/B 对比后选 A） |
| 2026-09-01 | 主侧栏 | 侧栏下半 353px 死空间、动作/内容/工具三组语义平铺无分隔、品牌区平淡；首版分隔线（1px + 8px 间距）过弱"看不太出来"——深挖发现真因是 Vite HMR 竞态致规则未渲染（同文件连续多次编辑必触发，本会话三度命中，**改后必须以 CSSOM 计算值为准验收**）；视觉增强经「线+微标签」迭代为**文字嵌线** `.nav-sep`（::before/::after 两侧拉线），用户指位"内容上方"实为**侧栏首页上方**，最终形态：`── 导航 ──`（新建按钮下方 12px）+ 四板块 + `── 更多 ──` + 标签/设置，上下对称；品牌 mark 换衬线「I」+ `--gradient-brand` 渐变（浅深两档新令牌） | 垂直剖面：nav 结束 y=493、foot y=846、空白 353px；HMR 竞态三次复现与 touch 重编译修复实录 | 侧栏分区分组（嵌线×2）+ 品牌标记个性化；方案 B（首页小节标题嵌线化）实施后用户澄清指位并要求回退，已恢复原 17.6px/600 裸标题（Home.vue 零 diff） | ✅ 已提交 `7a5682f`；首页部分已回退；组名「导航」为占位可换 |
| 2026-09-01 | 主侧栏 | 导航线上方四件套挤在 175px、间距节奏 12/8/12 阶梯差过小无段落感；实心蓝按钮 159×39 仅隔 8px 贴搜索框加重密度 | 间距实测：brand→搜索 12、搜索→新建 8、新建→嵌线 12 | 方案 A：brand→搜索与新建→「导航」线拉开至 `--space-5`（24px，阶梯无 20px 档取最近令牌），搜索↔新建保持 8px；随后用户要求「更多」线调回原距——嵌线分级：默认 12px（组内分隔）+ `:first-child` 24px（段落分隔），三级阶梯 8/12/24 各有语义；竞态四度命中按标准手法收口 | ✅ 已提交 `dd21735` |
| 2026-09-01 | 全站词条列表 | 视觉单元/悬停反馈/点击目标三重错位：行卡片蓝框悬停全域触发，但可点的只有左侧标题文字，点右侧无反应 | `NoteList.vue` 结构：`a.row-main` 仅含标题+IPA+来源；模板注释明示避免 `<a>` 嵌套 | 拉伸链接（纯 CSS ~20 行）：`.row-main::after` inset:0 铺满整行 + `.tag-badge` 定位提升保标签跳转 + 键盘焦点环随拉伸层圈整行（M7 focus-visible 约定）+ 标题变蓝放宽为整行悬停触发；不用 JS 委托保住中键/Cmd+click 原生行为；首页/板块页/AZIndex/标签页全站一致；验证：行右侧日期点击→词条页（首页+词汇板双验）、cet6 标签→标签页、hover 三重反馈联动截图 | ✅ 已提交 `c766bd8` |
| 2026-09-02 | 板块页/标签页/设置页 | 分段控件切换瞬移生硬；开关白胶囊点击不滑动——实锤两缺陷：选中态选择器 `input:checked ~ .thumb` 关系错位（thumb 是 track 子元素非 input 兄弟，永不命中）+ track 缺 `position:relative` 锚点（侥幸靠 label 落位）；回弹初版过冲 ~10% 偏夸张（用户拍板减弱） | Tags 排序 seg 为 Board 同族视觉但无滑块层（白底画在按钮上）；开关 track>thumb 结构 DOM 实测 | Board/Tags 分段控件移植白底滑块层（等宽双格 grid + translateX 0↔100%，零 JS 测量）；新增 `--ease-spring` 令牌（1.56→1.3 控制点，过冲 ~4%）+ `--duration-slow` 320ms 统三处手感；开关修复为 `input:checked + .track .thumb` + track relative；无害验证法：只设 DOM checked property 不派发 change（toggleLan 不执行、服务端零变更）；HMR 竞态第五/六度命中（第六度新变体：丢 **script** 模块更新——首轮验证全按旧代码走、`memory:null` 暴露） | ✅ 已提交 `fbdc3bf` |
| 2026-09-02 | 板块页 | 密度切换体感差异弱（紧凑仅收行 padding 一档 12→8px）；排序控件与标签挤同一行右侧；排序状态不记忆（回板块永远回默认）；短语板无字母目录 | 密度两档 padding 对比；board-filters 横向 flex space-between（chips y=171 与 seg y=168 同行）；sort ref 无 query 时空串、effectiveSort 才是生效排序——词汇板两值恒等掩盖分叉，短语默认 updated 序暴露「滑块在右索引却显示」 | 密度控件整体移除（类型/ref/localStorage/类绑定/seg 模板/:deep 覆盖六处，全板块统一舒适档，localStorage 旧键无人读取无害）；board-filters 纵向堆叠（标签行上、排序行下左对齐，8px 间距）；短语板开 AZIndex（组件板块无关零改动），索引显隐条件改用 **effectiveSort** 修复分叉；新增板块排序记忆 `en_tool:board-sort`：点击双写 query+记忆、显式 ?sort= 恒优先、无 query 回落每板块记忆、点当前生效序删 query 键观感由记忆接棒 | ✅ 已提交 `fbdc3bf`（`npm run build` 类型+构建全绿；六步记忆链路、四态索引断言、无害开关验证全过） |
| 2026-09-03 | 全站滚动/阅读页 | 滚动到底侧栏顶部被父容器拖出视口顶 -8px（跳动）；字母索引轨首屏底部裁切 18px；回顶按钮全站无一页面能触发（阈值 600 > 最长可滚 492）；移动阅读页正文仅 255px、表格竖排碎字且内滚永不触发（anywhere 逐字折让表格永不溢出）；h3 与正文同字号仅加粗（长词条扫读锚点弱） | 侧栏滚动三采样（0→-8）；全站 maxScroll 清单（93/213/492）；表格宽度侵蚀链 375→255px 三层 padding；单元格 5-6 行实测 | 侧栏 top 8px + calc(100vh-16px)（与浮卡边距协调）；索引轨字距 646→554px；回顶阈值 300px + 44px；阅读列宽三层收敛 255→287px；表格 v3「按词折 + 12em 列宽保底 + --text-sm」总宽 901→508px 内滚兜底；h3 升档 1.12em（19/17px，推翻 M2' 同字号定稿）+ 段距 1.05em + 移动正文 --text-md 16px | ✅ 已提交 `325df61`（npm run build 全绿；**真机不生效根因=扫码走 3000 端口 dist 构建产物，改动后须 rebuild**——已重建并四规则验证入产物；LAN 开关验证用 checked property 无害法；IAB 截图 surface 超时降级 evaluate 数据路线） |
| 2026-09-03 | 阅读页/编辑页（第二批） | 标题字号阶梯缺陷（h2 1.03em 与正文无层次、h3>h2 倒挂）+ 标题行高继承 1.8 大字号半行距悬空（标题-正文视觉间距 ≈ 行距）+ 标题上边距三级无差异且偏大 + 页头"偏左上"（操作组 131px 挤标题区）+ meta/按钮视觉噪音 + 无标签时空容器占位 + 编辑页标题说明冗余 + h2 快捷按钮与手敲习惯错配 | 标题-正文视觉间距 26px 拆解（10 margin + 9.6 h1 半行距 + 6.4 p 半行距）；h1 清零规则非 :deep 版永不匹配的 scoped 编译陷阱；删除确认态 CSSOM 枚举误报（真实路径验证无恙） | 标题阶梯 v3（h1 1.5em/h2 1.3em/h3 1.12em + 行高 1.25）；上边距递减收紧（1.0/0.9/0.8em = 26/20/15px）+ 段距 1.05em；页头纵向堆叠（column + 操作组左对齐）+ 方案 A 精简（meta 全灰 · 分隔单行、编辑/删除去框文字链接、:not(.confirm) 豁免确认态）；note-tags :empty 隐藏；编辑页标题说明移除 + h2 快捷按钮删除（h2 图标注册清理） | ✅ 已提交 `c109597`（npm run build 全绿；阅读页/编辑页双页验收；**CSSOM 枚举不完整教训修正**——验证以 computed + 真实交互为准） |

## 2026-09-03 · 反链区去标识

- 指位：NoteView「反向引用」section（abandon-oneself-to 页）——去除「反向引用」标题标识，仅留链接。
- 实施：删 h2.backlinks-title 及其样式；section aria-label 保留（无障碍），border-top 分隔线保留（布局分界）。无引用时整节隐藏逻辑不变。
- 验证：abandon-oneself-to 页反链区仅剩「长难句 2024 Text2 第三句」链接，标题无、aria 在、分隔线在。

## 2026-09-03 · 板块搜索提示按板块定制（方案 A）+ 移动端防截断

- 指位：搜索栏 placeholder 四板块同一句，「音标」维度仅词汇存在（其余板块 ipa 恒空），提示失真。
- 拍板：方案 A（按板块修正字段列表）+ 移动端精简文案防截断。
- 实施：Board.vue searchPlaceholder computed（useMediaQuery 767 断点切换）；桌面=词汇四项不变/其余去「音标」，移动=「过滤：标题 / 标签 / 音标|来源」（去低频「来源」留余量）；aria-label 不变。
- 验证：桌面/移动四板块文案矩阵全对；移动 input 312px 下文案 ~200px 无截断。

## 2026-09-03 · 搜索匹配字段排除音标

- 指位：音标不可键入，参与匹配只会造成噪声——从搜索匹配字段排除。
- 实施：searchBoard fields 移除 ipa（板块内过滤）；命令面板 Fuse keys 移除 'ipa'（结果项音标展示保留）；placeholder 同步去「音标」（四板块回归同一句「标题 / 标签 / 来源」，板块分支消失仅剩端型差异）。
- 验证：词汇页搜音标片段「ˈbæn」0 命中（修复前命中 abandon /əˈbændən/），搜「abandon」仍 1 命中。

## 2026-09-03 · 全局搜索阈值下调（threshold 0.35 → 0.2）

- 指位：取消字符分散命中（ser 搜出 perseverance 类）。0.2 恰是实现参数：3-4 字符查询的 1 错配分散命中分数 ~0.25-0.33 被拒，真拼写误差（serendipiy ≈0.09）保留。
- 实施：search.ts buildFuse（首页 SearchPanel 与命令面板共用工厂，两处全局搜索语义一致下调）。
- 验证：ser 只剩 serendipity（perseverance 被拒）；serendipiy/twinkel 仍命中。注意 IAB 节流冻结 transition 帧——thumb/动画类判据在节流 tab 是环境假象，以 aria-checked/键值为准。

## 2026-09-03 · 首页/命令面板搜索字段加上来源

- 指位：统一字段口径的第一步——首页 SearchPanel 与命令面板匹配字段加「来源」（对齐板块页）。
- 实施：search.ts BASE_KEYS/BODY_KEYS 加 source（全文=基础+body 一致性保持）；CommandPalette keys 加 source。
- 验证：搜「考研」（无标题含此词）——首页命中 serendipity+abandon（source「2024 考研阅读 Text 2」）；命令面板命中 2024 Text2 第三句/非谓语动词/serendipity/abandon 等，全部为 source 命中实证。

## 2026-09-03 · 首页搜索条目加来源展示

- 指位：首页搜索下拉的匹配条目显示来源说明（source 字段已参与匹配但结果不可见）。
- 实施：SearchPanel 条目加 item-source（行尾右对齐元信息位、超长省略号截断 + title 提示全文，secondary 小字；无来源不渲染）。
- 验证：搜「考研」五条结果行尾分别显示 考研阅读 Text 3 / 2024 考研阅读 Text 2 / 语法新思维 Unit 5——命中原因直观可见。

## 2026-09-03 · 首页搜索条目加标签徽章

- 指位：首页搜索条目加标签展示。
- 实施：条目加 item-tags 容器（纯展示 span 徽章，不带 RouterLink——条目整行已是跳词条 click，内嵌链接冒泡双导航；色对走全局 .tag-pair-N + tagColorIndex 注册表优先取色）+ 容器单行截断兜底防挤压行尾来源；紧随的 thumb 淡出修正（选中导入字体时预设 seg 两格均未选，thumb 停原格误导）。
- 验证：搜「考研」五条结果的徽章/色对/来源齐全（cet6=7、高频=6、雅思=1、考研=4 等稳定色）。

## 2026-09-03 · 搜索条目双行分层（方案 A）

- 指位：条目单行四段（标题/音标/标签/来源）在窄容器挤压溢出（标签裁半、标题折行错位、来源恒占 40%）。
- 拍板：方案 A 双行分层——行 1 主识别（标题+音标）、行 2 元数据（标签左+来源右推）。
- 实施：条目内容包 item-body（flex column），li 本身布局不动（组头行共用 .drop-item 类）；item-main/item-meta 各自 flex，截断链 min-width:0 保持。
- 验证：桌面 560 与移动 375 下五条结果全部双行、零横向溢出（scrollWidth≤clientWidth）、标签 3-4 枚与来源完整可见；组头行 flex-direction 仍 row。

## 2026-09-03 · 命令面板匹配字段收敛为标题+slug

- 指位：搜「词」因标签（动词/名词/名词性从句）大量命中词条，噪声大。
- 实施：CommandPalette keys = ['title', 'slug']（移除 tags/source）。动作组 label 匹配不受影响。
- 验证：搜「词」词条组仅剩「非谓语动词」（标题命中）；搜 slug 片段「2024-text2」仍命中。字段口径终态：板块页=标题/标签/来源（子串）、首页=标题/标签/来源（模糊 0.2）、命令面板=标题/slug（模糊 0.2）、[[ 补全=标题/slug（前缀/包含）。

## 2026-09-03 · 搜索逻辑专项复检（零缺陷）

- 静态走查：searchBoard/search()/CommandPalette/EditView 四消费者 + invalidateSearchIndex 失效链 + 动作过滤（子串+大小写正确）+ 组头/条目共用类隔离（item-body 包裹）+ fulltext 未就绪静默降级自愈 + 特殊字符安全（includes 纯字符串、Fuse 非 extended）——无洞。
- 实例矩阵：板块页特殊字符「(」/空格查询/标签词「高频」命中 ✓；首页「查看全部」→ /vocab?q=ser 联动（搜索框自动填充+结果过滤）✓；命令面板大写 RUNAWAY ✓、动作过滤「设」→仅设置 ✓、开→搜→Esc 关全流程 ✓；全文开关正文命中差异实证（搜「货车」关=0、开=runaway）✓。
- 两起验证手法假象（非应用 bug）：① Esc 合成事件派发 target=window 收不到 document capture 监听（真实键盘 target=input 传播链经过 document，无此问题）——修正为 document.dispatchEvent；② IAB 节流冻结 transition 帧致 thumb moved=false 假象（UI 高亮/键值正确）。

## 2026-09-03 · 阅读页加返回板块面包屑（方案 A）

- 指位：阅读页无页内返回板块入口（搜索/直链进来后只能靠浏览器返回或侧栏）。
- 拍板：方案 A——卡片顶部面包屑「← 词汇 · Vocab」（全称与板块页标题一致；aria 用短名「返回词汇板块」）；样式对齐编辑页 .back-link（secondary→hover accent，text-sm）。
- 实施：RouterLink 置于 note-header 之外（header 是横向 flex，面包屑须独占一行）；NoteView 本地加 BOARD_PAGE_LABELS 全称映射（反链徽章等窄位仍用短名）。
- 验证：桌面文字/href/aria/位置（header 之上）全对、点击跳 /v/vocab；移动 375 位于标题上方、无横滚。
- **修复（7380d25 引出）**：面包屑 `:to` 误带阅读页的 /v/ 前缀（板块页路由是 /vocab 非 /v/vocab）→ 点击 404（用户实测报障）。修为 `/${board}`；教训：**路由跳转验证必须断言落地页内容**（tab.url() 序列化异常返回 {} 掩盖了 404）。

## 2026-09-03 · 路由滚动管理（用户报障修复）

- 指位：板块页滚动后进词条，阅读页未从顶部开始（router 未配 scrollBehavior，SPA 切路由滚动位置被带过去）。
- 实施：router.ts 加 scrollBehavior——新导航回顶部（面包屑/搜索/面板跳转均适用），浏览器前进/后退恢复历史滚动位置（从词条返回板块页回到原滚动态）。
- 验证：板块页滚 272px 点词条 → 阅读页 scrollY=0；阅读页滚 300 → history.back → 板块页精确恢复 272.67px。

## 2026-09-03 · 移动端 meta 分隔点不对称修复（用户报障）

- 根因：移动端 .note-meta 被两个批次重复定义 gap——「移动排版」块列 gap 归零（间隔交分隔点对称 margin），后加的「页头精简」块单值 gap: var(--space-2) 把 8px 列间带回来——点左侧 16px（列gap 8 + margin 8）右侧 8px，2:1 不对称。
- 修复：页头精简块 gap 改双值 var(--space-2) 0（行 8 折行行距保留/列 0），注释标注勿再单值覆盖。
- 验证：metaGap 8px 0px、点盒 margin 8/8、两 div 间距 0——点两侧精确对称。
- 附带：期间 dev 服务因 tsx watch 热重载撞旧进程 EADDRINUSE 挂掉（改服务端 routes.ts 触发），已完整重启——铁律重演。

## 2026-09-03 · meta 分隔点移除（二次报障定稿）

- 二次报障：serendipity 三项 meta 移动端折行成两行，分隔点变行尾/行首悬空装饰（对称 margin 修复对折行场景无解——CSS 感知不到折行）。
- 定稿：移动端分隔点整体移除（点本就是移动端专属，桌面 meta 用 space-6 gap 从无点），行/列间距 12px + dt 灰/dd 白字色提供视觉分组。迭代中自查修正一次 gap 双值写反（列间距差点归 0）。
- 验证：桌面截图无点且两处间距均匀（32/32）；移动折行 dotCount=0、无横滚。

## 2026-09-03 · 音标移入标题行（用户指位）

- 指位：音标独立一行占纵向空间，移到标题行之后。
- 实施：p.ipa 移入 .title-row（标题后、朗读按钮前）；title-row 改 baseline 对齐 + flex-wrap（移动长标题+长音标挤不下时音标折行降级）；ipa margin 清零、align-self 默认 baseline；speak-btn align-self center 保持按钮视觉居中。
- 验证：桌面音标与标题同行（baseline 对齐截图确认）、移动 375 同行放得下、无横滚；页头高度收紧。

## 2026-09-03 · 朗读按钮改标题右上方小按键（用户拍板）

- 指位：音标移入标题行后，朗读色块贴在音标后打断文字流。
- 拍板：标题右上方小按键形态——24px 小巧按键（accent-soft 蓝底 + accent 喇叭 14px + radius-sm），顶部对齐（align-self flex-start + margin-top 1px）落在音标右上方；hover 实底蓝（color on-accent）。
- 验证：桌面/移动 375 截图确认按键位于音标右上、顶部平齐标题顶；无横滚。

## 2026-09-03 · 朗读按钮定稿：标题右上、音标之前、透明底（用户二次澄清）

- 澄清三处：位置=标题与音标之间（此前误放音标后）；形态=透明底（此前误用蓝底）；24px 灰色小喇叭，hover 浮现 accent + soft 底。
- 验证：DOM 顺序 title→speak-btn→ipa、透明底 rgba(0,0,0,0)、灰色、顶部对齐、移动截图确认。

## 2026-09-03 · 标题行间距收紧（用户指位）

- 指位：朗读按钮与标题、音标与按钮间距偏大。
- 实施：title-row 列 gap space-3(12px) → space-1(4px)——按钮距标题 4px、音标距按钮 4px（行 gap 8px 折行行距不变）。
- 验证：量化 title→按钮 4.0 / 按钮→音标 4.0，无横滚。

## 2026-09-03 · 移动端返回改板块图标钮（方案 A+C，用户拍板）

- 指位：移动端右上空旷 + 长标题与悬浮返回重叠风险——用户小想法：返回控件做成有创意的小图标（无文字）。
- 拍板：A+C——图标用各板块自己的图标（与底部导航同语言：词汇=book/短语=link/长难句=align-left/语法=graduation-cap）。
- 实施：面包屑 RouterLink 移入 title-row 参与布局流（结构性防重叠）；桌面 order:-1 + width:100% 独占首行复现文字面包屑（形态不变）；移动 order:3 + margin-left auto 排行尾、crumb-text 隐藏/crumb-icon 显示、圆形热区 + hover soft 底；aria/title 承载「返回板块」语义。Icon size 仅收 16|20 字面量。
- 验证：移动 375 图标右上可见/文字隐藏/无横滚；点击落地 /vocab 板块页非 404（断言落地内容）；桌面文字面包屑形态回归（icon 隐藏/文字显示/首行）。

## 2026-09-03 · 命令面板鼠标路径异常滚动修复（用户报障）

- 指位：鼠标放在面板上未动滚轮，从面板下边界离开时面板异常向下滚动。
- 根因：条目绑定 @mousemove=setActive（悬停项与键盘活动项共享高亮），扫过底缘裁切条目时 activeIndex 变更 → watch(activeId) 的 scrollIntoView(nearest) 把裁切条目完整滚入——鼠标路径不需要滚动（正看着它，滚走反而错位）。
- 修复：suppressScroll 标志——setActive（鼠标路径）置位抑制滚动，键盘 ↑↓ 与归零路径保留滚动跟随。
- 验证（矮视口 600 构造可滚列表 + 底缘裁切条目）：键盘 ArrowDown×13 scrollTop 0→34 跟随 ✓；mousemove 裁切条目 scrollTop 34→34 不变 ✓；再 ArrowDown 34→88.67 键盘路径完好 ✓。注：滚动容器是 .cmd-body（.cmd-list 为 visible，勿再探错对象）。

## 2026-09-04 · 数据目录面板：保存成功后按钮组退场 + 操作对等宽（用户指位）

- 指位（附截图）：保存成功后「取消/确认切换」仍以激活态与「已保存」横幅同屏——高亮实底主按钮呈现可再点击假象，与完成态语义冲突。
- 实施：ddApplied 后操作对整体退场，换成单个「完成」主按钮（closeDdPanel 收起面板并复位全部操作态）；未保存态按钮对不变；`.dd-panel-actions` 按钮加 min-width 88px 对齐（取消/确认切换/完成三态等宽）。
- 验证：vue-tsc 过；IAB 实测 isCurrent 短路态（无操作对，既有行为不变）、取消收起、toggle 语义完好；ddApplied「完成」态模板逻辑经编译校验（需真实 apply 才呈现，未拿用户配置做切换实验）。
- 追记（同日）：用户指位「确认切换是矩形、旁边按钮是圆角」——根因是 .dd-btn-primary 写成了修饰类却独立使用，缺 .dd-btn 基类（圆角/内边距/字号/指针全落浏览器默认）。三处主按钮（确认切换/完成/确认启用）补挂 dd-btn 基类，IAB computed 校验与取消键完全一致（radius 10px/padding 8px 16px/14.08px）。

## 2026-09-04 · 一键批量注册携带标签（G1.5，用户点名）

- 背景：目录切换暴露 union 语义盲区——未注册的携带标签「寄生」在词条上，切走即从标签页消失（用户连问两轮）。
- 实施：服务端 `POST /api/tags/register-carried`（仅增量、已存在不覆盖、NFC 归一、整批一次原子写）+ lib `registerCarriedTags`（聚合过滤已注册 + 颜色取 tagColorIndex 当前显示色=注册前后视觉零变化 + seq 守卫广播）+ 标签页时机化提示条（仅存在未注册携带标签时出现：列出标签名 + 「注册这 N 个」按钮 + 成功态文案）。
- 验证：curl 端到端——新增/重复注册不覆盖（高频 color6 与 created 保留）/NFC 两形态一次注册/非法入参 400 全过；注册表精确复原；空语料页提示条按预期隐藏。
- 注：提示条本身就是 union 语义的第一层揭示（方案 1）；静态徽标/页头说明等进一步可见性方案已提案待拍板。

## 2026-09-04 · union 语义可见化方案 2：色卡墙「未注册」徽标（用户拍板 1+2）

- 实施：词条携带但不在注册表的卡片，卡头计数旁加「未注册」虚线徽标（opacity 0.6 与「未使用」实线徽标区分），title 说明因果与出路（点色板/批量注册即固化）；注册后徽标即时消失（registry ref 驱动）。
- 顺带：用户要求切回默认目录并重启实测——提示条带 5 个标签（cet6/考研/搭配/语法核心/cet4）、8 卡全现、计数全对、5 卡徽标正确；IAB 截图仍整场超时未留图。
- 工具坑复现（非应用缺陷）：Bash cwd 跨调用持久——上次命令留在 web/ 时 `npm run dev` 只起了 vite（web workspace 脚本），server 未启动表现为 vite proxy ECONNREFUSED；**重启 dev 必须显式 cd 仓库根**。

## 2026-09-04 · 词汇/短语板块字母序空板块不显示空态修复（用户报障）

- 指位：字母序状态下空板块只渲染一排全禁用的字母索引轨，「暂无词条 + 新建词条」空态不出现（最近更新态正常——空态住在 NoteList 内部，AZIndex 分支拦截了空板块）。
- 修复：Board.vue AZIndex 分支条件补 `&& filtered.length > 0`——空板块落到 NoteList 自带的「暂无词条 + ＋ 新建词条」空态，零新增样式。
- 验证（Desktop\doc 空语料正好为测试床）：/vocab /phrase 字母序空态+CTA 出现、字母轨消失；/phrase?sort=updated 与 /sentence 行为不变；无报错。非空板块回归：条件仅对空数组追加放行，非空路径与原判定等价。

## 2026-09-04 · 新建标签表单展开不再拉伸同行卡片（用户报障）

- 指位：点击虚线新建卡展开表单后，同行的 cet4/cet6 标签卡被网格默认 stretch 撑高变形（82px→186px）。
- 修复：.card-wall 网格 align-items: start（行内顶对齐、不随最高项拉伸）；.ghost-card 单独 align-self: stretch——收起态虚线占位仍与同行等高（82px），展开态自身即行高来源。
- 验证（IAB 量化）：表单展开前后 8 张标签卡恒 82px 不变、表单卡 186px 自然高、取消后幽灵卡复原 82px；收起态等高语义保持。
- 追记（同日）：新建卡整卡热区——ghost-btn 绝对铺满虚线卡（inset:0 + flex 居中，拉伸层配方），点击卡内任意位置即展开表单；键盘焦点/读屏语义仍是这一颗按钮。实测角落命中+展开+收起正常。
