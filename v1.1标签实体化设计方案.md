# v1.1 · 标签实体化 设计方案

> 状态：**已定稿（2026-09-01 立项，基于独立评估确认）** → T1 已交付并通过复检 + 独立审查；**T2 · 管理功能已完成（2026-09-02，见 §8）**。
> 背景：UI 焕新阶段收官后的独立小阶段。用户在体验中提出：标签页希望可直接新增标签并可指定/后续修改颜色（词条来源标签保持随机色）。经评估，这要求标签从「frontmatter 派生聚合」升级为「可独立存在的实体」，**首次解除「零服务端变更」约束**（用户决策 2026-09-01）。

## 0. 目标与范围

**目标**：标签成为可独立存在、可携带颜色的实体；显式创建/改色经服务端持久化（跨设备生效）；词条来源标签保持 djb2 随机色不变。

**做**：`data/tags.json` 注册表、`GET/POST /api/tags` 两个端点、`tagColor.ts` 覆盖层解析、标签页新增区块（名称 + 8 色板取色）与云标签 union 显示、注册表标签改色入口。

**不做（明确出界）**：删除/重命名等管理功能（留待真实需求）、任意 hex 颜色（仅 0–7 色板索引）、板块页 chips 包含 count=0 标签、多用户/权限、localStorage 颜色方案（被本方案取代，陪测会话「想法 2」不再实施）。

## 1. 数据模型与存储

- **文件**：`data/tags.json`（仓库根新建 `data/` 目录，**置于 notes/ 之外**），UTF-8。
- **结构**：`{ "标签名": { "color": 0-7, "created": "YYYY-MM-DD" } }`；键 = trim 后非空、长度 ≤ 32 的标签名。
- **位置理由**：notes/ 内会让 chokidar 持续触发重扫抖动，且 DELETE 笔记的自动 `git add -A notes` 会把注册表变更卷进笔记提交；notes/ 外两者皆无。文件**纳入 git 跟踪**（属用户数据，随仓迁移）。
- **写入**：原子写（临时文件 + rename）。
- **损坏兜底**：JSON 解析失败 → 视为空表 + console 告警，**服务不拒启**。

## 2. API 契约（现有 9 端点零改动，9 → 11）

| 端点 | 行为 |
|---|---|
| `GET /api/tags` | 200 → 注册表全量对象 |
| `POST /api/tags` | body `{ tag: string, color: 0-7 }` → upsert：已存在仅更新 color；不存在创建（created=当天）；校验失败 400；成功 200 → 全量对象 |

- 校验：tag trim 非空、≤32 字符；color 为 0–7 整数。
- 无删除/重命名端点。

## 3. 前端设计

- **`lib/tagColor.ts`**：解析优先级 = **注册表 > djb2 hash 回落**；djb2 算法与 8 色令牌零改动（「换 hash = 全站重变色」铁律继续成立，hash 仅是回落）。注册表走轻量缓存（类比 `getSearchIndex` 的 Promise 缓存）+ 失效函数，改色后刷新。
- **标签页**（v1.1 实施后经六轮用户反馈迭代，最终**回退定稿为方案 A 原全染设计 + ③ 排布形态**，历程见 `T1实施记录.md`）：① 创建入口 = 网格末尾**虚线幽灵卡**（点击原位展开表单：名称 + 8 色板 + 创建/取消，upsert 语义：已有名称即更新颜色，无 409 分支）② **色卡墙 = union(笔记聚合, 注册表)** 全量标签一卡一色：卡体全染（`.tag-pair-N` 洗底、文字色经继承）、**两段式卡头**（名称左·计数或「未使用」徽标右）、整卡 stretched-link 进详情、surface 白托盘内色板点色即改（pulse 点击脉冲，无常亮环）③ 标签详情页页头同样提供取色行。
- **板块页 chips**：仅聚合**笔记携带**的标签，不含 count=0 注册表标签（板块页筛的是「词条拥有的标签」，语义注释写入代码）。
- **主题**：色板索引引用既有 `--tag-{0..7}-bg/c` 令牌，深色自动适配，零新令牌。

## 4. 既有决策改写清单（随 T1 回写 AGENTS.md）

1. 「零服务端变更」→ v1.1 起解除，服务端允许随需求演进（本方案为首个服务端变更）。
2. 「搜索与标签聚合全部在前端做，不加服务端搜索/标签端点」→ 保留半句：**笔记标签聚合仍在前端**；新增的是「标签注册表实体」端点，非聚合端点。
3. 数据模型章节：标签改为「双源」——笔记 frontmatter 聚合（派生）+ 注册表（实体），颜色解析注册表优先。

## 5. 里程碑与验收（T1 单里程碑，沿用实施 → 复检 → 独立审查流程）

> **✅ T1 已完成（2026-09-01，代码 `1c62aea`）**：验收要点 1–8 全部通过（API 契约矩阵 9/9、重启持久化、损坏兜底、watcher/git 零噪音、LAN 切换回归、三处取色一致 + hash 回落、标签页交互、12 页 × 浅深零横滚 + preview 复核、零新依赖）；实施记录 = 根目录 `T1实施记录.md`、截图 = `T1截图/`。§4 三条决策改写已回写 AGENTS.md。**已通过复检（2026-09-01，复检补记见实施记录「审查」节）**；独立审查待安排。

**验收要点**：
1. 注册表持久化：改色/新增后**重启服务仍在**；`data/tags.json` 入库。
2. upsert 校验：非法 body 400；重名 upsert 只改色。
3. 颜色全站生效：列表微章 / 标签页 / 云标签三处 + 浅深双主题；词条新增标签回落 hash 色。
4. 标签页：新增区块可用、count=0 标签带标记、改色即时生效。
5. 板块页 chips 口径：不含 count=0 注册表标签。
6. 原子写与损坏兜底：写入手工 kill 模拟不产生半文件；坏 JSON 不拒启。
7. watcher/git 零噪音：改色不触发笔记重扫、不卷入 DELETE 自动提交。
8. `npm run build` 通过；12 页 × 浅深走查零回归；零新依赖。

## 6. 风险对照

| 风险 | 对策 |
|---|---|
| watcher 噪音 / git 卷入 | 存储置于 notes/ 外（§1） |
| 颜色模型失控 | 仅 0–7 色板索引（§0） |
| 多端并发改色 | 后写赢（单用户可接受，记录为已知语义） |
| 空 JSON/半文件 | 原子写 + 损坏兜底（§1） |

## 7. 决策记录（2026-09-01 立项确认）

| # | 问题 | 决策 |
|---|---|---|
| 1 | 是否解除「零服务端变更」 | **解除**（用户决策），本方案为首个服务端变更 |
| 2 | 存储位置 | **notes/ 外 `data/tags.json`**，纳入 git |
| 3 | 颜色模型 | **0–7 色板索引**，不开放任意 hex |
| 4 | 管理功能（删除/重命名） | **留待后续**，本期不做 |
| 5 | localStorage 颜色方案 | **不实施**，由注册表取代 |

## 8. T2 · 管理功能（✅ 已完成，2026-09-02）

> 实施记录 = 根目录 `T2实施记录.md`、截图 = `T2截图/`。核心语义（深度删除/同步重命名/三段式确认/frontmatter 手术红线）由 T2 交接提示词定稿；本章记录最终语义与实际采用的 API/UI 设计。**✅ 完成提交：feat `d1a096e`（服务端+前端代码）、docs `6299a92`（本章节与实施记录回写）。**

### 8.1 语义（定稿，未改）

- **删除 = 深度删除**：注册表条目 + 全部携带词条 frontmatter 中对应标签项一并移除；影响面跨板块预览（数量 + 逐条清单）→ 内联确认（**禁 window.confirm**）→ 实施；标签不存在（不在注册表且无词条携带）→ 404。
- **重命名 = 注册表键改名（color/created 保留）+ 全部携带词条同步替换**；409 = 新名（NFC）已存在于注册表或任一词条（不做合并）；400 = 缺失/trim 空/>32/NFC 等于旧名；404 = 幽灵标签。
- **frontmatter 手术（红线级，`server/src/tagSurgery.ts`）**：只动围栏内 tags 项，其余逐字节原样（禁 gray-matter parse→dump 回写）；精确整项匹配（NFC，防「动」误伤「动词」）；支持单行 flow 与 block list、裸项与引号项；不安全形态（跨行 flow/嵌套/续行/空项/重复键）一律跳过并计入 warnings（部分成功如实上报）；改写后 `fs.utimesSync` 恢复原 mtime（updated 兜底排序不被污染）；tags 删空 → 移除整个 tags 键行。
- **索引一致性（T2 沉淀，重要）**：mtime 恢复使 chokidar 对手术写入**失明**（N1），手术写盘后必须立即 `reindexNote()`（scanner 新导出）——这是唯一索引更新路径，非优化。

### 8.2 API（11 → 13 端点，均注册于 registerRoutes，LAN 切换存活）

| 端点 | 行为 |
|---|---|
| `DELETE /api/tags/:tag` | → 200 `{ registry, removedFrom: [{board,slug}], warnings: [{board,slug,reason}] }`；404 幽灵；400 `__proto__` |
| `POST /api/tags/:tag/rename` body `{ newTag }` | → 200 `{ registry, renamedNotes, warnings }`；409/400/404 见 8.1 |

共用细节：键与匹配 NFC 归一（大小写敏感沿用）；写入词条的新名为 NFC 形式；`git add` **精确到被操作文件**（`commitNotesBatch`，不走 `-A notes`——保护用户未提交数据），注册表 json 不入提交；影响面预览不设端点（前端 TagDetail 已持 search index，页面本体即清单）。

### 8.3 UI 设计（自由度部分）

- **入口 = 标签详情页「管理」区**（取色行下方 surface 卡片）：「重命名」中性描边钮 +「删除标签」危险描边钮；两内联面板互斥展开；索引加载失败不渲染管理区（宁缺勿错删）。
- **重命名面板**：预填当前名，行内提示「将同步更新 N 个携带词条…」，前端同规则校验 + NFC 冲突早检（服务端仍权威），Enter 提交 / Esc 取消；成功 `router.replace(新名)`（路由 `:key=route.path` 整页重挂载即新数据）。
- **删除面板**：danger-soft 底 + 危险描边（missing-banner 同族语言），警示文案 + 跨板块影响面逐条清单（板块徽标 + 词条链接），「确认删除」实底红；成功回色卡墙；count=0 显示「仅从注册表移除」。
- 色卡墙（Tags.vue）**零改动**——八轮迭代定稿形态不回退。
- 前端失效：`lib/tagRegistry.ts` 的 `deleteTag`/`renameTag` 成功路径统一 `invalidateSearchIndex()`（全站第 4 处调用点）+ 注册表 apply + BroadcastChannel 广播。

### 8.4 T2 决策记录

| # | 问题 | 决策 |
|---|---|---|
| 1 | 管理入口落点 | TagDetail 管理区（详情页本体即影响面清单） |
| 2 | 影响面数据来源 | 前端 search index 计算，不设预览端点；响应回报实际结果 |
| 3 | tags 删空 | 移除整个 tags 键行（含行尾注释） |
| 4 | 新条目序列化 | bare-safe 判定，否则双引号转义 |
| 5 | git 提交范围 | 精确 add 被操作文件（偏离 -A notes 仅限 T2 新路径） |
| 6 | 索引更新 | 立即 reindexNote（chokidar 因 mtime 恢复失明，探针实证） |
