# 产品设计方案：EN_tool —— 个人英语学习知识沉淀库

> 版本：v1.1（复检定稿 · v1.0.0 已交付）
> 日期：2026-08-26

## 1. 产品概述

**一句话定位**：一个记录英语学习过程中知识重难点的个人知识沉淀库，Markdown 文件即数据，检索快、可互联、界面简洁美观。

**核心价值**：

- **零阻力记录**：手写 md 文件、粘贴 md 文本、界面内编辑，三种方式录入同一批数据。
- **零阻力检索**：全局搜索（模糊 + 精确）、A–Z 字典序目录、标签筛选、双向链接。
- **知识互联**：长难句 ↔ 词汇 ↔ 短语 ↔ 语法之间通过链接互相跳转，形成个人知识网。

**明确不做**（边界）：

- ❌ 不是背单词软件：无记忆曲线、无打卡、无复习提醒。
- ❌ 无云端同步、无多端、无账号体系：纯本地个人使用。
- ❌ 数据不上云：笔记保存在电脑本地，手机浏览器只是「遥控屏」——电脑开机且手机在同一局域网时才可用。
- ❌ 不强制内容模板：各板块、各词条的 md 正文格式完全自由。

## 2. 已确认的关键决策

| 决策项 | 结论 |
|---|---|
| 产品形态 | 本地 Web 应用（本地启动服务，浏览器访问） |
| 内容形式 | 纯自由 Markdown（不结构化拆解） |
| 发音音标 | 支持 IPA 音标字段 + 点击发音（TTS） |
| 复习机制 | 纯检索式知识库，不做复习提醒 |
| 编辑体验 | 分屏实时渲染（左源码 / 右预览） |
| 首页形态 | 大搜索框 + 四板块入口 + 最近添加 |
| 视觉风格 | 浅色极简（白底、留白、单一强调色） |
| 移动端访问 | 支持：同一局域网内手机浏览器访问，以阅览体验为优先（数据仍在 PC，手机为瘦客户端） |
| 访问密码 | 暂不需要；如后续在公共网络使用确有需要再补充 |
| 强调色 | 宁静蓝 `#3B82F6` |
| 版本管理 | 启用 git（后台静默提交，可回溯、误删可救） |
| 发音口音 | 默认美音（设置中可切换其他 voice） |
| 离家访问 | 不做（仅局域网内访问） |
| 前端框架 | Vue 3 + Vite + TypeScript |
| PWA | 暂不做（仅提供主屏图标，价值有限，后续如需再评估） |
| 标签体系 | 完全自由，不预设，随学习自然生长 |

## 3. 信息架构

### 3.1 四个板块

| 板块 | 说明 | 目录 |
|---|---|---|
| 词汇 Vocab | 单词词条，支持音标、发音 | `notes/vocab/` |
| 短语 Phrase | 固定搭配、习语 | `notes/phrase/` |
| 长难句 Sentence | 阅读中遇到的难句及结构分析 | `notes/sentence/` |
| 语法 Grammar | 语法知识点 | `notes/grammar/` |

### 3.2 页面结构

```
首页 /
 ├─ 顶部大搜索框（自动聚焦）
 ├─ 四个板块卡片入口（显示各板块词条数）
 ├─ 最近添加列表（按更新时间倒序，约 10 条）
 └─ 「新建词条」快捷按钮

板块页 /vocab /phrase /sentence /grammar
 ├─ 顶部板块内搜索框 + 「新建」按钮（直接在该板块新建词条）
 ├─ 词汇页专属：左侧 A–Z 竖排索引条 + 字母分组词条列表
 └─ 其他板块：词条列表（默认按最近更新倒序）+ 标签筛选

阅读页 /v/<板块>/<slug>（如 /v/vocab/abandon）
 ├─ 标题、标签徽章、来源、时间
 ├─ 音标（若有）+ 喇叭按钮（TTS 朗读）
 ├─ Markdown 渲染正文（含双向链接跳转）
 └─ 底部「反向引用」面板

编辑页 /v/<板块>/<slug>/edit
 ├─ 左：md 源码编辑器
 └─ 右：实时渲染预览

标签页 /tags 与 /tags/<标签名>
 └─ 标签列表（按词条数加权）/ 该标签的跨板块合集视图

设置页 /settings
 ├─ 局域网访问开关（默认关）+ 扫码二维码
 ├─ TTS voice 选择（默认美音）
 └─ 数据目录路径展示

左侧边栏（桌面端）含：四板块导航 + 标签云 + 设置入口。
```

### 3.3 移动端布局

手机浏览器通过同一局域网访问（见 5.8）。**移动端以阅览体验为优先**，编辑为从简的补充能力：

- 阅读页是移动端重点：舒适的阅读字号与行距、内容全宽、标签与反向引用面板自然下沉到文末。
- 底部导航栏（首页 / 词汇 / 短语 / 长难句 / 语法）替代桌面左侧边栏；标签页与设置入口收进首页右上角菜单。
- 词汇页 A–Z 索引条改为列表顶部**横向滚动字母条**（类似手机通讯录）。
- 搜索下拉面板触屏点选；桌面端键盘 ↑↓/Enter 导航保留。
- 编辑页移动端为**单视图**，提供「源码 / 预览」快速切换按钮（可用但不重点优化）；桌面保持分屏实时渲染。

## 4. 数据模型

### 4.1 文件规则

- **一个词条 = 一个 md 文件**，文件名即词条 slug，在板块内唯一；跨板块引用用「板块/slug」定位（见 4.3），同名冲突在保存时提示。
- 系统约定：目录结构 + frontmatter；**正文格式完全自由**。

```
notes/
├── vocab/
│   └── abandon.md
├── phrase/
│   └── abandon-oneself-to.md
├── sentence/
│   └── 2024-text2-sentence3.md
└── grammar/
    └── 非谓语动词.md
```

### 4.2 frontmatter 约定

```yaml
---
title: abandon          # 显示标题（无 frontmatter 时以文件名兜底）
ipa: /əˈbændən/         # IPA 音标（可选，任何板块可用；词汇常用）
tags: [cet6, 高频]      # 自由标签（可选）
source: 考研阅读 Text2   # 来源记录（可选）
created: 2026-08-26     # 系统自动写入
updated: 2026-08-26     # 系统自动维护
---
```

手写的 md 若无 frontmatter：系统以文件名作为 title 收录，tags / source 留空，created / updated 自动补齐。

### 4.3 双向链接语法

- `[[abandon]]`：默认在词汇板块查找，未命中再依次查其余板块；
- `[[grammar/非谓语动词]]`：显式指定板块，精确到板块不回落；
- 全部未命中时渲染为红色虚线下划线，点击可直接创建该词条（stub 创建）。

### 4.4 参考示例（骨架仅为建议，可自由删改）

**词汇**：

```markdown
---
title: abandon
ipa: /əˈbændən/
tags: [cet6, 高频, 动词]
source: 2024 考研阅读 Text 2
---

# abandon

**词性**：vt. 抛弃；放弃

**释义**：...

**搭配**：
- abandon oneself to 沉溺于

**例句**：
- 详见 [[sentence/2024-text2-sentence3]]

**近义词辨析**：...
```

**长难句**：

```markdown
---
title: 2024 Text2 第三句
tags: [考研, 名词性从句]
source: 2024 考研阅读 Text 2
---

# 2024 Text2 第三句

**原句**：...

**结构拆解**：
- 主干：...
- 从句：...

**重点词汇**：[[abandon]]、[[phrase/abandon-oneself-to]]

**翻译**：...
```

## 5. 功能设计

### 5.1 首页

- 大搜索框自动聚焦，输入即搜（防抖约 200ms），下方下拉面板动态展示匹配结果。
- 四板块卡片：名称 + 词条计数，点击进入板块。
- 最近添加：按 `updated` 倒序取最近 10 条（跨板块），点击直达。
- 新建词条：先选板块 → 弹出小表单（标题、标签、来源）→ 生成带 frontmatter 的骨架进入编辑页。

### 5.2 搜索

- **范围**：默认搜标题 + 标签；切换「全文」后搜索正文内容。
- **模糊匹配**：Fuse.js，容忍拼写错误；**精确匹配结果优先展示**。标题为中文的板块（语法/长难句）同样支持模糊匹配。
- **结果分组**：按四板块分组，每组展示前 5 条 + 「查看全部」。
- **键盘导航**：↑↓ 选择、Enter 打开、Esc 关闭。
- 板块页顶部有同款搜索框，范围限定当前板块。

### 5.3 词汇板块（专属）

- 左侧竖排 A–Z 索引条，点击字母滚动到对应分组；无该字母词条时置灰。
- 词条行显示：单词、音标（若有）、标签徽章。
- 组内按字母序排列。

### 5.4 阅读页

- 头部：标题、标签徽章（点击跳标签页）、来源、创建/更新时间。
- 音标斜体显示 + 喇叭按钮，点击用浏览器 speechSynthesis 朗读词条标题（默认美音，设置中可切换其他 voice）。
- 正文 Markdown 渲染，`[[链接]]` 呈现为可点击链接。
- **选中任意文本**弹出浮动工具条「朗读」——长难句整句跟读、短语连读。
- 底部反向引用面板：列出所有正文中链接到本词条的其他词条。
- 操作：编辑、删除（二次确认）。

### 5.5 编辑页

- 分屏：左侧 md 源码编辑器，右侧实时渲染预览（防抖更新），Ctrl+S 保存。
- 保存后自动更新全文索引与链接图。
- 支持把本地 md 文件拖入编辑页读入内容；也支持直接粘贴 md 文本。
- 自动保存（防丢失）：编辑暂停约 3 秒后静默保存草稿。
- 移动端：单视图 + 「源码 / 预览」切换按钮（窄屏不足以分屏，编辑从简）；桌面端保持分屏。

### 5.6 标签页

- `/tags`：标签云，字号按词条数加权。
- `/tags/<标签名>`：该标签的合集视图，按板块分组展示。

### 5.7 文件监听

- 应用启动时扫描 `notes/` 全部 md，解析 frontmatter + 正文，构建全文索引与链接图。
- 运行期间监听目录变化（chokidar），你随手往文件夹丢的手写 md 会自动收录，无需重启。

### 5.8 手机浏览器访问（局域网）

- 服务默认只监听本机（`127.0.0.1`）；设置中开启「局域网访问」后监听 `0.0.0.0`，并在终端打印**二维码**，手机扫码即打开（`http://<电脑IP>:端口`）。
- 手机端重点保证阅览体验：搜索、浏览、朗读、标签跳转完整可用；编辑可用但从简。TTS 音色随手机系统语音库，与桌面略有差异。
- 选中朗读在手机上通过长按选中文本触发，浮动工具条与系统原生菜单共存。
- 安全说明：开启局域网访问后，同一 Wi-Fi 下的其他设备理论上也能访问你的笔记；当前不做密码保护（家庭网络场景风险可接受），后续确有需要再补充。默认不开启局域网访问。

## 6. 英语学习特性清单

- **IPA 音标**：frontmatter `ipa` 字段，阅读页以斜体 IPA 字符渲染（字体需覆盖音标字符，如 Noto Sans）。
- **词条朗读**：TTS 朗读，默认美音，设置中可切换其他 voice。
- **选中朗读**：任意文本选中即读，服务于长难句跟读、短语连读、例句纠音。
- **来源记录**：`source` 字段记录出处（哪篇阅读、哪部剧），复盘时还原语境。
- **例句 ↔ 长难句联动**：词汇条目的例句若本身是长难句，用 `[[sentence/...]]` 链接过去，形成"词 → 句"的检索路径。
- **建议骨架**（仅新建时生成，可自由删改）：
  - 词汇：词性 / 释义 / 搭配 / 例句 / 近义词辨析 / 词根词缀
  - 短语：释义 / 用法 / 例句 / 易混辨析
  - 长难句：原句 / 结构拆解 / 重点词汇链接 / 翻译
  - 语法：规则 / 例句 / 易错点 / 对比辨析

## 7. 视觉设计

- **配色**：白底 `#FFFFFF`、页面背景 `#FAFAFA`、主文字 `#1F2937`、次要文字 `#6B7280`、强调色宁静蓝 `#3B82F6`、标签徽章用浅色底 + 强调色文字。
- **字体**：西文 Inter / 系统默认；中文思源黑体 / PingFang；IPA 用含音标字符的字体（Noto Sans 系列）。
- **布局**：左侧窄边栏（四板块导航 + 标签云 + 设置入口）+ 内容区；阅读正文居中、最大宽度约 720px 的舒适阅读栏宽。
- **质感**：大量留白、圆角卡片、细微边框、克制的 hover 微交互；无弹窗轰炸，编辑/新建用页面而非模态为主。

## 8. 技术方案（建议，实现阶段可再调）

| 层 | 选型 | 说明 |
|---|---|---|
| 本地服务 | Node.js + Fastify | 提供文件读写与搜索 API |
| 前端 | Vite + Vue 3 + TypeScript | 单文件组件直观，适合中小型项目 |
| Markdown | markdown-it + gray-matter | 渲染 + frontmatter 解析 |
| 模糊搜索 | Fuse.js | 前端即时搜索；全文索引在服务端构建 |
| 文件监听 | chokidar | 手写 md 自动收录 |
| 局域网访问 | 服务绑定 0.0.0.0 + 二维码 | 默认仅本机；设置中可开启 |
| TTS | 浏览器 speechSynthesis | 零依赖 |
| 数据管理 | 纯 md 文件 + git 版本管理（启用） | 每次修改可回溯，误删可救 |

工程结构示意：

```
en_tool/
├── notes/              # 你的数据（四个板块文件夹），可整体迁移
├── server/             # 扫描、索引、API、文件监听
├── web/                # 前端 SPA
└── 产品设计方案.md
```

## 9. 开发路线图（v1.0.0 已全部完成，2026-08-30）

| 里程碑 | 内容 | 状态 |
|---|---|---|
| M1 骨架 | 服务端扫描 + API；首页；板块列表；阅读页渲染 | ✅ 完成 |
| M2 检索 | 全局/板块搜索（模糊 + 精确）；A–Z 目录；标签页 | ✅ 完成 |
| M3 移动端 | 响应式布局、底部导航、横向 A–Z 条、移动端阅读体验、扫码访问 | ✅ 完成 |
| M4 录入 | 编辑页分屏实时渲染；新建骨架；拖入/粘贴 md；自动保存；移动端单视图切换 | ✅ 完成 |
| M5 互联 | 双向链接渲染与跳转；反向引用面板；stub 创建 | ✅ 完成 |
| M6 发音 | IPA 渲染 + 词条朗读 + 选中朗读 | ✅ 完成 |
| M7 打磨 | 视觉细节、失效链接提示、删除流程、悬停预览 | ✅ 完成 |

> v1.0.0 各里程碑的实现偏离与测试修复记录见 `AGENTS.md`「偏离事实记录」；测试报告见 `docs/v1.0.0/`。

## 10. 可扩展项（未来可选）

- [ ] Tauri 打包成桌面应用（零成本迁移）
- [ ] 数据导出 zip 备份
- [ ] 局域网访问密码（如后续在公共网络使用确有需要）
- [ ] PWA 主屏图标（如手机访问频率高，可再评估）
